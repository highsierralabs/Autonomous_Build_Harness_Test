# RHACO-HND-20260903-001 -- Handback (sessions 1-4)

| Field | Value |
|---|---|
| Hand | `docs\handoffs\RHACO-HND-20260903-001_Corpus_Explorer_Autonomous_Build_Dispatch.md` -- Class C, route `ops`, execution_mode sequential-rounds |
| Parent | RHACO-CMP-20260903-001 (M0 complete on the workspace side, session 3; the CMP section 8 M0-closure entry rides the next docs hand per section 6) |
| Executor | Claude Code, observatory PC; client 2.1.260; session model Claude Fable 5.1 (`claude-fable-5-1`); effort xhigh -- identical in sessions 1, 2, and 3 (no version or model change; section 2 M halt condition not triggered) |
| Session 1 window | 2026-09-04T00:20Z - 00:40Z (2026-09-03 PDT evening) -- section 0 halted at item 7 |
| Session 2 window | 2026-09-04T00:57:46Z - 01:32Z -- re-entry per section 2 M; item 7 discharged; items 8-14 evaluated; section 0 halted at item 9(b) |
| Session 3 window | 2026-09-04T01:53:09Z (client start) - open -- Director-directed Rule-2 commit and push of the A1 pair (`3b860bf`); re-entry per section 2 M with Amendment A1 applied; item 9(b) discharged as amended (A1.2); Part A (M0) COMPLETE 02:24:07Z; Part B rounds 0-1 complete; closed 2026-09-04T03:50Z at the round-1 boundary -- sections J, J.7 |
| Session 4 window | 2026-09-04T23:39:34Z (client start) - 2026-09-05T02:03Z -- under RHACO-CCX-20260904-001 section 4.1 and the Director's S4 launch note: Rule-2 commit and push of the CCX pair (`4032742`); criterion-6 reconciliation; re-entry per section 2 M with A1 (PASS); Director ruling AC-3; correction round (probe round 2, corpus_adapter round 2); wave 2 -- sections K, P |
| **Status** | **Section 0 PASS in full as of session 3: the session-2 halt at item 9(b) is closed by Amendment A1 (A1.2 -- variables present, spawn depth holds, concurrency non-enforcement recorded; wave cap orchestrator-enforced per A1.3). **Part A (M0) COMPLETE** at 02:24:07Z (section J.4: workspace commits `e9c9539`, `8da0712`). **Part B: rounds 0 and 1 complete** -- round 0 inspection + CONSTRAINTS / ARCHITECTURE / SCOPE / REFERENCE / LAYERS; round 1 / wave 1 (corpus_adapter, probe, diagnostics) built, merged, integrated (52/52 tests, L1 PASS, probe production run PASS, L4 oracle SCOPE); 3 of 28 builder rounds consumed. Session 3 closed at the round-1 boundary 03:49Z; boundary commit `62685da` pushed (first push, O-1). Part C not reached.** **Session 4 (2026-09-04T23:39Z - 2026-09-05T02:03Z): section 0 re-run PASS; CCX-20260904-001 pair filed (`4032742`, pushed red, attribution-closed); AC-3 ruled; correction round (probe R2 predicate, adapter R2 guard) merged at `c96664dd`; wave 2 = round 2 (catalog, search, reader) + probe round 3 (wave-2 runners; Q0-Q2 qualified; production presets PRODUCT_EVIDENCE_PASS) merged; 181 tests; 9 of 28 builder rounds; round-2 boundary `6a83537` pushed (O-1). Part C not reached. Sections K, P.** |
| Findings file | `working\preflight_findings_HND-20260903-001.md` -- session 1 record (items 1-8, host residuals) + "Session 2 re-entry" record (items 1-14 with command output, pins of record, the 18-strand item 9 probe record, section 0 verdict) + "Session 3 re-entry" record (A1 pre-findings note, re-pins 1 / 5 / 6 / 7 / 8 / 9 / 14, db-pin reconciliation, 9(b) as amended, item 12 VERIFY answer) |

## A. Pre-findings note -- push of `main` under Director ratification (2026-09-03)

**Act.** `git push --follow-tags origin main` from `C:\highsierralabs\RHACO` at **2026-09-04T00:20:32Z**: `32195e1..0243874  main -> main`. Verified after the push: `origin/main` = `HEAD` = `0243874489268d7a78a2e2e6d8d39eeb144ef522`; `git ls-remote origin refs/heads/main` agrees; `main...origin/main` in sync. Five commits went up, all Rule-3-conformant, no tags:

| Commit | Subject |
|---|---|
| `2d8258b` | `RHACO-CMP-20260903-001: charter Corpus Explorer build harness test` |
| `4fc46ea` | `RHACO-CHG-20260903-001: file Subagent Guide v1.3 model-tier CHG` |
| `b9ed835` | `RHACO-HND-20260903-001: file Corpus Explorer build dispatch hand` |
| `376f5d6` | `RHACO-CCX-20260903-001: session handoff for build dispatch` |
| `0243874` | `RHACO-CHG-20260903-001: Subagent Guide v1.3 model-tier rule D-13` |

**Gate state at push -- pushed red.** The SOP-708 closeout gate run after `0243874` (tree clean) returned:

```
librarian        : PASS  (13.3s)
regression       : FAIL  (30.6s)
auditor          : FAIL  (19.3s)   baseline compare MISMATCH (+33/-0)
structure-guard  : PASS  (0.1s)    13/13 cases
git-status-clean : PASS  (0.0s)
overall          : FAIL  (exit 1)
```

The `regression-gate.md` standing-exception registry is empty, so the gate was not accepted and the `/ho` step-17 rule withheld the push. The push was then taken under **explicit Director ratification, 2026-09-03** (instruction: "push main (5 ahead) to origin/main; no baseline writes, no `--write-baseline`, no `tests\` edit"). As directed: **no baseline written, no `--write-baseline`, no `tests\` edit**, no force, no amend, no history rewrite. This note is the record that the red was known, attributed, and pushed by ratification rather than by acceptance.

**The three red items, their attribution, and the delta attributable to the pushed commits -- 0 / 0 / 0.**

| # | Red item (gate member) | Baseline it is measured against | Attribution of record | Delta attributable to the five pushed commits |
|---|---|---|---|---|
| 1 | regression: `tests\regressions\test_required_claim_floor_v9.py::test_no_other_renderer_behaviour_changed_across_the_archived_corpus` | `docs\regression_baseline.json` `"failing": []` (seeded 2026-07-13) | **RHACO-INC-20260829-001** (bounded gate exception ratified 2026-08-29, INC line 86) | **0** -- red in the pre-edit baseline run of this session; counts identical before and after (824 passed / 1 skipped / 2 failed) |
| 2 | regression: `tests\regressions\test_corpus_index_self_edges.py::test_self_edges_are_dangling_cites` | same | **RHACO-INC-20260830-001** (`SELF_EDGE_BASELINE` pin stale across five amendment hands; INC line 15) | **0** -- same evidence; a documents-route hand cannot reach either test |
| 3 | auditor: `unwaived_gating=344`, baseline compare MISMATCH, **+33 / -0** | citation-auditor gating baseline (auditor v1.3 comparator, keyed class / target / anchor / source, multiplicity preserved) | **25** adds from the pre-session backlog (sources introduced at or before `32195e1`, the session's starting HEAD) + **8** from the four sequencing-guard commits `2d8258b` / `4fc46ea` / `b9ed835` / `376f5d6` (Director-directed via CCX-20260903-001 section 0) + **0** from `0243874` | **0** from the CHG execution commit; the 8 guard-commit adds are the filed documents' own forward references (CMP `M0`/`M1` anchors, CHG `section 0`), not defects introduced by execution |

Zero of the 33 adds reference `RHACO_Subagent_Pattern_Application_Guide` as target or source; the CHG section 5 Execution Record introduces no section-qualified citation. The 33-add table verbatim (grouped by key, multiplicity shown, source-introducing commit):

| x | class | target | anchor | source document | introduced by |
|---|---|---|---|---|---|
| 2 | anchor_not_found | RHACO-CHG-20260830-002_INC_20260830_001_Execution_Record_Merge_Hash.md | section 7 | RHACO-CCX-20260830-002_..._M1_Partial_Execution_Resume_Handoff | `32195e1` |
| 2 | anchor_not_found | RHACO-CHG-20260903-001_Subagent_Guide_v1_3_Model_Tier_Declaration.md | section 0 | RHACO-HND-20260903-001_Corpus_Explorer_Autonomous_Build_Dispatch | `b9ed835` (guard) |
| 1 | anchor_not_found | RHACO-CMP-20260808-001_Skill_Governance_Contract_And_Attestation.md | M1 | RHACO-CCX-20260830-001_M1_Contract_Preflight_Readout_Handoff | `f0341e5` |
| 5 | anchor_not_found | RHACO-CMP-20260808-001_..._Attestation.md | M1 | RHACO-CCX-20260830-002_..._M1_Partial_Execution_Resume_Handoff | `32195e1` |
| 1 | anchor_not_found | RHACO-CMP-20260808-001_..._Attestation.md | M1 | RHACO-CCX-20260903-001_Corpus_Explorer_Build_Dispatch_Handoff | `376f5d6` (guard) |
| 4 | anchor_not_found | RHACO-CMP-20260808-001_..._Attestation.md | M1 | RHACO-CHG-20260830-001_Skill_Contract_SSOT_Ho_Door_Check_Mirror_Stamp_v1_1 | `db4e8d6` |
| 2 | anchor_not_found | RHACO-CMP-20260808-001_..._Attestation.md | M1 | ..._v1_1_Amendment_A1_Milestone_Anchor_Correction_And_Stamp_Semantics_Fold | `db4e8d6` arc |
| 3 | anchor_not_found | RHACO-CMP-20260808-001_..._Attestation.md | M1 | ..._v1_1_Amendment_A2_Specimen_Carve_And_CS8_Recut | `db4e8d6` arc |
| 1 | anchor_not_found | RHACO-CMP-20260808-001_..._Attestation.md | M2 | ..._v1_1_Amendment_A2_Specimen_Carve_And_CS8_Recut | `db4e8d6` arc |
| 1 | anchor_not_found | RHACO-CMP-20260903-001_Corpus_Explorer_Autonomous_Build_Harness.md | M0 | RHACO-CCX-20260903-001_Corpus_Explorer_Build_Dispatch_Handoff | `376f5d6` (guard) |
| 2 | anchor_not_found | RHACO-CMP-20260903-001_..._Build_Harness.md | M0 | RHACO-CHG-20260903-001_Subagent_Guide_v1_3_Model_Tier_Declaration | `4fc46ea` (guard) |
| 1 | anchor_not_found | RHACO-CMP-20260903-001_..._Build_Harness.md | M0 | RHACO-HND-20260903-001_Corpus_Explorer_Autonomous_Build_Dispatch | `b9ed835` (guard) |
| 1 | anchor_not_found | RHACO-CMP-20260903-001_..._Build_Harness.md | M1 | RHACO-HND-20260903-001_Corpus_Explorer_Autonomous_Build_Dispatch | `b9ed835` (guard) |
| 1 | anchor_not_found | RHACO-HND-20260826-001_M2_Pilot_Scoring_Hand.md | section 0 | RHACO-CCX-20260829-001_..._WAN_Degradation_Resume_Conditions_Handoff | `77ea663` |
| 1 | anchor_not_found | RHACO-HND-20260826-001_M2_Pilot_Scoring_Hand.md | section 0 | RHACO-INC-20260829-001_Required_Claim_Floor_V7_..._Gate_Regression_Red | `66d0631` |
| 1 | anchor_not_found | RHACO-HND-20260826-001_M2_Pilot_Scoring_Hand.md | section 0.4 | RHACO-CCX-20260826-001_..._M2_Scoring_STOPs_Verdict_Freeze_Pilot_ANL_And_M2_Gate_Handoff | `2b6c0b6` |
| 1 | anchor_not_found | RHACO-INC-20260829-001_..._Gate_Regression_Red.md | section 0 | RHACO-CCX-20260829-001_..._WAN_Degradation_Resume_Conditions_Handoff | `77ea663` |
| 2 | anchor_not_found | RHACO-INC-20260830-001_Self_Edge_Baseline_Pin_Stale_Across_Five_Amendment_Hands_Four_Pushed_Red.md | section 7 | RHACO-CHG-20260830-002_INC_20260830_001_Execution_Record_Merge_Hash | `ed39c31` |
| 1 | unresolved_target | RHACO-HND-20260825-002_report | section 0 | RHACO-CCX-20260829-001_..._WAN_Degradation_Resume_Conditions_Handoff | `77ea663` |

Total 33 = 25 backlog + 8 guard + 0 execution. The auditor baseline re-seed (`--write-baseline`) and the regression-baseline disposition of the two filed incidents remain separate Director-ratified acts, not taken here.

## B. Section 0 status (consolidated, sessions 1-2)

| Item | Gate | Verdict | Session | Evidence (full detail in the findings file) |
|---|---|---|---|---|
| 1 | Charter pin / successor / card state | PASS | 1; re-pinned 2 and 3, unchanged | `c7104f1a37c53528922241472de7c08449259bf9d13d28ea6dd387e33404952b` 15,216 B; section 8 append only; card Active / OPEN |
| 2 | CHG-20260903-001 executed | PASS | 1 | body Status Baseline; commit `0243874` |
| 3 | Guide v1.3 present, D-13, section 6 lines, v1.2 Superseded | PASS | 1 | `fdd1c5f68ecfbd3b613370a3d9abfef1c2c2ab3277d2f48902d1ed5a7aac65a9` 19,842 B |
| 4 | Retrieval dispositions unchanged | PASS | 1 | ANL-20260712-001 = pin; CMP-20260628-001 CLOSED |
| 5 | Substrate pins | PASS | 1; re-pinned 2 (all five unchanged); re-pinned 3 (four unchanged; db pin moved and reconciled -- section J.2 / J.3) | module `7548e47e...` 118,123 B; librarian `22e1923a...` v1.19; db `b009d057...` 115,097,600 B @ 2026-09-04T00:04:23Z; gold v1.1 `f20bc6a2...` 40 rows; v1.2 `d456f981...` |
| 6 | Repo state | PASS (deviation D-0.1) | 1; re-pinned 2; re-pinned 3 (note D-0.3) | `0243874` = origin/main, descendant of `32195e1`; porcelain empty |
| 7 | Workspace repo | **PASS (note D-0.2) -- discharged** | 2; re-checked 3 | git repo at the workspace root; private `origin` `highsierralabs/RHACO_Corpus_Explorer` (created 2026-09-04T00:41:58Z, empty); untracked set = the two instruments; not nested under RHACO; `core.autocrlf=false` |
| 8 | Prompt / rationale pins of record | **PASS** | observed 1; verdict 2; re-pinned 3, unchanged | `PROMPT.md` `dd75dd2ebf6a641bd223acf47289e0f3dd5b5b70c2b506b70d0c986cded98d0d` 28,665 B; `RATIONALE.md` `3070c961b08aa90bdfc6da0d87b806f3e97b98f95fba032d3f6617506d372a6b` 26,151 B -- = chat-side provenance, LF, no BOM, line 1 / `Start now.` match |
| 9 identity | version / model / effort unchanged | PASS | 2; re-pinned 3 (settings.json changed: opus effort high -> medium; no version or model change) | 2.1.260; `claude-fable-5-1` (`claude-fable-5-1[1m]` in settings); effortLevel xhigh; R-A8 closed by the Director's fact of record (manual update, auto-updater disabled: `DISABLE_AUTOUPDATER` Process=1, User=1) |
| 9(a) | per-subagent model-pin surface | PASS | 2 | Agent tool `model` (enum sonnet / opus / haiku / fable); agent-definition frontmatter `model`; Workflow `agent()` `opts.model` + `opts.effort` -- verified from the live tool schema and exercised on 17 strands |
| 9(b) spawn depth | nested spawn refused | PASS | 2 | at depth 1 no `Agent` / `Task` / `Workflow` tool exists on either surface (strands C1, D5) |
| **9(b) concurrency** | **fourth concurrent queued** (parent text) / variables present + spawn-depth holds (A1.2) | **FAIL as parent text (2); PASS as amended by A1.2 (3)** | 2; 3 | `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=3` in the client process env; name present in the binary; a fourth concurrent strand was never queued: runs B (Agent tool), D (Workflow), E (model turns) |
| 9(c) | tier models resolve to client-reported identifiers | PASS | 2 | `claude-sonnet-5`; `claude-opus-5[1m]`; `claude-haiku-4-5-20251001` |
| 10 | Suppression not inherited | PASS | 2 | no `--disallowed-tools`; deny / allow lists empty; no managed or local settings; no hook removes a tool; Agent + Workflow present |
| 11 | WAN / provisioning | PASS | 2 | `.venv` on Anaconda 3.13.9; `pip install fastapi uvicorn jinja2 playwright pytest` exit 0; `playwright install chromium` exit 0 (Chrome for Testing 151.0.7922.34); + pyyaml 6.0.3, sqlite-vec 0.1.9 (module runtime deps, recorded) |
| 12 | RHACO import path | PASS | 2 | both imports from the venv via path entries; `DEFAULT_DB == C:\RHACO\index\corpus_index.db`; VERIFY marker discharged (db per call; body root by argument or in-process override) |
| 13 | Vector channel state | recorded **AVAILABLE**; W7 mechanism **PROVEN** | 2 | sqlite-vec loads, nomic set 1,756 rows, Ollama reachable (1.0 s); `RHACO_CORPUS_DISABLE_VEC=1` child -> typed `VecUnavailable`, `search_hybrid` degrades to FTS-only with no raise; db sha / mtime unchanged after |
| 14 | Device safety | PASS -- recorded before any strand | 2; re-recorded 3 | logger PID 19516; sidecars 19544 / 19552 / 19588 / 20156 / 19308; launcher 15268; `castle_event_log.csv` appending |

## C. Halt (session 2)

**Disposition (Director, 2026-09-03): amend, do not hold -- Amendment A1, filed and committed as `3b860bf`. The halt below is closed; item 9(b) passes as amended (section J.2). Retained as the record of the trigger.**

**Trigger.** Section 0.2 item 9(b), concurrency half -- "confirm ... a fourth concurrent is queued" -- is not met. `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=3` is present in the client process environment (set in the launching shell, PID 24716; Process scope only, as section 2 I directs) and the variable name occurs five times in the 2.1.260 binary, yet a fourth concurrently dispatched strand ran at once in every measured form: live strands in foreground idle waits (run B, Agent tool: four alive 01:19:31Z-01:19:36Z; run D, Workflow surface: four alive 01:26:01Z-01:26:40Z) and concurrent model-generation turns (run E: four generating 01:29:17Z-01:29:26Z). The spawn-depth half is satisfied in the strongest form (no spawn tool exists at depth 1 on either surface). Section 0 halts on the mismatch: CMP section 4 blocker; D-13 last clause (caps verified live against the current release, never assumed from documentation).

**Smallest discriminating next check** (section 7; no fix proposed). Relaunch the client with `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=1` in the launching shell and dispatch two `general-purpose` strands with the run-B prompt from the findings file (foreground 40 s idle wait bracketed by `Get-Date` timestamps). If the second strand's t_start precedes the first's t_end, the variable gates nothing at any value in this release; if the second waits, the cap is honored only at value 1 and the value-3 result needs a different explanation. Facts recorded without recommendation: the binary also carries `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` and `CLAUDE_CODE_MAX_TOOL_USE_CONCURRENCY`.

**Findings surfaced alongside the halt (observations, not verdicts).**
1. `subagent_type: Explore` with a `sonnet` pin refused a governance-framed probe outright as a suspected injection (run A2, 0 tool uses), while `general-purpose` strands complied under prompt-level read-only rules. Section 2 I types the inspection strands and the critic as Explore.
2. The client's own rail blocks `Start-Sleep` / `sleep` chains in both shell tools, and a strand not told "no run_in_background" explicitly falls back to background execution (run A1, A3: a D-3 breach induced by the rail). Every strand prompt of the build needs that rule stated.
3. The `opus` pin resolves to `claude-opus-5[1m]` (1M-context variant) in this session -- the CCX-20260825-002 exemplar reproduced.
4. Section 2 C (zero-hit grep for the five index-writing names) and section 2 F (fixture index built "through the module's own functions") are in tension: `reindex_full` is the module's only full-index path; the building blocks beneath it (`connect`, `_card_row`, `_card_id_aliases`, `iter_body_docs(root)`, `emit_edges`, `_set_meta`, `_rollup_sha`) are not among the five names. Flagged for the architecture step; not reached.
5. `general-purpose` strands retain `Edit` / `Write` (tool list captured by D5); the D-9 structural guarantee holds only for `Explore`.

## D. Resumption log (section 2 M)

| Session | Opened | Last completed | Next action | Open halts |
|---|---|---|---|---|
| 1 | 2026-09-04T00:20Z | section 0 items 1-6 (PASS); item 8 observed | Director: item 7 act. Then re-open section 0 at item 7; items 8-14 follow; re-run items 1, 5, 6 pins if a session boundary intervenes | item 7 (BLOCKING) -- cleared by the Director between sessions (repository and private origin created 2026-09-04T00:41:58Z) |
| 2 | 2026-09-04T00:57:46Z | section 0 items 1, 5, 6, 8, 9, 14 re-pinned; item 7 discharged; items 8, 10, 11, 12, 13, 14 PASS; item 9 evaluated live (identity, 9(a), 9(b) depth, 9(c) PASS; 9(b) concurrency FAIL) | Director disposition on item 9(b) concurrency (section C check). Then re-enter section 0 per section 2 M (re-run items 1, 5, 6, 7, 8, 9, 14); at item 7 expect untracked `PROMPT.md`, `RATIONALE.md`, `preflight\` (`.venv\` is self-ignored by the venv's own `.gitignore`), all products of this pass (section I); once 9(b) is dispositioned, re-confirm item 14 and open Part A (M0) at section 1 item 1 | item 9(b) concurrency (BLOCKING) |
| 3 | 2026-09-04T01:53:09Z | Director-directed Rule-2 commit + push of the A1 pair (`3b860bf`); section 0 re-run per section 2 M PASS with 9(b) as amended; Part A M0 COMPLETE (`e9c9539`, `8da0712`); Part B round 0 (four inspection strands; `5b1b5fa`, `7a8edb4`, `4987cca`, `4e6fdc4`, `bafaa53`) and round 1 / wave 1 (corpus_adapter, probe, diagnostics; merges `8fa5648`, `c479c6f`, `6f7f42c`; integration `6b74f0d`) complete; **boundary commit `62685da` pushed to `origin/main` 03:49:4xZ (first push; fast-forward, new branch)** | Session 4: re-enter per section 2 M with A1 (items 1, 5, 6, 7, 8, 9, 14; a version or model change is a halt); read `build_state.json` (`next_action`); dispatch **wave 2 = round 2** builders catalog, search, reader from `62685da` (worktrees `.worktrees/<module>` on `build/<module>`; every prompt begins with the section 2 L text as amended; gates ruff + pytest + L1 at the worktree root; cap 3); the probe builder implements W1-W10 / KG / KB1-KB5 in wave 2; then wave 3 (lineage, web); re-run the L4 oracle at closeout; push at each boundary (O-1) | none blocking. For the Director, not halts: (a) L4 SCOPE row -- adapter == module 40/40, Recall@10 0.675 vs 0.700, row 27 live-head drift (SCOPE.md); (b) documentation drift recorded as CONSTRAINTS O27 (Tool Reference binding line; Glossary walker-exempt list; GDE / BLD vocabulary); (c) builders B2 / B3 self-reported approximate opening timestamps |
| 4 | 2026-09-04T23:39:34Z | Director-directed Rule-2 commit + push of the CCX-20260904-001 pair (RHACO `4032742`, pushed red, attribution-closed: regression delta 0 / 0, auditor +2 = the CCX's own forward references); criterion-6 reconciled (04:04:23Z CCX pair write via the rotated `mcp_validate.log.2026-09-03`; post-gate open pin `61ea99c9...` 115,089,408 B 23:58:11Z); section 0 re-run per section 2 M PASS; Director ruling AC-3 recorded (K.4) and section P seeded; `build_state.json` bookkeeping committed (workspace `67b01d0`: session 3 closed_utc, exceedances reconciled, pushes seeded, AC-3 reclassification); correction round dispatched from `62685da` -- probe round 2 (observed-fault predicate + AC-3 vocabulary; `docs/rounds/R01C_probe.prompt.md`) and corpus_adapter round 2 (`connect()` isfile guard; `R01C_corpus_adapter.prompt.md`) -> merged, correction boundary `c96664dd`; **wave 2 = round 2** (catalog S4-B4, search S4-B5, reader S4-B6; merged `2698b66` / `8dc8810` / `32808db`; integration `2d83808`, A21, O29) and **probe round 3** (S4-B7: wave-2 runners; ledger KB1 / KB2 / KB4 / KB5; production presets PRODUCT_EVIDENCE_PASS; merged `3d81949`) complete; 181 tests; builder rounds 9 of 28; **round-2 boundary `6a83537` pushed** (K.7) | Session 5: re-enter per section 2 M with A1 (items 1, 5, 6, 7, 8, 9, 14; a version or model change is a halt); read `build_state.json` (`next_action`); dispatch **wave 3 = round 3** builders lineage and web from `6a83537` (worktrees `.worktrees/<module>` on `build/<module>`; L text first; dispatch form = L blocks + sha-pinned pointer; cap 3); then probe round 4 (Q3: KB3, W5, W6; FINAL suite) as a sequential strand; then the L4 oracle re-run, the acceptance set on the production path, and the critic (Opus 5, Explore, effort set explicitly per section 2 I); push at each boundary (O-1) | none blocking. For the Director, not halts: (a) S4-B7 launcher-rule deviation (scratch-launched uvicorn against the fixture; self-disclosed; K.7.2); (b) `availability.py` line 49 direct module `connect()` (same O10 class as the guard fix; K.6); (c) OS-temp writes by test tooling and scratch scripts vs HND section 5 wording (A2 candidate; K.7.2); (d) probe budget 3 of 4 consumed with Q3 / FINAL still ahead; (e) CONSTRAINTS O29 (`search_fts` bare-string filter, a module finding). Recorded, not halts (K.5): the launch note lists two chat-side log entries, three exist (22:55:00Z read-only census unlisted) and the pair-write entry had rotated into `mcp_validate.log.2026-09-03`; CCX section 0.2 "makes 1444" vs observed 1443; CCX section 0.4 "20 commits" vs 12 first-parent / 26 reachable; launch cwd = the workspace project root rather than `C:\highsierralabs\RHACO` (as in session 3) |
| 5 | 2026-09-05T02:21:15Z (re-entry per section 2 M with A1; SAME client process PID 9800 as session 4, continued after a context compaction) | Section 0 items 1, 5, 6, 7, 8, 9, 14 re-run: **PASS in full, no halt at re-entry** (charter `c7104f1a...`; four substrate pins unchanged; **db pin `61ea99c9...` 115,089,408 B 23:58:11.272Z UNCHANGED since the session-4 close pin** -- no librarian reindex and no MCP census entry after it, so criterion 6 needed no reconciliation; RHACO `4032742` clean; workspace `6a83537` = origin/main clean; PROMPT / RATIONALE pins unchanged; 2.1.260 / `claude-fable-5-1` / xhigh at 02:21:32Z; identical production PID set). Baseline gates on main: ruff clean, L1 6/6 PASS, pytest **181 passed**. **Director rulings of 2026-09-05 recorded** (five items: 1 S4-B7 launcher deviation recorded, NOT invalidating, no retroactive act; 2 `availability.py` direct `connect()` deferred to the next adapter round as a **hard floor before FINAL**, one round spent if none would otherwise arise; 3 OS-temp writes accepted as a section 5 application note, 'no write to any governed tree', A2 line at terminal; 4 probe budget **not waived** -- round 4 carries Q3 + FINAL, any further need is BOUNDED_FAIL, ledger P-8; 5 discrepancies recorded with no act, CCX not edited, **O29 is a RHACO module finding for a Director-side CHG, not a build item**). Wave 3 assembled from `6a83537` (three prompts archived; three worktrees created) but **NEVER DISPATCHED** | **HALTED by the Director 2026-09-05T02:37Z -- see the halt row below. On resumption: the Director supplies the model-swap boundary times and rules on the disposition of the acts tagged INDETERMINATE (`build_state.json` `orchestrator_acts_session_5`); then either re-issue or re-author wave 3 under the model of record, dispatch (lineage S5-B8, web S5-B9, corpus_adapter S5-C3), merge, then probe round 4 (Q3 + FINAL), the L4 re-run, the acceptance set, and the critic; push at the boundary per O-1** | **HALT (BLOCKING): mid-session client model swap.** `claude-fable-5-1` -> a safeguard-degraded Opus -> `claude-opus-5` (Director-set), reported by the Director at 02:37Z. HND section 2 M / CMP-20260903-001 section 6 REVIEW trigger. On-disk anchor: `~/.claude/settings.json` rewritten **2026-09-05T02:30:18.101Z**, sha `cea59185...` -> `26c5f7b0...`, 9,037 -> 9,002 B, the `model` key (`claude-fable-5-1[1m]`) now **absent**. The orchestrator's own system prompt still read `claude-fable-5-1` at 02:37Z, so the swap is **not corroborable from inside** -- ledger P-9. Nothing was dispatched or merged after the swap. **Director disposition 2026-09-05:** acts 8-12 DISCARDED (the three uncommitted `R03_*.prompt.md` prompts and their scratchpad bodies deleted; the lineage and web worktrees and branches removed; `build/corpus_adapter` restored to `202282f` from the reflog); acts 13-14 KEPT as records tagged authored under the degraded model (mechanical records, no judgment); the halt records committed alone and pushed per O-1. **Measured boundary correction** (transcript per-request model field, now the identity source of record): the safeguard switch to `claude-opus-4-8` took effect at **02:25:02.164Z**, 2 min 27 s before the notice record at 02:27:33.708Z, so orchestrator act 6 and the two progress messages of 02:27:23Z / 02:29:29Z were also on the degraded model; `claude-opus-5` from **02:30:48.002Z**. Section 0's evidence (acts 1-4, 02:21Z) stands under `claude-fable-5-1`; its PASS *summary to the Director* was written on the degraded model. Ledger P-9, P-10 |
| 6 | 2026-09-05T03:10:50Z (re-entry per section 2 M with A1; **new client process 26148**, started 03:09:23Z from launching shell 24716 -- the session 1-5 process 9800 is gone) | Section 0 items 1, 5, 6, 7, 8, 14 re-run and **PASS unchanged** (charter `c7104f1a...`; hand `ed01fa54...`; **A1 `167771c7...` 9,698 B**; four substrate pins unchanged; **db pin `61ea99c9...` 115,089,408 B 23:58:11.272Z UNCHANGED since the session-4 close** -- criterion 6 needed no reconciliation; RHACO `4032742` clean; workspace `f25ca27` = origin/main clean, worktrees only `probe` at `a0dacd7`, branch set back to the session-4 shape; `PROMPT.md` / `RATIONALE.md` / `DISPATCH_PARAMETERS.md` pins unchanged; identical production PID set, castle log appending). Baseline gates on main: ruff clean, L1 6/6 PASS, pytest **181 passed**. **Item 9 shows the model change `claude-fable-5-1` -> `claude-opus-5[1m]`, PRE-DISPOSITIONED by Director ruling MOD-1 and therefore NOT a halt.** System prompt now reads verbatim 'You are powered by the model named Opus 5 (1M context). The exact model ID is claude-opus-5[1m].' -- correct this time because the client restarted and regenerated it. Effort resolves to **xhigh** from every source (settings `effortLevel: xhigh` plus modelSettings for both `claude-opus-5` and `claude-opus-5[1m]` at xhigh; env `CLAUDE_EFFORT=xhigh`) -- they AGREE, so this session cannot discriminate which would win; the settings file is what changed to produce the agreement. **MOD-1 recorded**: identity pin amended to `claude-opus-5[1m]` / xhigh from session 6, cause = client safeguard degradation of Fable on the build's own tool output; A2 line at terminal; `docs/LAYERS.md` section 3 gains the shared-model-family caveat; every dispatch record from here carries `orchestrator_model`. **Wave 3 re-authored fresh and dispatched** from `6a83537` | Session 6 continues: merge wave 3, then probe round 4 (Q3 + FINAL), the L4 oracle re-run, the acceptance set, and the critic; seal release at the first `docs/critique/` report; push at the boundary per O-1 | none. MOD-1 pre-disposes the item-9 change; the shared-model-family caveat is recorded in LAYERS section 3, not treated as a defect |
| 6 (cont.) | -- | **Round 4 (probe, module round 4 of 4) merged `e36d9d6`.** The qualification ledger now holds **KB1, KB2, KB3, KB4 and KB5**; no preset remains `not_implemented_yet`; the strand reports no fifth round is needed. **CMP-20260903-001 criterion 3 (and HND section 8 item 4) is therefore SATISFIABLE FOR THE FIRST TIME**: the probe is qualified for all five known-bad classes plus the known-good set, so an L2 observation can now serve as acceptance evidence for the capabilities it covers, under the AC-3 progressive reading. The L4 gold oracle re-run reproduces round 1 exactly (40/40 list equality, 0.675 vs the 0.700 threshold, verdict SCOPE), re-verifying the standing SCOPE row rather than changing it. **W6 FAILED on the production path** and that failure is the harness working: it exposed a reader defect that 500s on 60.0% of the corpus (ledger P-14), repaired in reader round 2 | Reader round 2 dispatched; then the new L4 card-agreement oracle, the W-preset re-runs, the acceptance set, critic round 1, seal release, and only then the single batched adapter round 4 | none |
| 7 | 2026-09-05T13:51:18Z (re-entry per section 2 M with A1; new client process, launch per CCX-20260904-001 section 4.1 steps 1-5: fresh shell, caps SPAWN_DEPTH=1 / MAX_CONCURRENT_SUBAGENTS=3 present, `claude --version` 2.1.260, `--dangerously-skip-permissions`; a prior session-7 attempt 13:25:31Z-13:49:01Z, also under `claude-fable-5-1`, crashed after the Rule-2 commit `fb81f3f` and before its push) | Director-directed Rule-2 push of the CCX-20260905-001 pair (`fb81f3f`, pushed 14:14:32Z red, attribution-closed: regression delta 0 = the INC-20260829-001 / INC-20260830-001 pair, auditor +1 = the CCX's own `CMP-20260815-002 M3` forward reference; L.1); criterion 6 reconciled -- open db pin `ea8f7df2...` 115,081,216 B 2026-09-05T14:05:55Z after the gate librarian member's full reindex (L.2); 1449-vs-1443 reconciled (6 cards in `docs\docs_archive\`, a librarian DENY_DIR; L.2); section 0 items 1, 5, 6, 7, 8, 14 PASS unchanged (hand `ed01fa54...` 23,306 B; A1 `167771c7...` 9,698 B; charter `c7104f1a...` 15,216 B; Guide v1.3 `fdd1c5f6...` 19,842 B; CHG-20260903-001 card Baseline; `RHACO_corpus_index.py` `7548e47e...` 118,123 B; librarian `22e1923a...` 131,038 B v1.19; RHACO `fb81f3f` = origin/main clean; workspace `c861680` clean, 38 ahead of origin/main `f25ca27`). **Item 9 FAILS: model `claude-fable-5-1` (system prompt 'You are powered by the model named Fable 5.1. The exact model ID is claude-fable-5-1.' and 48/48 per-request `message.model` fields) vs the MOD-1 pin `claude-opus-5[1m]`; `~/.claude/settings.json` `model` = `claude-fable-5-1[1m]`, sha256 `70c8f58a5918ea4134df6af88ab9fcd923320e379bfcf718be5a795ead10f542`, 9,100 B, rewritten 2026-09-05T06:02Z -- a DIRECTOR ACT (Director disposition 2026-09-05 item 2; recorded here, no ledger row) -- vs the pinned `831dbfb9...` 9,097 B.** **Director act, this session (2026-09-05T14:36:20Z, after the halt report): `~/.claude/settings.json` `model` restored to `claude-opus-5[1m]` and saved -- re-hashed 14:49:39Z: sha256 `06f37244dda720a925b1a237d04a30a172b11444713a792de6d25d4e153cd279`, 9,097 B; `effortLevel: xhigh`; `modelSettings` `claude-opus-5` and `claude-opus-5[1m]` both xhigh; `switchModelsOnFlag: false` -- the MOD-1 SETTINGS PIN OF RECORD from session 8, superseding `831dbfb9...`. Both rewrites (06:02Z to Fable, 14:36Z back to Opus) were Director acts; no ledger row. Effective at the client restart; the running session-7 process stays `claude-fable-5-1`.** Effort xhigh from every source (env `CLAUDE_EFFORT=xhigh`; settings `effortLevel: xhigh`; no `modelSettings` entry for the Fable id, so the global governs); `/effort` set to ultracode (xhigh + workflow orchestration) this session, not exercised -- no strand dispatched, no workflow run. Not pre-dispositioned: the launch instruction named MOD-1 as the identity of record -- **HALT at re-entry**; CMP-20260903-001 section 6 REVIEW trigger fires for the second time. **Director dispositions 2026-09-05: (1) Option A ratified -- MOD-1 (`claude-opus-5[1m]` / xhigh) REMAINS the identity of record; (2) the 06:02Z settings.json rewrite recorded as a Director act, no ledger row; (3) the drafted records (`working\S7_halt_records_DRAFT_HND-20260903-001.md`) written as-is under the halted identity -- this row, L.1-L.4, P-16, P-17, P-18, `build_state.json` (`sessions` 7, `session_model_history` seq 5, `substrate_pins_open.corpus_index.db`, `director_rulings.S7_dispositions_20260905`, `next_action`, `rhaco_side_findings` RS-3) -- each tagged; workspace records committed (`build_state.json` only), NOT pushed.** P-16 / P-17 were absent from every on-disk build record (present only in the session-6 transcript: Director ruling 05:36:52Z, orchestrator numbering 05:41:54Z; the session compacted at 05:58Z and ended 06:01:12Z without writing them) and are now written; P-18 records the CCX-20260905-001 'dispatched-or-about-to-be' error (P-6 class). Lineage round 2 and adapter round 4 were NOT dispatched (no R07/R08 prompt; `build/lineage` `386afba`, `build/corpus_adapter` `7c1e9f4` 0 ahead of `c861680`); `build_state.json` `next_action` was stale (named critic round 1) and is re-pointed to the session-8 re-entry. Nothing else executed: no SA-3 edit, no dispatch, no convergence step [authored session 7 under `claude-fable-5-1`, identity halt open -- session-5 precedent: mechanical records, no judgment] | Session 8: settings restored and re-pinned this session (`06f37244...` 9,097 B, 14:36:20Z); the Director restarts the client after the close report; re-enter per section 2 M with A1 (items 1, 5, 6, 7, 8, 9 -- expect `claude-opus-5[1m]` / xhigh and settings sha `06f37244...` -- and 14; db pin `ea8f7df2...`); then CCX-20260905-001 section 4 step (1) -- lineage round 2 + adapter round 4 (SA-1 -> SA-2 -> amends click-through) and the SA-3 = (b) integrator text edit (L.3 census: SA-4 needs a scope clause) -- through step (9); O-1 push of the workspace at the first boundary under the pinned identity (39 ahead after the records commit) | **item 9 identity -- HALT OPEN until the session-8 re-entry under MOD-1**; no other halt |
| 8 | 2026-09-05T14:57:54Z | Section 0 items 1, 5, 6, 7, 8, 9, 14 PASS with no halt (item 9 resolved by P-19 rather than tripped); `build_state.json` reconciled (`4e56ca3`); **O-1 push discharged -- `f25ca27..4e56ca3`, 40 commits, fast-forward, the late session-6 boundary push**; two stale worktrees removed; SA-3 remedy (b) landed (`f29bcc9`); three R07 prompts archived (`f8c3287`, corrected `3e7521b`) with the P-11 byte-diff control PASS; three worktrees recreated from `main`; **round 6 dispatched -- S8-B12 lineage r2, S8-C4 corpus_adapter r4 (batched, last), S8-B13 diagnostics r2, concurrent, `wf_b0d40995-3c0`** | Await the three strands, then integrate, then continue the CCX-20260905-001 section 4 sequence from step (2) | none open |

## E. Dispatch record (Guide v1.3 section 6 / HND section 2 I)

Eighteen read-only probe strands, all dispatched after the item-14 record was on disk (01:12:31Z), all carrying the section 2 L text verbatim at the top of the prompt, all pattern #1 (pre-flight verification), `tier_escalation: none`. `model` is the string each strand copied from its own system prompt at spawn (client-reported), never the dispatch intent; A2 reported none. `effort` is the client setting in force at spawn (the Agent tool has no per-call effort parameter; the Workflow surface has `opts.effort`). `build_state.json` does not yet exist (a Part B artifact, prompt section 10); these rows are to be seeded into it when Part B opens.

| strand_id | role | pattern_primary | agent_type (surface / type) | model (client-reported) | effort | execution_mode | tier_escalation | acceptance |
|---|---|---|---|---|---|---|---|---|
| A1 | s0 item 9(b)/(c) identity + timing, run 1 | #1 | Agent tool / `Explore` (read-only) | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | identity PASS; timing invalid (sleep rail -> background re-run, D-3 breach) |
| A2 | same | #1 | Agent tool / `Explore` (read-only) | requested `sonnet`; none reported | xhigh (session, inherited) | concurrent | none | REFUSED by the strand (0 tool uses) |
| A3 | same | #1 | Agent tool / `Explore` (read-only) | `claude-opus-5[1m]` | high (`modelSettings.claude-opus-5`) under session xhigh | concurrent | none | identity PASS; timing invalid (background re-run) |
| A4 | same | #1 | Agent tool / `Explore` (read-only) | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | identity PASS; t_end blocked |
| B1 | s0 item 9(b)/(c) identity + timing, run 2 | #1 | Agent tool / `general-purpose` (prompt-level read-only) | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | PASS |
| B2 | same | #1 | Agent tool / `general-purpose` | `claude-sonnet-5` | xhigh (session, inherited) | concurrent | none | PASS |
| B3 | same | #1 | Agent tool / `general-purpose` | `claude-opus-5[1m]` | high (`modelSettings.claude-opus-5`) under session xhigh | concurrent | none | PASS |
| B4 | same -- fourth concurrent | #1 | Agent tool / `general-purpose` | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | PASS -- **not queued** |
| C1 | s0 item 9(b) spawn depth | #1 | Agent tool / `general-purpose` | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | single-pass | none | PASS -- no spawn tool at depth 1 |
| D1 | s0 item 9(b)/(c) identity + timing, Workflow surface | #1 | Workflow `agent()` / default workflow subagent | `claude-haiku-4-5-20251001` | low (`opts.effort`) | concurrent | none | PASS |
| D2 | same | #1 | Workflow `agent()` / default workflow subagent | `claude-sonnet-5` | low (`opts.effort`) | concurrent | none | PASS |
| D3 | same | #1 | Workflow `agent()` / default workflow subagent | `claude-opus-5[1m]` | low (`opts.effort`) | concurrent | none | PASS |
| D4 | same -- fourth concurrent | #1 | Workflow `agent()` / default workflow subagent | `claude-haiku-4-5-20251001` | low (`opts.effort`) | concurrent | none | PASS -- **not queued** |
| D5 | s0 item 9(b) spawn depth, Workflow surface | #1 | Workflow `agent()` / `general-purpose` | `claude-haiku-4-5-20251001` | low (`opts.effort`) | single-pass | none | PASS -- no spawn tool; tool list captured |
| E1 | s0 item 9(b) turn-cap semantics | #1 | Agent tool / `general-purpose` | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | PASS (1500 literal lines; 31.8 s turn) |
| E2 | same | #1 | Agent tool / `general-purpose` | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | PASS (30.3 s turn) |
| E3 | same | #1 | Agent tool / `general-purpose` | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | PASS (31.3 s turn) |
| E4 | same -- fourth concurrent | #1 | Agent tool / `general-purpose` | `claude-haiku-4-5-20251001` | xhigh (session, inherited) | concurrent | none | PASS -- **not queued** (30.4 s turn overlapping all three) |

Governance check on the record: 18 of 18 dispatches carry all seven fields; zero spawns outside the tier map (haiku = small, sonnet = mid, opus = judgment; no `fable` strand); zero nested spawns (structurally impossible at depth 1); zero escalations.

## F. D-12 application note

D-12 is applied in the narrowed section 2 L form (workspace interpreter only; not "no process"). Each of the 18 strands carried the section 2 L blockquote verbatim as the first twelve lines of its prompt. Strand activity was confined to `Get-Date`, one foreground idle wait or one literal-argument command, and self-inspection of the tool list: no interpreter launched, no RHACO module imported, no device transport, no `C:\RHACO\data\` access, no file written; two recorded deviations (A3 read its own task-output file for a timestamp; A4 used Bash for a blocked sleep). Main-thread activity beyond section 0 reads: the venv creation via the Anaconda interpreter exactly as item 11 directs, the pip and Playwright installs, and the two preflight scripts run under the workspace venv interpreter (the sanctioned form). Production PIDs (item 14) were untouched before and after; `castle_event_log.csv` kept appending through the session (mtime 01:13Z at the last read).

## G. Per-part status

| Part | Status |
|---|---|
| Section 0 pre-flight | PASS in full (session 3; item 9(b) as amended by A1.2) |
| Part A -- M0 completion | **COMPLETE** (session 3, 02:24:07Z): `e9c9539` (`PROMPT.md`, `RATIONALE.md` unchanged + `preflight\`), `8da0712` (`DISPATCH_PARAMETERS.md` sha256 `f6e9cd3e...`) -- section J.4 |
| Part B -- harness execution | rounds 0 and 1 complete (session 3): CONSTRAINTS / ARCHITECTURE / SCOPE / REFERENCE / LAYERS written; wave 1 modules corpus_adapter, probe, diagnostics merged and integrated; builder rounds consumed 3 of 28; next = wave 2 (catalog, search, reader) -- section J.7 |
| Part C -- terminal state | not reached; no terminal state declared |

## H. Criterion-6 interim pins (session 2 close, 2026-09-04T01:31Z)

RHACO repo: `HEAD` = `origin/main` = `0243874489268d7a78a2e2e6d8d39eeb144ef522` -- no build commits since section 0.6; `git status --porcelain` empty (no change under `docs\`); `rhaco\RHACO_corpus_index.py` sha256 `7548e47ebdcc24cdc0621208d06673542889c75b000780c650628b0678a5f098` unchanged; `C:\RHACO\index\corpus_index.db` sha256 `b009d057ad1163e6aa55d0d86cfb0aebab74899459480acb2808c3f0d11434f7`, mtime `2026-09-04T00:04:23.6648Z` unchanged and explained by the librarian reindex logged at 2026-09-03 17:04:23 local; no `-journal` / `-wal` sidecar. The `connect()` read paths exercised at item 13 were byte-neutral on the live index.

## I. Workspace state at close

`C:\highsierralabs\RHACO_Corpus_Explorer\`: git repository, branch `main`, no commits, `origin` private and empty. Untracked: `PROMPT.md` (`dd75dd2ebf6a641bd223acf47289e0f3dd5b5b70c2b506b70d0c986cded98d0d`, 28,665 B) and `RATIONALE.md` (`3070c961b08aa90bdfc6da0d87b806f3e97b98f95fba032d3f6617506d372a6b`, 26,151 B) -- the instruments, unchanged; `preflight\item12_import.py` (`d9fd2893a9294714b41b10d32944d8803e30eb7aab3847791f8baaf9aa187de6`, 1,845 B) and `preflight\item13_vec.py` (`e36baf21ae738f8d51e59c6d327329d678d395659aa24cdf37dac7e44546778b`, 2,759 B) -- the item 12 / 13 evidence scripts (read-only against the live index through the module's own `connect()`; never call the five index-writing names). `.venv\` (155 MB; Python 3.13.9; the item-11 set plus pyyaml 6.0.3 and sqlite-vec 0.1.9; self-ignored by the venv's own `.gitignore`, so absent from `git status`; never to be committed). Not yet present: `DISPATCH_PARAMETERS.md` (Part A item 3), `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json` (Part B).

## J. Session 3 -- re-entry with Amendment A1 applied; Part A (M0) opened

Opened 2026-09-04T01:53:09Z (client PID 10408, same launching shell PID 24716 as sessions 1-2). Executor unchanged: Claude Code 2.1.260, `claude-fable-5-1`, effort xhigh (`/effort` ultracode) -- no section 2 M halt. Amendment A1 read in full (sha256 `167771c75575ceef2d07e21a9fbfb6f00a24776ca619514cc9b9a216414be99a`, 9,698 B) and applied: A1.2 (item 9(b)), A1.3 (section 2 I caps), A1.4 (section 2 L prefix), A1.5 (section 2 C / F), A1.6 (refusal rule, additive under section 3), A1.7 (acceptance 2 and 6), A1.8 (facts of record). Full evidence: findings file, "Session 3 re-entry" record.

### J.1 Pre-findings note -- Director-directed Rule-2 commit and push of the A1 pair (2026-09-04)

| Step | Result |
|---|---|
| P1 validate (02:05:27Z) | `--validate-only --baseline --no-reindex`: 1442 cards, missing=0, orphans=3, carded-with-parent=128, carded-with-code=0, broken=0, errors=0, baseline=MATCH; live db untouched |
| Commit | `3b860bfe4167345a89258ce13c4ff946faeb5cb7` @ 02:08:32Z -- `RHACO-HND-20260903-001: file Amendment A1 cap re-home and findings` (66 chars, ASCII, one GOV-ID; body carries both shas and the census). Both blobs byte-identical to disk; commit-msg and structure-guard hooks passed |
| Closeout gate (02:09:55Z-02:10:59Z) | librarian PASS (13.4s; logged full reindex, baseline MATCH) / **regression FAIL** (31.8s) / **auditor FAIL** (19.1s, MISMATCH +35/-0, unwaived_gating 344 -> 346) / structure-guard PASS (13/13) / git-status-clean PASS / **overall FAIL exit 1** -- the same two members red as at the session-2 push (section A) |
| Attribution of the red to `3b860bf` | regression: `test_required_claim_floor_v9` (INC-20260829-001) delta 0; `test_corpus_index_self_edges` (INC-20260830-001, stale `SELF_EDGE_BASELINE` pin across amendment children) -- A1 is one more child sharing its parent's id, so the pinned membership moved by construction inside an already-red, incident-covered test. auditor: 33 adds unchanged from section A + **2 new**, both `unresolved_target RHACO-HND-20260903-001_report section 0` from A1.1's citation of `working\HND-20260903-001_report.md` (handbacks live outside the census walk; same shape as the `HND-20260825-002_report` row already in the 33). Zero adds name the Subagent Guide. No baseline write, no waiver edit, no `tests\` edit |
| Push (02:11:42Z) | `0243874..3b860bf  main -> main`; `origin/main` = `HEAD` = `3b860bf...`; `ls-remote` agrees; 0/0; tree clean. Pushed red under the Director's instruction of record, on the section A precedent |

This is a filing act directed by the Director, not a build commit: the criterion-6 close check ("RHACO `git log` since section 0.6 shows no build commits") must read `3b860bf` as the A1 filing.

### J.2 Section 0 re-entry summary (section 2 M set; A1.9)

| Item | Verdict (S3) | Evidence (detail in the findings file) |
|---|---|---|
| 1 | PASS, unchanged | `c7104f1a...` 15,216 B; section 8 append-only vs `2d8258b`; card Active / OPEN |
| 5 | PASS -- four pins unchanged; **db pin moved, reconciled** | module `7548e47e...` 118,123 B; librarian `22e1923a...` v1.19; gold v1.1 `f20bc6a2...`; v1.2 `d456f981...`. db at 01:58Z: `87134d0b...`, 115,097,600 B, mtime 01:51:01.945Z -- **no librarian log entry**; `index_meta` (read via the module's `connect()`, byte-neutral): `corpus_card_count` 1441 -> 1442, `corpus_cards_sha` changed, `last_full_reindex_utc` unchanged; a `cards` row for the A1 card with `indexed_at_utc 01:51:01.94Z`, spelled at the physical path `C:\highsierralabs\RHACO\...`; no `fts_docs` row and no edges for A1. Reading: a pair-level card reindex of the freshly written A1 card by the chat-side MCP observatory server filing the pair through the same module; not an act of this hand. **Reconciled (Director ruling, J.5): `docs\logs\mcp_validate.log` line 7: `2026-09-03 18:51:15,410 INFO MCP census summary: cards=1442 missing=0 orphans=3 carded_with_parent=128 broken=0 errors=0 warnings_total=5203 ...` (= 2026-09-04T01:51:15Z; the A1 card row's `indexed_at_utc` is 01:51:01.94Z, the census summary follows the pair write by 13 s; the preceding entry, 17:08:41 local, reports cards=1441). The 02:10:08Z touch: `docs\logs\catalog_librarian_validation.log` `2026-09-03 19:10:08,428 INFO corpus index reindexed: 1442 cards ... corpus_cards_sha=ed86c3cdc60b` -- the closeout gate's librarian member (J.1 step 3).** |
| 6 | PASS (note D-0.3), then clean | pre-commit: `0243874` = origin/main, untracked = exactly the A1 pair (this hand's own amendment pair, the object of the directed commit); post-push: `3b860bf` = origin/main, porcelain empty |
| 7 | PASS (D-0.2 extended) | git repo, no commits, private empty origin (gh: PRIVATE, created 00:41:58Z), not nested, `core.autocrlf=false`; untracked = `PROMPT.md`, `RATIONALE.md`, `preflight\item12_import.py`, `preflight\item13_vec.py` (the session-2 set). Config act: repo-scope `user.name` / `user.email` set to the RHACO repo's identity (no email existed at any scope; a commit would have failed) |
| 8 | PASS, pins of record hold | `PROMPT.md` `dd75dd2e...` 28,665 B; `RATIONALE.md` `3070c961...` 26,151 B; LF, no BOM; line 1 / `Start now.` MATCH |
| 9 identity | PASS -- no halt | 2.1.260; `claude-fable-5-1`; xhigh. `settings.json` changed between sessions (`cea59185...`, 9,037 B, 01:53:20Z): `modelSettings.claude-opus-5.effortLevel` **high -> medium** -- Agent-tool Opus strands now inherit medium; Workflow `opts.effort` is the per-strand control of record for the critic |
| 9(a) | PASS | same release; live schema re-shows Agent `model` enum and Workflow `opts.model` / `opts.effort` |
| **9(b) as amended (A1.2)** | **PASS** | `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` and `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=3` present in the client process environment (inherited by the tool shell; same launching shell); spawn-depth live result of session 2 stands (no client-version change); concurrency non-enforcement in 2.1.260 recorded, wave cap orchestrator-enforced per A1.3. **Session-2 halt closed** |
| 9(c) | PASS (carried, A1.8) | `claude-sonnet-5`; `claude-opus-5[1m]`; `claude-haiku-4-5-20251001` |
| 14 | PASS -- recorded before any strand | identical PID set to session 2 (launcher 15268; logger 19516; sidecars 19544 / 19552 / 19588 / 20156 / 19308); `castle_event_log.csv` appending (mtime 01:53:51Z at the 01:57:42Z census) |

**Item 12 VERIFY answer (explicit; selects the A1.5 branch).** `connect()` and every public entry point take `db_path` -- the fixture **db is selected per call by argument** (no edit, no monkeypatch). `BODY_SCAN_ROOT` is a module constant read at call time and `reindex_full` calls `iter_body_docs()` with no root, so a fixture body scan through `reindex_full` **requires the in-process override `RHACO_corpus_index.BODY_SCAN_ROOT = <fixture root>`** (never on disk). Therefore `fixtures/build_fixture_index.py` takes A1.5 (i) in both halves -- db path by argument, docs root by in-process monkeypatch -- with the (ii) path assertion before any write and the (iii) `CONSTRAINTS.md` record of the monkeypatch as a user-invisible constraint. Editing the module stays `BLOCKED`.

### J.3 Criterion-6 open pins for the build (session 3, post-gate)

RHACO `HEAD` = `origin/main` = `3b860bfe4167345a89258ce13c4ff946faeb5cb7` (one commit since section 0.6: the directed A1 filing, J.1); porcelain empty. `rhaco\RHACO_corpus_index.py` `7548e47ebdcc24cdc0621208d06673542889c75b000780c650628b0678a5f098` unchanged. `C:\RHACO\index\corpus_index.db` sha256 `274c1e96b67b560073be9683ff0788ca86192a5ca40ae59ca821fe83008e28f1`, **115,093,504 B**, mtime `2026-09-04T02:10:08.415Z` -- explained by the gate's librarian reindex logged 19:10:08 local (`1442 cards ... corpus_cards_sha=ed86c3cdc60b`; `index_meta.last_full_reindex_utc 2026-09-04T02:10:04.272949Z`; fts_docs 1607, edges 10507, id_aliases 2653); unchanged through the direct regression run and the read-only `connect()` probes that followed. No `-journal` / `-wal` sidecar. **Close pins are compared against these values.** Standing criterion-6 reconciliation sources (Director ruling, J.5): `docs\logs\catalog_librarian_validation.log` reindex entries (librarian runs, including the gate's librarian member) and `docs\logs\mcp_validate.log` census entries (chat-side MCP pair writes).

### J.4 Part A -- M0 completion (section 1, as amended by A1.7 item 2 and A1.8)

| Item | Record |
|---|---|
| 1 -- handback | This file: sections J.1-J.3 (section 0 findings summary, pins of record, criterion-6 open pins) written 02:20Z, before the commits below |
| 2 -- first build commit | `e9c9539f59cac1fb6346561071dbd17274d64c51` @ 2026-09-04T02:24:06Z, subject `Land frozen build prompt and rationale` (as section 1 item 2 dictates), author = the repo-scope identity. Files: `PROMPT.md` (blob sha256 `dd75dd2ebf6a641bd223acf47289e0f3dd5b5b70c2b506b70d0c986cded98d0d`, 28,665 B), `RATIONALE.md` (`3070c961b08aa90bdfc6da0d87b806f3e97b98f95fba032d3f6617506d372a6b`, 26,151 B) -- both **byte-identical to the pins of record**, so committed unchanged; plus `preflight\item12_import.py` (`d9fd2893...`, 1,845 B) and `preflight\item13_vec.py` (`e36baf21...`, 2,759 B) per A1.8 (evidence, not product). The body carries all four shas. **Any later edit to `PROMPT.md` is a halt.** |
| 3 -- `DISPATCH_PARAMETERS.md` | sha256 **`f6e9cd3ee7e7b23bcc9057f402ed33210f738d677a9b3e26cf752ec713edad51`**, 9,730 B, 141 lines, LF, UTF-8, no BOM; commit `8da0712b9eeee43056fe98f38792caa2fd6c9179` @ 02:24:07Z, subject `Add DISPATCH_PARAMETERS.md (hand section 2 as amended by A1)`; blob sha = file sha. **Assembly of record (A1.7 item 2 -- "parent section 2 as amended"):** source = the parent hand's section 2 block, hand lines 140-266 (block sha256 `d56395a14c7d30b38a05a506be24926668af5944375f6143aaae7c8ce03297aa`, 8,585 B, from parent sha `ed01fa54...`); item C body <- A1.5 lines 98-103; item F body <- A1.5 lines 105-115; item I from `Caps:` to the item's end <- A1.3 lines 64-77; item L: the A1.4 D-3 block (amendment lines 85-88) inserted immediately after the D-12 blockquote, before the "This narrows D-12(a)" paragraph. Parent bytes verbatim outside those four hunks. Amendment text verbatim except: the lead-ins `Replacement: ` / `section 2 C, replacement: ` / `section 2 F, replacement: ` dropped; two-space continuation indent (the parent's list convention); `  > ` indent on the D-3 lines. The parent's bold item labels for C and F retained; A1.4's "Reason of record" paragraph not carried (amendment rationale, not prefix text). Verified by unified diff parent-block -> assembled = exactly the four intended hunks (assembler `assemble_dispatch_parameters.py` and its diff in the session scratchpad / transcript). Cosmetic artifact kept rather than edited: the C and F bodies open lowercase ("the L1 static check ...", "fixture corpus ...") because the amendment's sentences continued their lead-ins. |
| Workspace after M0 | branch `main`, 2 commits, 5 tracked files (`DISPATCH_PARAMETERS.md`, `PROMPT.md`, `RATIONALE.md`, `preflight\item12_import.py`, `preflight\item13_vec.py`), porcelain empty, `.venv\` self-ignored. `origin` untouched (empty) at M0; O-1 ruled in J.5 -- the first workspace push lands at this session's boundary with the `build_state.json` commit |

**M0 COMPLETE at 2026-09-04T02:24:07Z** (section 1: items 1-3 done and recorded here). The CMP section 8 M0-closure entry is queued to the next docs hand (section 6).

### J.5 Director rulings received in session 3 (2026-09-04)

1. **O-1 -- workspace push cadence.** Director ruling O-1 (2026-09-04): push `main` of the workspace to `origin` at each session boundary (with the `build_state.json` commit) and at terminal state; fast-forward only; each push recorded in the resumption entry. Applied from this session's boundary onward; the resumption entry records each push (commit range, `origin/main` after, fast-forward confirmed).
2. **Criterion-6 reconciliation sources.** Director ruling (2026-09-04): reconcile the 01:51Z index change against `docs\logs\mcp_validate.log` (chat-side MCP pair write) and the 02:10:08Z touch against the gate's librarian member; carry both as standing criterion-6 reconciliation sources. Evidence of record: `docs\logs\mcp_validate.log` line 7: `2026-09-03 18:51:15,410 INFO MCP census summary: cards=1442 missing=0 orphans=3 carded_with_parent=128 broken=0 errors=0 warnings_total=5203 ...` (= 2026-09-04T01:51:15Z; the A1 card row's `indexed_at_utc` is 01:51:01.94Z, the census summary follows the pair write by 13 s; the preceding entry, 17:08:41 local, reports cards=1441). The 02:10:08Z touch: `docs\logs\catalog_librarian_validation.log` `2026-09-03 19:10:08,428 INFO corpus index reindexed: 1442 cards ... corpus_cards_sha=ed86c3cdc60b` -- the closeout gate's librarian member (J.1 step 3). The 01:51Z change is thereby explained (MCP pair write) and the 02:10:08Z change is explained (gate librarian member); the section 2 N closeout reconciliation reads both logs.

### J.6 Part B progress (interim, written during the session; final state at J.7 / section D at the boundary)

| Step (PROMPT.md section 14) | State | Evidence |
|---|---|---|
| 1 inspect standing contracts | DONE 02:32Z-02:49Z | four read-only Sonnet 5 Explore strands (S3-I1..I4), wave of 3 then 1; prompts + reports archived `docs/rounds/R00_*`; workflow wf_c2507f3a-9da, 547,372 subagent tokens, 0 errors |
| 2 `CONSTRAINTS.md` | DONE | 8 standing + 28 observed constraints (A/B/C sections), each with basis, consequence, user-visible flag; commit `5b1b5fa` |
| 3 `ARCHITECTURE.md` + ownership | DONE | ownership table, module contracts, documented read-only SQL D-Q1..D-Q11, decision records A1-A18, waves; commits `5b1b5fa`, `4987cca` |
| `SCOPE.md`, `docs/REFERENCE.md` | DONE | scope register from the first build commit after CONSTRAINTS / ARCHITECTURE (same commit); standing retrieval dispositions of record, no superseding evidence found |
| integrator scaffold | DONE | `explorer/app.py`, `config.py`, `models.py`, `faults.py`, web seeds, `requirements.txt` (adds markdown-it-py, mdit-py-plugins, ruff, httpx), `ruff.toml`, `tools/l1_index_write_check.py`; commits `7a8edb4`, `4987cca`, `4e6fdc4` + the L1 exclusion fix |
| 6 `docs/LAYERS.md` | DONE (written at dispatch of the adapter and probe) | shared-dependency register; no independence claimed among L2-L4; commit `4e6fdc4` |
| L4 oracle | WRITTEN, not yet run (needs the adapter) | `tools/l4_gold_oracle.py` -- adapter ids vs module-direct ids, Recall@10, `resolve: supersession-head` honored |
| 4-5, 8 (wave 1) | **IN PROGRESS** | round 1 / wave 1 dispatched 2026-09-04T03:08:28Z: B1 corpus_adapter, B2 probe, B3 diagnostics -- three Sonnet 5 general-purpose builders, `opts.effort` xhigh, each in `.worktrees/<module>` on `build/<module>` from `4987cca`; prompts archived `docs/rounds/R01_*.prompt.md` (section 2 L prefix as amended first); workflow wf_e159befc-5e3; builder rounds consumed 3 / 28 |

Interpretations recorded (for the closeout): (a) the L1 index-write grep excludes the three pinned instruments (`PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`) alongside `CONSTRAINTS.md` / `docs/` -- hand text, not build code -- and the gitignored `.worktrees/` (checked at each worktree's own root); (b) `ARCHITECTURE.md` and `SCOPE.md` at the root deliberately do not spell the five names; (c) builder worktrees live inside the workspace root because item A confines every build mutation there (ARCHITECTURE A15).

### J.7 Part B -- round 0 and round 1 / wave 1 closed; session boundary (2026-09-04T03:50Z)

**Round 0 (prompt section 14 steps 1-3, 6).** Four read-only inspection strands (Sonnet 5, Explore, Workflow `agent()`, `opts.effort` xhigh; wave of 3 then 1; no A1.6 refusal occurred with the section 2 L prefix first) -> `CONSTRAINTS.md` (8 standing + 28 observed), `ARCHITECTURE.md` (ownership, contracts, documented SQL D-Q1..D-Q11, decisions A1-A20), `SCOPE.md`, `docs/REFERENCE.md`, `docs/LAYERS.md`, integrator scaffold, `tools/l1_index_write_check.py`, `tools/l4_gold_oracle.py`. Commits `5b1b5fa`, `7a8edb4`, `4987cca`, `4e6fdc4`, `bafaa53`. Reports and prompts archived under `docs/rounds/R00_*`.

**Round 1 / wave 1 dispatch record** (Guide v1.3 section 6 / section 2 I as amended; effort xhigh via Workflow `opts.effort`; agent_type general-purpose write-scoped to a worktree; pattern #3 candidate; execution_mode concurrent, three = the cap; tier_escalation none; every prompt archived `docs/rounds/R01_<module>.prompt.md` beginning with the section 2 L text as amended):

| Strand | Module | Model (client-reported) | dispatched_utc | t_start (self-reported) | t_end | returned_utc | Branch head / commits | Gates at the worktree root | Status |
|---|---|---|---|---|---|---|---|---|---|
| S3-B1 | corpus_adapter | `claude-sonnet-5` | 03:08:28.129Z | 03:10:51.583Z | 03:31:39.278Z | 03:33:52Z | `0395f74` / 5 | ruff clean; pytest 15 passed; L1 PASS | COMPLETE |
| S3-B2 | probe | `claude-sonnet-5` | 03:08:28.129Z | ~03:17:53Z (approximate, self-reported deviation) | 03:31:20Z | 03:36:06Z | `736ed1c` / 6 | ruff clean; pytest 12 passed; L1 PASS (6 allowlisted hits, fixture builder only) | COMPLETE |
| S3-B3 | diagnostics | `claude-sonnet-5` | 03:08:28.129Z | 03:19:47.334Z (closeout-phase capture, self-reported) | 03:21:38.360Z | 03:22:36Z | `c65be94` / 3 | ruff clean; pytest 25 passed; L1 PASS | COMPLETE |

Concurrency audit: three strands live 03:08:28Z-03:22:36Z, two to 03:33:52Z, one to 03:36:06Z -- never four; `concurrency_exceedance`: none. Workflow run wf_e159befc-5e3: 762,694 subagent tokens, 257 tool uses, 1,526 s.

**Integration (integrator, main).** Merges `--no-ff`: `8fa5648` corpus_adapter, `c479c6f` diagnostics, `6f7f42c` probe -- zero conflicts (disjoint owned paths). Post-merge gates: ruff clean; L1 `hits=6 allowlisted=6 defects=0 PASS`; pytest 48 passed / 4 failed -> the four were **invalidated evidence after merge** (three diagnostics tests assumed the adapter module was unbuilt; one assumed no probe-run directory) -> repaired by the integrator (explicit `explorer.app.ADAPTER_ABSENT` sentinel; temporary runs glob) -> 52 passed. Change requests applied: `Subtree` canonicalized in `explorer/models.py` (B1); `base.html` `html_attrs` / `main_attrs` blocks and the DOM state convention `data-mode` / `data-selected-doc-id` / `data-doc-id` (B2, B3; ARCHITECTURE A19). Recorded as A20 and as Pattern #3 disposition evidence (section 2 J): merge conflicts 0; contract change requests 1; template change requests 2; invalidated tests 4. Integration commit `6b74f0d`; boundary commit `62685da` (`build_state.json`: returns, merges, gates, L4, probe, module status, `next_action`). The three merged worktrees were removed; their branches remain as history. Builder rounds consumed **3 of 28**.

**L4 oracle (`tools/l4_gold_oracle.py`, live index, report `docs/probe-qualification/gold_v1_1_compat.json`).** Adapter ids identical to the module's direct `search_hybrid` ids on **40/40 rows** (the compatibility claim the build owns holds). Recall@10 = **0.675 (27/40)** for the adapter and for the module itself, against the standing 0.700 (28/40; RHACO-ANL-20260712-001 line 34). Hit rule as the gold freeze defines it: any-target-in-top-10; family targets under `resolve: supersession-head` resolved to the live head (supersedes-edge terminal, max-version fallback per RHACO-ANL-20260712-002; rows 16, 27, 35 resolve to `RHACO_Castle_Stability_Reference_v1_1`, `RHACO_Card_YAML_Schema_Specification_v1_9`, `RHACO_Glossary_v4_10`). The one changed row is **27**: the family head was v1_8 at the July evaluation and is v1_9 today (cut 2026-07-24); the hybrid top-10 contains eight older versions including v1_8 but not v1_9. Rows 16, 31, 35 miss as they did then; S1 8/8, S2 6/6, S3 6/12, S4 4/6, S5 3/8. Verdict **SCOPE** (PROMPT.md section 8 item 9 path): `SCOPE.md` row "Gold v1.1 compatibility" records the regression and its cause -- corpus evolution, not the adapter; nothing in this build may re-rank or re-index to recover it (prompt section 11). Live db pin unchanged before and after the run.

**Probe production-path run** (`docs/probe-qualification/runs/20260904T034711Z`; live db read-only; the probe spawned uvicorn on port 8791 as its own foreground child and tore it down): verdict PASS -- `healthz` 200; `diagnostics` 200 page (screenshot, console errors 0, failed requests 0, `data-mode="hybrid"`, `data-vector="available"`); W1-W10, KG, KB1-KB5 report `not_implemented_yet` cleanly (wave 2). The diagnostics page on the live index shows 1,442 cards / 1,607 fts_docs / 10,507 edges / 2,653 aliases, vector available (probe ~46 ms), exclusion sets, the rerank standing text with the artifact directory present, and the authority notice.

**Push record (O-1 ruling).** `git push -u origin main` at 2026-09-04T03:49:4xZ from the boundary commit: `* [new branch] main -> main` (the remote was empty; fast-forward by construction, no force); `origin/main` = `HEAD` = `62685dad6f40f91c6b1b84500a213f0a43ea8673`; `git ls-remote` agrees; tree clean.

**Criterion-6 close pins (session 3, 03:50Z).** RHACO `HEAD` = `origin/main` = `3b860bfe4167345a89258ce13c4ff946faeb5cb7`; the only commit since the section 0.6 pin `0243874` is the Director-directed A1 filing (J.1) -- no build commit; `git status --porcelain` empty; no change under `docs\`. `rhaco\RHACO_corpus_index.py` `7548e47ebdcc24cdc0621208d06673542889c75b000780c650628b0678a5f098` unchanged. `C:\RHACO\index\corpus_index.db` `274c1e96b67b560073be9683ff0788ca86192a5ca40ae59ca821fe83008e28f1`, 115,093,504 B, mtime 2026-09-04T02:10:08.415Z -- identical to the J.3 open pin; no `-journal` / `-wal`; no `catalog_librarian_validation.log` reindex entry and no `mcp_validate.log` census entry after the two reconciled ones (J.5). Every build-side access to the live index (15 adapter tests, the oracle's 80 hybrid calls, the probe run) was through the module's `connect()` and left the file byte-identical.

**D-12 application note (session 3).** Every strand carried the section 2 L text as amended (D-12 block + D-3 block) verbatim first; the only processes launched were the workspace venv interpreter (ruff, pytest, the fixture builder against a fixture db under `tmp_path` / `fixtures/`, the oracle) and, once, uvicorn as the probe's foreground child; no `run_in_background`; no RHACO production process, device transport, or `C:\RHACO\data\` access by any strand or by the orchestrator. Production PIDs at close identical to item 14 (launcher 15268; logger 19516; sidecars 19544 / 19552 / 19588 / 20156 / 19308); `castle_event_log.csv` appending (mtime 03:41:31Z, 4,539,306 B). Self-reported deviations of record: B2 and B3 captured their opening timestamps late (approximated, not fabricated); I4 treated junction-spelled grep hits as out of scope (corrected by the orchestrator, CONSTRAINTS O26); no device-safety deviation.

**Workspace at close.** `main` = `origin/main` = `62685da`; 20 commits; tree clean; `.worktrees/` empty (gitignored); branches `build/corpus_adapter`, `build/diagnostics`, `build/probe` merged. `build_state.json` `next_action` names wave 2.

## K. Session 4 -- re-entry under RHACO-CCX-20260904-001 section 4.1; Director rulings; correction round; wave 2

Opened 2026-09-04T23:39:34Z (client PID 9800, same launching shell PID 24716 as sessions 1-3). Executor unchanged: Claude Code 2.1.260, `claude-fable-5-1`, effort xhigh (`/effort` ultracode; `settings.json` unchanged since session 3) -- no section 2 M halt. Instruction of record: `working\S4_launch_note_HND-20260903-001.md` (Director ruling C' ratified), read in full and executed in order. Full evidence: findings file, "Session 4 re-entry" record.

### K.1 Pre-findings note -- Director-directed Rule-2 commit and push of the CCX-20260904-001 pair (2026-09-04/05)

| Step | Result |
|---|---|
| Untracked set (23:47Z) | exactly the CCX pair; the launch note, the two sealed files, the handback and the findings file confirmed ignored (`.gitignore` `/working/`) |
| P1 validate (23:52:39Z) | `--validate-only --baseline --no-reindex`: 1443 cards, missing=0, orphans=3, carded-with-parent=128, carded-with-code=0, broken=0, errors=0, baseline=MATCH, warnings=5203; live db untouched |
| Commit | `4032742f1fe87d056c60ffc05c8ae2eb2b81de0d` @ 23:57:44Z -- `RHACO-CCX-20260904-001: file build shepherd session handoff` (59 chars, ASCII, one GOV-ID; body: purpose, both shas + bytes, the census, the 1443-vs-1444 note). Both blobs byte-identical to disk; commit-msg and structure-guard hooks passed |
| Closeout gate (23:57:45Z-23:59:04Z) | librarian PASS (26.1s; logged full reindex, baseline MATCH) / **regression FAIL** (33.9s) / **auditor FAIL** (19.5s, MISMATCH +37/-0, unwaived_gating 346 -> 348) / structure-guard PASS (13/13) / git-status-clean PASS / **overall FAIL exit 1** -- the same two members red as at the session-2 and session-3 pushes |
| Attribution of the red to `4032742` | regression: `test_required_claim_floor_v9` (INC-20260829-001) delta 0; `test_corpus_index_self_edges` (INC-20260830-001, stale `SELF_EDGE_BASELINE` pin) delta 0 -- the CCX has a unique id and no self-reference; identical failure set to session 3 (2 failed, 27 passed). auditor: 35 adds unchanged from J.1 + **2 new**, both the CCX's own forward references to milestone anchors (`RHACO-CMP-20260903-001 M0`, `RHACO-CMP-20260808-001 M3`; same shape as the section A guard-commit adds). Zero adds name the Subagent Guide. No baseline write, no waiver edit, no `tests\` edit |
| Push (2026-09-05T00:01:59Z) | `3b860bf..4032742  main -> main`; `origin/main` = `HEAD` = `4032742...`; `ls-remote` agrees; 0/0; tree clean. Pushed red under the launch note's instruction, on the section A / J.1 precedent; attributed, not accepted |

A Director-directed filing act, not a build commit: the criterion-6 close check reads `4032742` as the CCX filing (as `3b860bf` is the A1 filing).

### K.2 Section 0 re-entry summary (section 2 M set; launch note step 3)

| Item | Verdict (S4) | Evidence (detail in the findings file) |
|---|---|---|
| 1 | PASS, unchanged | `c7104f1a...` 15,216 B; card Active / OPEN |
| 5 | PASS -- four pins unchanged; **db pin moved twice, both reconciled** (K.3) | module `7548e47e...` 118,123 B; librarian `22e1923a...` v1.19; gold v1.1 `f20bc6a2...`; v1.2 `d456f981...` |
| 6 | PASS (note D-0.4), then clean | pre-commit: `3b860bf` = origin/main, untracked = exactly the CCX pair (the directed filing set); post-push: `4032742` = origin/main, porcelain empty |
| 7 | PASS | `main` = `origin/main` = `62685da`, clean, `.worktrees/` empty; `DISPATCH_PARAMETERS.md` `f6e9cd3e...` 9,730 B; baseline gates on main before any act: ruff clean, L1 PASS (6 allowlisted hits), pytest 52 passed. Commit count 12 first-parent / 26 reachable (CCX section 0.4's "20" matches neither; recorded) |
| 8 | PASS | `PROMPT.md` `dd75dd2e...` 28,665 B; `RATIONALE.md` `3070c961...` 26,151 B; line 1 / `Start now.` MATCH |
| 9 identity | PASS -- no halt | 2.1.260; `claude-fable-5-1`; xhigh; `settings.json` unchanged since S3 (`cea59185...`); launch cwd = workspace project root, as in S3 (recorded); second `claude.exe` PID 28760 (parent 25328) observed, not evaluated |
| 9(a) / 9(b) as amended / 9(c) | PASS | live schema unchanged; both cap variables present (inherited from launching shell 24716); spawn-depth result stands; tier strings carried |
| 14 | PASS -- recorded before any strand | identical PID set to sessions 2-3 (launcher 15268; logger 19516; sidecars 19544 / 19552 / 19588 / 20156 / 19308); `castle_event_log.csv` appending (mtime 23:45:43Z at the 23:47Z census) |

**Section 0 PASSES in full** (session 4). Items 2, 3, 4, 10-13 stand on their session-1/2 records.

### K.3 Criterion-6 reconciliation (launch note step 2) and the open pin

| Pin | sha256 | Bytes | mtime (UTC) | Explained by |
|---|---|---|---|---|
| J.7 close (S3) | `274c1e96...` | 115,093,504 | 2026-09-04T02:10:08.415Z | gate librarian reindex 19:10:08 local (J.3) |
| At S4 re-entry, before any act | `6aeb72d6c7c6612df4d3332b145f0f2136d700c52b1dc1ea0e13477cb16bd7eb` | 115,093,504 | 2026-09-04T04:04:23.766Z | **chat-side MCP pair write of the CCX** (launch note (a), 04:04:15Z): one new `cards` row for `RHACO-CCX-20260904-001` (`indexed_at_utc 04:04:23.756Z`, `card_mtime_utc 04:04:15.006Z`, physical-path spelling), `corpus_card_count` 1442 -> 1443, `corpus_cards_sha` -> `8d81893f...`, `last_full_reindex_utc` unchanged, no `fts_docs` row, no edges (read through the module's `connect()`, byte-neutral). Log: **rotated** `docs\logs\mcp_validate.log.2026-09-03` line 8, `2026-09-03 21:04:37,274 ... cards=1443 errors=0` (04:04:37Z, 14 s after the row) |
| After the S4 gate (**open pin for the build from here**) | `61ea99c98b3b26922e7dd306ff0dd06944f662cc57dc0044dc9c6dbbffd7a0c0` | 115,089,408 | 2026-09-04T23:58:11.272Z | gate librarian member full reindex at `4032742`: `catalog_librarian_validation.log` `2026-09-04 16:58:11,284 corpus index reindexed: 1443 cards, 2253 programs, 14735 tags, corpus_cards_sha=9d36e8973132`; one page smaller |

Read-only censuses with no db touch (mtime stayed 04:04:23Z): `mcp_validate.log` line 2 `2026-09-04 16:15:08,475 ... cards=1443` = the note's (b) 23:14:59Z census (summary 9 s after invocation); and line 1 `2026-09-04 15:55:00,294 ... cards=1443` (22:55:00Z), **not listed in the launch note** -- recorded as a discrepancy, not a halt. No unexplained change; no criterion-6 halt. Closeout note: the reconciliation sources include the rotated `mcp_validate.log.YYYY-MM-DD` files (the 04:04Z entry had rotated out of the live log before this session opened).

RHACO `HEAD` = `origin/main` = `4032742f1fe87d056c60ffc05c8ae2eb2b81de0d`; commits since the section 0.6 pin `0243874`: `3b860bf` (A1 filing, J.1) and `4032742` (CCX filing, K.1) -- both Director-directed, no build commit; porcelain empty; `rhaco\RHACO_corpus_index.py` `7548e47e...` unchanged.

### K.4 Director ruling AC-3 (launch note step 4) -- recorded; rides Amendment A2 at terminal state

CMP-20260903-001 section 3 criterion 3 and HND section 8 item 4 are read as **progressive qualification at the point an observable becomes measurable**, not as a single gate before the first probe citation: **Q0** probe mechanism (server lifecycle, screenshot, DOM capture, console / network / page-error capture, an observed-fault verdict predicate -- K.6); **Q1** search surfaces (wave 2): known-good exact-id and lexical-phrase cases; KB1 (wrong document for identifier), KB2 (stale / mismatched card metadata); **Q2** reader (wave 2): KB4 (broken jump / line target); known-good body-and-card match to fixture source; **Q3** lineage (wave 3): KB3 (reversed relation direction); known-good typed edge direction; **FINAL**: the complete known-good + known-bad suite before the critic's first round and before any PASS / BOUNDED_FAIL declaration. No L2 result becomes acceptance evidence for a capability until the probe is qualified for that capability's failure classes.

- The wave-1 run `docs/probe-qualification/runs/20260904T034711Z` is **reclassified `FRAMEWORK_SMOKE_PASS`** -- production-path mechanism evidence only; not qualification, not product evidence. The worktree run `20260904T033047Z` carries the same classification. Neither run directory is rewritten; the classification lives in `build_state.json` (`modules.probe.run_classifications`; `rounds[1].probe_production_run.reclassified`) and the probe README from Q0 on.
- **Temporal miss against criterion 3, recorded (section P, P-1):** as written, criterion 3 requires qualification complete before any product claim; PROMPT.md section 14 orders step 5 (build and qualify the probe) before step 8, but section 4 wave 1 co-dispatches probe and diagnostics, and KB1-KB4 need wave-2 / wave-3 surfaces. The J.7 line "probe production run PASS" was a mechanism verdict under the round-1 predicate, not a product claim; it is read as FRAMEWORK_SMOKE_PASS from this session on.
- Probe result-state vocabulary from Q0 onward: `FRAMEWORK_SMOKE_PASS | QUALIFICATION_PASS | PRODUCT_EVIDENCE_PASS | FAIL | INCOMPLETE`; each run records `qualification_state`, `product_evidence_eligible`, `qualified_failure_classes`.

### K.5 Launch-note and CCX discrepancies recorded this session (observations, not halts)

1. CCX-20260904-001 section 0.2: "this CCX pair makes 1444" -- observed 1443 at filing (04:04:37Z census) and at the commit; 1442 + one card = 1443. Authoring arithmetic error (P-6); the CCX is not edited.
2. Launch note step 2 names two chat-side entries; three are present (the 22:55:00Z read-only census is unlisted). K.3.
3. Launch note step 2 names `docs\logs\mcp_validate.log`; the 04:04Z entry lives in the rotated `mcp_validate.log.2026-09-03`. K.3.
4. CCX section 0.4 "20 commits" vs 12 first-parent / 26 reachable at `62685da`. K.2 item 7.
5. CCX section 4.1 step 2 `cd C:\highsierralabs\RHACO` vs the client's project root = the workspace (as in session 3). K.2 item 9.

### K.6 Correction round (launch note steps 5-7) -- CLOSED at the correction boundary `c96664dd82e19da730cbd741f0e2ace43501121d` (2026-09-05T00:30:14Z)

**Dispatch record** (Guide v1.3 section 6 / section 2 I as amended; effort xhigh via Workflow `opts.effort`; agent_type general-purpose write-scoped to a worktree; pattern #3 candidate; execution_mode concurrent, two = under the cap of three; tier_escalation none; workflow run `wf_0d9310f0-d5c`; both worktrees created from `62685da` with the module branch fast-forwarded to it):

| Strand | Module / round | Model (client-reported) | dispatched_utc | t_start (self) | t_end (self) | returned_utc | Branch head / commits | Gates at the worktree root (re-run by the integrator: identical) | Status |
|---|---|---|---|---|---|---|---|---|---|
| S4-C1 | probe, round 2 of 4 | `claude-sonnet-5` | 2026-09-05T00:08:56.827Z | 00:09:30Z | 00:22:55Z | 00:26:28Z | `96440d8` / 5 | ruff clean; pytest tests/probe 40 passed (12 + 28 new); L1 PASS (6 allowlisted) | COMPLETE |
| S4-C2 | corpus_adapter, round 2 of 4 | `claude-sonnet-5` | 2026-09-05T00:08:56.827Z | 00:09:34Z | 00:14:49Z | 00:18:33Z | `202282f` / 4 | ruff clean; pytest tests/corpus_adapter 17 passed (15 + 2 new); L1 PASS; live db `corpus_cards_sha` / mtime unchanged across its session | COMPLETE |

Concurrency audit: two strands live 00:08:57Z-00:18:33Z, one until 00:26:28Z -- never three, never four; `concurrency_exceedance`: none. Workflow usage: 2 agents, 0 errors, 332,422 subagent tokens, 117 tool uses, 1,024 s. Prompts archived verbatim, each beginning with the section 2 L text as amended: `docs/rounds/R01C_probe.prompt.md` (sha256 `b601c51d...`, 18,135 B), `docs/rounds/R01C_corpus_adapter.prompt.md` (`23ef44b5...`, 8,906 B). **Dispatch form of record (session 4):** the dispatched text = the two section 2 L blocks verbatim + a pointer naming the archived prompt file and its sha256; each strand read the file (an authorized read of one file in the main checkout), confirmed the hash, and executed it. Reason: a prompt of 18 KB carrying Windows paths cannot pass through the client's JSON tool parameter without hand-escaping every backslash (`\t`, `\b`, `\f`, `\n`, `\r` inside paths would silently corrupt); the archived file is the prompt of record and the hash check makes the dispatched and archived texts identical by construction. Both strands captured their opening timestamp at the first tool call (rule 7 reworded after P-5); no deviations reported.

**Step 5 -- probe verdict predicate repaired (PROMPT.md section 2 repair rule; probe round 2 of 4).** `tools/probe_corpus_explorer.py` v1.1: `evaluate_page(observation, spec) -> Verdict` (rules a-e: navigation error; http >= 400 unless `expected_status`; any console error; any failed request; declarative `data_mode_in` / `data_vector_in` / `require_title` assertions), a Playwright `pageerror` listener mirrored into `console_errors` (empirical finding: the diagnostics template's `throw` arrives on `pageerror`, not on the `console` event -- the round-1 listener alone would never have seen it), `FAULT_CLASS_TABLE` (KB1..KB5 -> `explorer.faults.FAULTS`) and `FAULT_TEXT_MARKERS` (KB5), the AC-3 vocabulary (`run_kind` qualification | smoke | product; `qualification_state`; `product_evidence_eligible`; `qualified_failure_classes`; `incomplete_presets`; `product_evidence_blocked_by`), `derive_qualification()`, the qualification ledger `docs/probe-qualification/qualification_ledger.json` (`--ledger`), exit codes 0 / 1 (FAIL) / 3 (INCOMPLETE); the diagnostics preset now requires 200 + `data-mode` in {hybrid, hybrid-degraded-lexical} + `data-vector` + a title; a run selecting a stub is INCOMPLETE, never a PASS. Q0 evidence, committed: `runs/20260905T002014Z` (clean fixture, diagnostics: FRAMEWORK_SMOKE_PASS), `runs/20260905T002024Z` (`--fault console_error`: diagnostics FAIL with `fault_detected` / `fault_class KB5`; QUALIFICATION_PASS; ledger gains KB5), `runs/20260905T002034Z` (`--preset all`: INCOMPLETE, exit 3, 16 stubs listed). 28 new tests (predicate rules, derivation states, a subprocess fault run against a tmp ledger). **Q0 is qualified.** Root cause recorded (section P, P-2). The probe's remaining rounds: 2 of 4 (round 3 = wave-2 runners; round 4 = Q3 / FINAL in wave 3) -- a later repair round would exhaust its budget; flagged for the Director.

**Step 6 -- criterion-6 guard (corpus_adapter round 2 of 4).** `CorpusAdapter._require_db_exists()` shared by `__init__` and `connect()`; `connect()` raises `ConfigurationError` per call when the file is absent; `tests/corpus_adapter/test_connect_guard.py` (construct against an empty tmp file, remove it, `connect()` raises, `os.listdir(tmp) == []`; and `connect()` still returns a usable connection on the live index). **Disclosed by the strand, not fixed (outside its deliverable): `explorer/corpus_adapter/availability.py` line 49 calls `RHACO_corpus_index.connect(db_path)` directly inside the vector-availability probe, bypassing the adapter's guard -- the same O10 hazard class at a second call site.** For Director disposition: a follow-up adapter round would either guard-and-raise inside `probe()` (changing `vector_availability()` from "never raises") or route the call through `CorpusAdapter.connect()`. Recorded in `build_state.json` (`modules.corpus_adapter.open_issues`).

**Integration (integrator, main).** Merges `--no-ff`: `90c8c71` corpus_adapter, `5ba2274` probe -- zero conflicts (disjoint owned paths). Post-merge gates: ruff clean; L1 `hits=6 allowlisted=6 defects=0 PASS`; pytest **82 passed** (52 + 28 + 2); no invalidated evidence (Pattern #3 evidence: conflicts 0, contract change requests 0, template change requests 0, invalidated tests 0). **Integrator product-path run** `docs/probe-qualification/runs/20260905T002845Z`: `--preset diagnostics` on the live index (read only through the server): http 200, `data-mode="hybrid"`, `data-vector="available"`, no console / page errors, no failed requests -> `run_kind product`, **`qualification_state PRODUCT_EVIDENCE_PASS`** (`product_evidence_eligible true`, KB5 ledgered) -- the first product-evidence-eligible observation of the build, for the diagnostics surface alone. Live db pin before and after: `61ea99c9...`, 115,089,408 B, 23:58:11.272Z -- unchanged.

**Step 7 -- `build_state.json` bookkeeping** (commits `67b01d0` and the boundary): session 3 `closed_utc` 03:50Z (history preserved; `62685da` not rewritten); `concurrency_exceedances` = the three session-2 rows (S2-B4, S2-D4, S2-E4) with the qualifier `pre_a1_3 / deliberate cap probe`, so the aggregate and the rows agree; `workspace_pushes` = the 03:49Z push (`* [new branch] main -> main`, `62685da`), with the structural note that each push is recorded at the next commit; round `1C` entry with both dispatch records (all seven governance fields + `dispatched_utc` / `returned_utc`); builder rounds consumed **5 of 28** (corpus_adapter 2/4, probe 2/4, diagnostics 1/4); AC-3 ruling block and the two run reclassifications; `next_action` = wave 2. Correction boundary commit **`c96664dd82e19da730cbd741f0e2ace43501121d`** (2026-09-05T00:30:14Z); wave 2 dispatches from it.

### K.7 Wave 2 = round 2 (launch note step 9) -- catalog, search, reader built, merged, integrated; probe round 3 (wave-2 runners) -- see K.7.2

#### K.7.1 Wave-2 builders (round 2 of the build; module round 1 of 4 for each)

**Dispatch record** (Guide v1.3 section 6 / section 2 I as amended; effort xhigh via Workflow `opts.effort`; agent_type general-purpose write-scoped to a worktree; pattern #3 candidate; execution_mode concurrent, three = the cap; tier_escalation none; workflow run `wf_e5bc7467-57b`; worktrees created from the correction boundary `c96664dd` on new branches `build/catalog`, `build/search`, `build/reader`; dispatch form = the section 2 L blocks verbatim + sha-pinned pointer, as in K.6):

| Strand | Module | Model (client-reported) | dispatched_utc | t_start (self) | t_end (self) | returned_utc | Branch head / commits | Gates at the worktree root (re-run by the integrator: identical) | Status |
|---|---|---|---|---|---|---|---|---|---|
| S4-B4 | catalog | `claude-sonnet-5` | 2026-09-05T00:31:39.127Z | 00:32:20Z | 00:47:25Z | 00:48:09Z | `fd365fa` / 4 | ruff clean; pytest tests/catalog 24 passed (full worktree 106); L1 PASS | COMPLETE |
| S4-B5 | search | `claude-sonnet-5` | 2026-09-05T00:31:39.127Z | 00:32:23Z | 00:49:39Z | 00:51:30Z | `a6987fa` / 3 | ruff clean; pytest tests/search 22 passed (full worktree 104); L1 PASS | COMPLETE |
| S4-B6 | reader | `claude-sonnet-5` | 2026-09-05T00:31:39.127Z | 00:32:23Z | 00:54:50Z | 00:59:15Z | `f6a97f1` / 2 | ruff clean; pytest tests/reader 34 passed (full worktree 116); L1 PASS | COMPLETE |

Concurrency audit: three strands live 00:31:39Z-00:48:09Z, two until 00:51:30Z, one until 00:59:15Z -- never four; `concurrency_exceedance`: none. Workflow usage: 3 agents, 0 errors, 810,297 subagent tokens, 274 tool uses, 1,622 s. Prompts archived verbatim, each beginning with the section 2 L text as amended: `docs/rounds/R02_catalog.prompt.md` (`84704856...`, 14,725 B), `R02_search.prompt.md` (`795dd48b...`, 15,807 B), `R02_reader.prompt.md` (`213db495...`, 18,744 B); every strand confirmed its prompt's sha256 before acting and captured its opening timestamp at the first tool call. **Deviation of record (return channel, not work):** S4-B4's StructuredOutput `report` field arrived as the six-character string `ededed` instead of the report markdown; the on-disk `docs/rounds/R02_catalog.report.md` (301 lines) is complete and is the record. No other deviations reported.

**What landed.** Catalog (`GET /`, `/api/catalog`): nine filters parsed from the query string, the CMP lifecycle control rendered separately and only when CMP is selected or the under-filter facets carry a value, sortable table with `data-doc-id` / `data-card-ref` / `data-status` / `data-lifecycle-state` per row, the visible "frozen" marker, the "informational (non-CMP)" marker, a fixed "no cards match" state (no fabrication), filter-preserving pagination, `catalog.css`. Search (`GET /search`, `/api/search`): four modes with hybrid the default; identifier mode lists every card row per id (O7) and marks an unresolved id "no card row in the index"; the lexical-only filters and the hybrid/graph post-hoc narrowing are labelled exactly as ARCHITECTURE 4.3 requires; degraded results carry the exact notice and never the bare "Hybrid" label; every result has the "Why this result" disclosure with the literal component-rank line; `data-mode` / `data-vector` / `data-mode-effective` / `data-doc-id` / `data-card-ref` / `data-rank`. Reader (`GET /doc/{card_ref}`, `/api/doc/{card_ref}`): the card FILE, the INDEX ROW ("as indexed", with a per-field comparison exposed as `data-card-index-mismatch`), and the DOCUMENT kept distinct (S1); markdown-it rendering with heading `data-line` sourced from the adapter and independently verified (`data-line-verified`, O23); a numbered source view; `?line=N` jump with an explicit not-found state; the card panel with dependencies, supersession / amendment state, campaign parentage, unresolved targets marked, the local path, and a `/lineage/<card_ref>` link (wave 3); 404 for unknown or traversal `card_ref`; the `stale_card` and `broken_jump` faults visible on the page and the API (34 tests, incl. fixture-fault clients).

**Integration (integrator, main).** Merges `--no-ff`: `2698b66` catalog, `8dc8810` search, `32808db` reader -- zero conflicts (disjoint owned paths). Post-merge gates: ruff clean; L1 `hits=6 allowlisted=6 defects=0 PASS`; pytest **162 passed** (82 + 24 + 22 + 34); **no invalidated evidence** -- the wave-2 builders coded against the merged adapter and diagnostics, not fakes, so nothing assumed another module's absence (Pattern #3 evidence: conflicts 0, contract change requests 0, template change requests 1, invalidated tests 0). Change request applied: `base.html` `{% block head_extra %}` hook (catalog, S4-B4) as ARCHITECTURE **A21**. Integrator record: CONSTRAINTS **O29** -- the search builder's substrate finding that `RHACO_corpus_index.search_fts` iterates a bare `doc_type_filter` string character by character (a bare `"EVT"` matches nothing, silently); callers pass a list; the adapter passes the argument through unchanged (a round-3 adapter change request candidate; module finding for the Director, not a build defect). Integration commit `2d83808` (2026-09-05T01:01:58Z); wave-2 records in `build_state.json` at `011daa2`. Builder rounds consumed **8 of 28** (corpus_adapter 2, probe 2, diagnostics 1, catalog 1, search 1, reader 1). The three wave-2 worktrees were removed after the merge; the branches remain as history.

**Open items from the wave-2 reports (for the Director / later rounds, not halts):** reader -- the generic lineage list shows an `amends` edge's literal from_id (the filename stem) unlinked because `edges_for` returns it that way (O7/O8; a module-boundary question for the lineage round); `.txt`-carded and binary-carded documents have no fixture; search -- `search.css` not written; line anchors applied to every sibling row of a shared id (code inspection only); catalog -- compact-abstract marker convention and the `path_prefix` / `project_knowledge` controls untested. Wave 3 (lineage, web) remains next after this round.

#### K.7.2 Probe round 3 -- wave-2 runners W1-W10 / KG / KB1-KB5 under AC-3 Q1 / Q2 -- CLOSED; round-2 boundary `6a8353705a2933cdd4696391da904ed3c680b398`

**Dispatch record** (sequential, one strand live, after the wave-2 merge; workflow run `wf_6149474e-16a`; worktree `.worktrees/probe` on `build/probe` fast-forwarded to the integration commit `2d83808`; dispatch form as in K.6):

| Strand | Module / round | Model (client-reported) | dispatched_utc | t_start (self) | t_end (self) | returned_utc | Branch head / commits | Gates at the worktree root (re-run by the integrator: identical) | Status |
|---|---|---|---|---|---|---|---|---|---|
| S4-B7 | probe, round 3 of 4 | `claude-sonnet-5` | 2026-09-05T01:03:27.115Z | 01:03:53Z | 01:55:51Z | 01:58:15Z | `a0dacd7` / 6 | ruff clean; pytest tests/probe 59 passed (40 kept + 19 new); L1 PASS | COMPLETE |

Prompt archived verbatim: `docs/rounds/R02_probe.prompt.md` (`67cb18fe...`, 16,679 B), beginning with the section 2 L text as amended; hash confirmed by the strand before acting. Workflow usage: 1 agent, 0 errors, 446,468 subagent tokens, 209 tool uses, 3,269 s. Concurrency: one strand live throughout (01:03:27Z-01:58:15Z).

**What landed** (`tools/probe_corpus_explorer.py` v1.2; merge `3d81949`): a multi-step page driver (`Session`) with accumulated console / page-error / request capture per step; real runners for W1-W4, W7-W10, KG, KB1, KB2, KB4, KB5 (W5, W6, KB3 remain `not_implemented` for the lineage surface -- Q3, wave 3); fault-attribution predicates next to the registry (KB1: the identifier page's first card_ref rotated to the amendment's; KB2: the reader's `data-card-index-mismatch` marker OR the probe's own card-file-vs-index comparison; KB4: a `data-line-verified="false"` heading OR the probe's own heading/line re-check, re-implemented rather than imported so it is independent; KB5: the text marker; W10: every browser request on 127.0.0.1); `db_meta_before` / `db_meta_after` per run read through `/api/diagnostics`, never by opening the db; the README's "which presets are real" table. 19 new tests (17 pure-predicate, 2 subprocess: KG smoke and a KB2 qualification against a tmp ledger).

**Q1 / Q2 qualification (fixture, fresh index):** `20260905T014232Z` KG FRAMEWORK_SMOKE_PASS (E000777 -> the EVT card first; "quartz lattice lantern" -> the ANL at line 15 with the excerpt, page and API agreeing; reader body sha256 == the file's; card panel == the card file parsed from disk; `/api/doc` lines == the file's lines); `20260905T014234Z` KB1 QUALIFICATION_PASS (first card_ref rotated to the amendment's, detected); `20260905T014244Z` KB2 QUALIFICATION_PASS (mismatch marker `title,status`; indexed "Fixture Analysis [STALE]" / Draft vs file "Fixture Analysis" / Active); `20260905T014247Z` KB4 QUALIFICATION_PASS (4 headings shifted +50, page marker and independent re-check both fire); `20260905T014249Z` KB5 QUALIFICATION_PASS (one composite run: the throw appears on diagnostics only; catalog, search, reader stay clean in the same run -- the fault is confined to the one template that carries it, demonstrated rather than assumed); `20260905T014258Z` W8 FRAMEWORK_SMOKE_PASS (a temp copy of the fixture corpus with one added card, its own uvicorn child, the freshness POST clicked -> DRIFT reported with the added id, never repaired). **Ledger: KB1, KB2, KB4, KB5.** Q0-Q2 qualified; Q3 (KB3, typed edge direction) and FINAL wait for lineage.

**Production path (live index, read only through the server; db mtime identical before and after every run):** `20260905T014316Z` W1, `...4319Z` W2 (Gold v1.1 S2 row 10 "tungsten putty seal" -> `RHACO-MTN-20260520-001`; the returned excerpt verified against the file on disk), `...4330Z` W3 (Gold v1.1 S3 row 21 in hybrid mode with the vector channel available; the top-10 byte-identical, in order, to the L4 oracle's recorded ids; every result carries the component-rank-not-exposed line), `...4336Z` W4 (36 CMP rows; `lifecycle_state=ACTIVE` narrows to 9 and `status=Active` to 29 independently; both attributes on every row; no lifecycle value inside a status attribute), `...4346Z` W9 (jump to the searched line; both regions present), `...4349Z` W10 (catalog, W1, W2, W9, W4, diagnostics in one context; 25 requests, none off 127.0.0.1), `...4401Z` healthz, `...4402Z` diagnostics, `...4404Z` W7 (`--disable-vec`: search and diagnostics both `hybrid-degraded-lexical`, the exact notice, `data-vector="unavailable"`, identifier and lexical searches still return) -- **all `PRODUCT_EVIDENCE_PASS`** (the ledger covers each preset's required classes; each preset's own assertions passed). Under AC-3 these are the first acceptance-eligible L2 observations for the search, catalog, reader, and diagnostics surfaces; W5 / W6 (lineage) are not yet observable.

**Integration (integrator, main).** Merge `3d81949` (`--no-ff`, zero conflicts); post-merge gates ruff clean, L1 PASS, pytest **181 passed** (162 + 19); no invalidated evidence. **Integrator re-runs at the boundary** (independent reproduction of the strand's evidence with the merged tool): `20260905T020023Z` KB2 (fixture, `--fault stale_card`) QUALIFICATION_PASS; `20260905T020025Z` W1, `...0028Z` W9, `...0032Z` W10, `...0036Z` W7 (`--disable-vec`) on the live index -- all PRODUCT_EVIDENCE_PASS; live db pin `61ea99c9...` / 23:58:11Z unchanged before and after. The ledger's KB2 pointer now names the integrator's run (latest-evidence policy); the strand's KB2 run stays in `runs[]`.

**Strand deviation of record (for Director disposition).** S4-B7 disclosed that, before its runner code existed, it launched `uvicorn explorer.app:create_app` directly from a scratch script (outside the worktree, never committed) against the FIXTURE db to inspect `/api/*` shapes and fault behaviours -- the workspace interpreter running workspace code against the fixture (never the live index, never a RHACO_* process, no device), but not "via the probe" as the section 2 L D-12 block and DISPATCH_PARAMETERS item K / CONSTRAINTS O12 require (the probe is the only sanctioned launcher). The L text says a violation is a halt, not a retry; the strand disclosed instead of halting. Every committed evidence run was produced by the probe, and the integrator reproduced the production presets independently after the merge, so the evidence stands on its own; the conduct deviation is recorded in `build_state.json` (`strand_deviations_of_record`) and here, on the session-2 precedent (D-3 breaches recorded as deviations), and is not dispositioned by the orchestrator. Related observation for the same ruling: builders' pytest `tmp_path` fixtures, the search builder's scratch files, W8's temp corpus, and this scratch script all write under the OS temp directory -- outside the workspace root as HND section 5 words it; this has been the practice since round 1 (tests build fixture indexes into `tmp_path`) and is read as "no write to any governed tree"; a temp-directory carve-out is an A2 candidate.

**Open items from the probe report (not halts):** W3's exact-order match against the oracle's recorded top-10 is a snapshot of a growing live corpus, not a standing guarantee (a future FAIL is a retrieval-stability finding to disposition, not a probe defect to relax); W2 / W9 / W10 verify whichever occurrence the live FTS returns against disk (this round the amendment file's line 1); KB3's rule is undesigned until lineage exists; W8's temp cleanup is best-effort on Windows; one run per production preset (no flake-detection repeat). **Probe budget: 3 of 4 rounds consumed; round 4 must carry Q3 (KB3, W5, W6) and FINAL; any repair round after that exceeds the module budget** (PROMPT.md: budget exhaustion is BOUNDED_FAIL, never a lowered bar).

**Round-2 boundary.** Builder rounds consumed **9 of 28** (corpus_adapter 2, probe 3, diagnostics 1, catalog 1, search 1, reader 1; lineage 0, web 0). Boundary commit **`6a8353705a2933cdd4696391da904ed3c680b398`** (`build_state.json`: probe round-3 record, integrator re-runs, budgets, session 4 close, `next_action` = wave 3). **Push record (O-1):** `git push origin main` at 2026-09-05T02:03:24Z -> `62685da..6a83537  main -> main` (fast-forward; the remote stood at the session-3 boundary); `origin/main` = `HEAD` = `6a8353705a2933cdd4696391da904ed3c680b398`; `ls-remote` agrees; `main...origin/main` 0/0; tree clean; 24 first-parent commits. To be recorded in `build_state.json` `workspace_pushes` at the next commit (structural note, K.6 step 7).

**Criterion-6 close pins (session 4).** RHACO `HEAD` = `origin/main` = `4032742f1fe87d056c60ffc05c8ae2eb2b81de0d`; commits since the section 0.6 pin `0243874`: `3b860bf` (A1 filing) and `4032742` (CCX filing) -- both Director-directed, no build commit; porcelain empty; no change under `docs\` beyond the filed pair. `rhaco\RHACO_corpus_index.py` `7548e47ebdcc24cdc0621208d06673542889c75b000780c650628b0678a5f098` unchanged. `C:\RHACO\index\corpus_index.db` `61ea99c98b3b26922e7dd306ff0dd06944f662cc57dc0044dc9c6dbbffd7a0c0`, 115,089,408 B, mtime 2026-09-04T23:58:11.272Z -- identical to the post-gate open pin (K.3); no `-journal` / `-wal` sidecar; no librarian reindex entry and no MCP census entry after the reconciled ones. Every build-side access to the live index this session (adapter, catalog, search, reader tests' live smokes; the probe's 14 production runs; the integrator's 5) was through the module's `connect()` behind the server or the adapter, and left the file byte-identical.

**D-12 application note (session 4).** Every strand carried the section 2 L text as amended verbatim first (dispatched inline, with the archived prompt of record read by sha); the processes launched were the workspace venv interpreter (ruff, pytest, fixture builders against tmp / fixture paths, the probe) and uvicorn as the probe's foreground child (once per probe run; W8 adds a second child on a free port, torn down by the same lifecycle) -- plus the S4-B7 scratch-launched uvicorn against the fixture, disclosed above. No `run_in_background`; no RHACO production process, device transport, or `C:\RHACO\data\` access by any strand or by the orchestrator. Production PIDs at close identical to item 14 (launcher 15268; logger 19516; sidecars 19544 / 19552 / 19588 / 20156 / 19308); `castle_event_log.csv` appending (4,577,460 B, mtime 2026-09-05T01:49:32Z at the 02:01:38Z close census).

**Workspace at close.** `main` = `origin/main` = `6a8353705a2933cdd4696391da904ed3c680b398`; tree clean; `.worktrees/` holds only `probe` (removed at the next session's re-entry or reused for round 4); branches `build/*` remain as history. `build_state.json` `next_action` names wave 3 (lineage, web), probe round 4 (Q3, FINAL), the L4 oracle re-run, the acceptance set, and the critic.

## L. Session 7 -- re-entry HALTED at section 2 M item 9 (identity); Director-directed Rule-2 push of the CCX-20260905-001 pair; criterion-6 reconciliation

Records written 2026-09-05T14:51:58Z per Director disposition 2026-09-05 item 3, from `working\S7_halt_records_DRAFT_HND-20260903-001.md` as drafted. [authored session 7 under `claude-fable-5-1`, identity halt open -- session-5 precedent: mechanical records, no judgment] Session record: D row 7. Ledger rows: P-16, P-17, P-18.

### L.1 Pre-findings note -- Director-directed Rule-2 commit and push of the CCX-20260905-001 pair (2026-09-05)

| Step | Result |
|---|---|
| Commit (prior session-7 attempt, crashed) | `fb81f3f96b20539af221febc89e23aa297e13e71` @ 13:48:26Z -- `RHACO-CCX-20260905-001: file build convergence session handoff` (62 chars, ASCII, one canonical id). Body carries both shas + bytes, the 13:46:49Z pre-commit census (1444, baseline=MATCH, warnings=5233), and the P-16/P-17 ledger note. Blob identity re-verified this session: `git rev-parse HEAD:<path>` = `git hash-object <path>` = `0af30ac8...`; sha256 `ebcdc8bd...` 20,507 B and card `2b5c858b...` 4,220 B as pinned. Authored under `claude-fable-5-1` (68/68 per-request fields, transcript fde11754...); a Director-directed filing act, not a build commit |
| Closeout gate (14:01Z-14:03Z; repeated 14:04Z-14:06Z to a file) | librarian PASS (1444 cards, missing=0, orphans=3, carded-with-parent=128, carded-with-code=0, broken=0, errors=0, baseline=MATCH, warnings=5233; **each run performed a full reindex of the live index** -- log 07:02:54 / 07:05:55 local) / **regression FAIL** (0 baselined, observed 2: `test_corpus_index_self_edges.py::test_self_edges_are_dangling_cites`, `test_required_claim_floor_v9.py::test_no_other_renderer_behaviour_changed_across_the_archived_corpus`) / **auditor FAIL** (MISMATCH +38/-0, unwaived_gating 349) / structure-guard PASS (13/13) / git-status-clean PASS / overall FAIL exit 1 -- the same two members red as at the session-2, -3 and -4 pushes |
| Attribution of the red to `fb81f3f` | regression: identical failure set to session 4 (INC-20260829-001, INC-20260830-001), delta 0 -- the commit touches `docs\reports\` only, no `tests\`, no `rhaco\`. auditor: +37 at `4032742` (K.1) -> +38, unwaived 348 -> 349; the one add is `anchor_not_found` `RHACO-CMP-20260815-002 M3` cited from the CCX's own section 7 table (line 108) -- the CCX's forward reference to a milestone anchor, same shape as the session-4 adds. No baseline write, no waiver edit, no `tests\` edit |
| Push (2026-09-05T14:14:32Z) | `4032742..fb81f3f  main -> main`; `origin/main` = `HEAD` = `ls-remote` = `fb81f3f...`; 0/0; porcelain empty. Pushed red under the CCX section 4.1 instruction, on the K.1 precedent; attributed, not accepted |

A Director-directed filing act, not a build commit: the criterion-6 close check reads `fb81f3f` as the CCX-20260905-001 filing (as `4032742` is the CCX-20260904-001 filing and `3b860bf` the A1 filing).

### L.2 Criterion-6 reconciliation (CCX-20260905-001 section 0 items 4-5)

| Pin | sha256 | Bytes | mtime (UTC) | Explained by |
|---|---|---|---|---|
| S4 post-gate open pin (K.3) | `61ea99c9...` | 115,089,408 | 2026-09-04T23:58:11.272Z | gate librarian member full reindex at `4032742` |
| At the session-7 launch, before any act | `53a48901...` | 115,089,408 | 2026-09-05T05:58:37Z | **chat-side MCP pair write of RHACO-CCX-20260905-001** (card `card_mtime_utc` 05:58:28.555Z; one new `cards` row, one `fts_docs` row; 1443 -> 1444). Log: **rotated** `docs\logs\mcp_validate.log.2026-09-04` line 3 `22:56:21,702` local (05:56:21Z) cards=1443 = pre-write census; line 4 `22:58:48,534` local (05:58:48Z) cards=1444 = post-write census, 11 s after the row |
| After this session's gate (**open pin from here**) | `ea8f7df2238a2643e664ced4619912984a6e506d4192e75c4035a407b5e262c7` | 115,081,216 | 2026-09-05T14:05:55.07Z | gate librarian member full reindex, twice: `catalog_librarian_validation.log` `2026-09-05 07:02:54,019 corpus index reindexed: 1444 cards, 2255 programs, 14745 tags, corpus_cards_sha=bf0d0a53ebf3` and `07:05:55,085` (same sha); `index_meta.last_full_reindex_utc` 2026-09-05T14:05:50.644Z; two pages smaller. Read-only check via `sqlite3` URI `mode=ro`, db sha unchanged before/after |

Read-only chat censuses, no db touch: rotated log line 2 `16:15:08,475` local (23:15:08Z) cards=1443 = the 23:14:59Z census (already reconciled at K.3); live `mcp_validate.log` line 1 `06:25:45,824` local (13:25:45Z) cards=1444 = the 13:25:27Z census (index mtime stayed 05:58:37Z through it). RHACO-side validate-only censuses with no reindex: 13:46:49Z (crashed session-7 pre-commit); the crashed session's gate at 13:49:00Z was killed by the client crash before its librarian member reindexed (index mtime unchanged at 14:00:42Z). No unexplained change; no criterion-6 halt.

RHACO `git log` since the section 0.6 pin `0243874`: `3b860bf` (A1 filing), `4032742` (CCX-0904 filing), `fb81f3f` (CCX-0905 filing) -- all Director-directed, no build commit; porcelain empty; `rhaco\RHACO_corpus_index.py` `7548e47e...` 118,123 B unchanged; librarian `22e1923a...` 131,038 B unchanged.

**1449 vs 1443 (CCX section 7 OPEN item): RECONCILED.** 1,450 `.card.yaml` files under `docs\` today (1,449 + this CCX's card); 6 of them sit in `docs\docs_archive\` (a librarian `DENY_DIR`, frozen partition) and are outside the census walk; 1,444 outside the deny set = the census. The Explorer's scan walked `docs_archive\`; the librarian does not.

### L.3 SA-3 = (b) -- on-disk importer census for the integrator (NOT executed; identity halt)

Director ruling (session-7 launch instruction): SA-3 = (b) -- integrator text edit to ARCHITECTURE 4.1 + the package docstring enumerating the three licensed importers; SA-4's L1 boundary check asserts that set. Targets: `ARCHITECTURE.md` line 50 heading "4.1 `corpus_adapter` (read-only; the only RHACO importer)"; `explorer/corpus_adapter/__init__.py` docstring "`adapter.py` is the only file in the workspace that imports either module." (The SEAL names the corpus_adapter package docstring, not `explorer/__init__.py`, which carries no importer claim.)

Importers of `RHACO_corpus_index` / `RHACO_tool_catalog_librarian` in the workspace (main, `.worktrees` excluded):

| File | Module(s) | Licence |
|---|---|---|
| `explorer/corpus_adapter/adapter.py:121, :159` | both | the adapter (4.1) |
| `explorer/diagnostics/service.py:85` | corpus_index (import only, `RERANK_MODEL_DIR` isdir) | O17 (R01 diagnostics dispatch) |
| `fixtures/build_fixture_index.py:57-58` | both | O3 (fixture builder; already L1-allowlisted for the write names) |
| `preflight/item12_import.py:13-14`, `preflight/item13_vec.py:15` | both / corpus_index | hand section 0 instruments (not build code) |
| `tests/probe/test_fixture_builder.py:34` | corpus_index | test of the O3 builder |
| `tools/l4_gold_oracle.py:131` | corpus_index | L4 instrument (AC-4) |

Consequence for SA-4: "exactly the three licensed importers" is true only within a declared scope (`explorer/**` + `fixtures/**`); an L1 import-boundary check needs either that scope clause or an allowlist naming the four instrument/test importers above. Separately (SA-3 second half): `explorer/diagnostics/service.py:83` uses `sys.path.insert(0, entry)` where `corpus_adapter/paths.py:37` and S2 require `append` -- a code change in a builder-owned module (diagnostics: 1 of 4 rounds consumed), not a text edit; the (b) ruling does not cover it.

### L.4 `build_state.json` and workspace facts at the session-7 open (convergence step (4) inputs; section 2 N)

- `next_action` still read "Next: CRITIC round 1 ..." -- critic round 1 was filed at `b9c41c2` and the seal released; the CCX section 0 item 4 claim that `next_action` names the convergence sequence did not hold on disk. The convergence sequence existed only in the session-6 report of 05:41:54Z and CCX section 4. [Re-pointed to the session-8 re-entry in the session-7 records commit; the prior text is kept in `next_action_prior`.]
- `dispatch_records` (34) end at round 3 (S6-B8, S6-B9, S6-C3); no records for reader round 2 (R05) or the critic (S6-CR1); `budgets.builder_rounds_consumed_total` = 12 (reader round 2 not counted; should be 13; `per_module_consumed.reader` 1 -> 2); `sessions[5]`/`[6].closed_utc` null; `rounds` end at round 2. These are the critic's ranked issue 8 / overstated claims, still open -- convergence step (4) for the pinned orchestrator, not touched by the session-7 records.
- Lineage round 2 and adapter round 4 were NOT dispatched: no `R07_*`/`R08_*` prompt under `docs/rounds/`, `build/lineage` (`386afba`) and `build/corpus_adapter` (`7c1e9f4`) have 0 commits ahead of main, both worktrees clean. The CCX header's "dispatched-or-about-to-be" resolves to "about to be" (P-18).
- Workspace `main` `c861680` was **38 commits ahead of `origin/main` `f25ca27`** (the session-5 halt commit) at the session-7 open. The O-1 push at the session-6 boundary did not happen; the last recorded push is session 5. Not pushed this session (Director: "do not push yet"); 39 ahead after the records commit.
- SCOPE row 16 not located by a `| 16 |` grep (table layout differs); `explorer/search/service.py:93` still carries the bare `standing="ACCEPTED -- the operational default. Recall@10 0.700 aggregate on Gold v1.1."` (search round 2 target). `ARCHITECTURE.md:111` still names `last_run.json` (SA-5).

## M. Session 8 -- re-entry under the restored MOD-1 pin; reconciliation, O-1 push, SA-3 (b), round 6 dispatched

Section record: D row 8. Ledger rows: P-19, P-20. Orchestrator `claude-opus-5[1m]` / `xhigh`, the MOD-1 identity of record, in force and verified before any act.

### M.1 Section 0 re-entry (section 2 M set: items 1, 5, 6, 7, 8, 9, 14) -- PASS in full, no halt

| Item | Result |
|---|---|
| 1 charter | `RHACO-CMP-20260903-001` sha256 `c7104f1a37c53528...` 15,216 B -- matches the CCX-20260905-001 section 0 item 3 pin. Card `lifecycle_state: ACTIVE` |
| 3 pins (CCX item 3 set) | HND `ed01fa5412238767...` 23,306 B; Amendment A1 `167771c75575ceef...` 9,698 B; Guide v1.3 `fdd1c5f68ecfbd3b...` 19,842 B -- all three match |
| 5 substrate | `RHACO_corpus_index.py` `7548e47ebdcc24cd...` 118,123 B; `RHACO_tool_catalog_librarian.py` `22e1923a131bc453...` 131,038 B, `LIBRARIAN_VERSION` 1.19; gold sets `f20bc6a2...` 5,381 B and `d456f981...` 6,727 B. **Live db `ea8f7df2238a2643e664ced4619912984a6e506d4192e75c4035a407b5e262c7`, 115,081,216 B, mtime 2026-09-05T14:05:55.07Z -- byte-for-byte the session-7 open pin. Criterion 6 needed no reconciliation: the index has not moved since session 7 closed** |
| 6 RHACO repo | `main` = `origin/main` = `fb81f3f96b20539af221febc89e23aa297e13e71`; `git status --porcelain` empty. Since the section 0.6 pin `0243874`: `3b860bf` (A1 filing), `4032742` (CCX-0904 filing), `fb81f3f` (CCX-0905 filing) -- all Director-directed, no build commit |
| 7 workspace | `C:\highsierralabs\RHACO_Corpus_Explorer` present, private `origin` under `highsierralabs`, tree clean, `main` at `bb1727d` (the session-7 records commit), 39 ahead of `origin/main` `f25ca27` |
| 8 prompt/rationale | `PROMPT.md` `dd75dd2e...` 28,665 B; `RATIONALE.md` `3070c961...` 26,151 B; `DISPATCH_PARAMETERS.md` `f6e9cd3e...` 9,730 B -- all match the `instruments` pins. Line 1 and last non-empty line as specified |
| 9 identity | **PASS -- see M.2. No halt.** `claude --version` 2.1.260; settings sha `06f37244...` 9,097 B byte-identical to the session-7 pin of record; system prompt names `claude-opus-5[1m]` verbatim; env `CLAUDE_EFFORT=xhigh`, `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1`, `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=3` |
| 14 device safety | logger PID 19516 (`pythonw`), launcher 15268, sidecars: health_monitor 19308, spaceweather 19544, castle_pressure 19552, castle_contamination 19588, radon_crm 20156. One instance each; all live; untouched for the whole session |

### M.2 Item 9 -- the halt condition as instructed was unsatisfiable, and the instrument said so

The session-8 launch instruction read: *"every per-request `message.model` must read `claude-opus-5[1m]`, effort xhigh -- any other value is a halt."* The observed value was `claude-opus-5`. Under the instruction's letter that is a halt.

It is not one, and the reason is a finding rather than an exception. **The per-request `message.model` field strips the `[1m]` context-window suffix.** Two controls establish it, neither of them this session's own reading:

1. **Session 6** -- the session in which MOD-1 was declared and verified in force, under `~/.claude/settings.json` `model: "claude-opus-5[1m]"` (sha `831dbfb9...`), with the system prompt reading *"The exact model ID is claude-opus-5[1m]"* verbatim -- logged **`claude-opus-5`** in **all 255** of its per-request fields (transcript `918204e5-8e7a-4c4c-a4ea-0e647796b497.jsonl`, slice from 03:09:23Z).
2. **Session 7** -- under `model: "claude-fable-5-1[1m]"` (sha `70c8f58a...`) -- logged **`claude-fable-5-1`** in **all 173** of its per-request fields (114 in `318cf70c...`, 59 in `fde11754...`).

No session under any `[1m]` pin has ever produced a `[1m]`-suffixed string in that field. The instruction as worded would therefore halt every conformant session, including the one that ratified MOD-1.

Applied rule (CLAUDE.md section 17 rule 8, suspect the instrument before the artifact): the identity of record is the **conjunction** of three surfaces, none of which is sufficient alone --

- `settings.json` sha + `model` key (the only surface carrying the context-window suffix);
- the system-prompt sentence (regenerated **only** at a client restart -- P-9 / P-10);
- the per-request `message.model` field, read as a **family-and-generation** check, with the suffix supplied by settings.

This **refines P-10's template item (a)**, which had reduced the check to the per-request field alone precisely because the system prompt goes stale. Both surfaces are individually insufficient, in opposite ways: the system prompt is stale-but-precise, the per-request field is live-but-truncated. Ledger row P-19.

### M.3 Session acts, in order

| # | Act | Result |
|---|---|---|
| 1 | Section 0 re-entry (M.1) | PASS, no halt |
| 2 | `build_state.json` reconciliation -- CCX section 4 step (4) | Workspace `4e56ca3`. 361 insertions / 26 deletions. Three missing dispatch records rebuilt from primary evidence; budgets 12 -> **14**; rounds 4 and 5 added; sessions 5 and 6 closed; gate 7 withdrawn to REPAIRED-BUT-UNEVIDENCED; all ten critic ranked issues dispositioned item by item; the post-fix card-agreement run recorded; GD-1 filed. See M.4 |
| 3 | **O-1 push (the late session-6 boundary push)** | `f25ca27..4e56ca3  main -> main`, 40 commits, fast-forward. `HEAD` = `origin/main` = `git ls-remote` = `4e56ca3c0f955605d1998e30f3f2f1a8aa2ad0e9`; `main...origin/main` 0/0; porcelain empty. **The session-6 boundary push never happened and the session-5 one was the last of record; this discharges it.** `git push --ff-only` is not a valid push flag -- plain `git push` already refuses a non-fast-forward, which is the O-1 requirement |
| 4 | Two stale worktrees removed | `.worktrees/lineage` (`386afba`) and `.worktrees/corpus_adapter` (`7c1e9f4`), both verified 0 commits ahead of `main` and clean before removal, so nothing was lost. `git worktree prune` run |
| 5 | **SA-3 remedy (b)** | Workspace `f29bcc9`. See M.5 |
| 6 | Three R07 prompts authored and archived | Workspace `f8c3287`, corrected at `3e7521b`. See M.6 |
| 7 | Three worktrees created from current `main` | `.worktrees/lineage`, `.worktrees/corpus_adapter`, `.worktrees/diagnostics`, each on its own branch fast-forwarded to `main`, all three at `3e7521b`, all clean. `build/diagnostics` had had no worktree since round 1 |
| 8 | **Round 6 dispatched -- three concurrent strands** | See M.7 |

### M.4 What the reconciliation found that the critic could not see

Critic round 1's ranked issue 8 said `build_state.json` was "two builder rounds stale". It was, and the arithmetic correcting it was itself wrong: the session-7 `next_action` proposed "budgets 12 -> 13 with reader 1 -> 2", missing probe round 4 as well. The observed total is **14** (corpus_adapter 3 + probe 4 + diagnostics 1 + catalog 1 + search 1 + reader 2 + lineage 1 + web 1). That is a second instance of the P-6 class -- a predicted count stated as an observed one -- and it is recorded in `budgets.arithmetic_correction_of_record` rather than silently fixed, because the M3 ANL counts instances.

Three strands had executed with **no `dispatch_records` row at all**: S6-B10 (probe round 4), S6-B11 (reader round 2), S6-CR1 (critic round 1). Section 2 I makes a row missing any of its seven governance fields a governance defect the closeout must report; a row missing entirely is the stronger form. Filed as **GD-1**.

Every reconstructed field came from primary evidence and none was estimated: orchestrator `dispatched_utc` / `returned_utc` from the session-6 transcript's `tool_use` and `task-notification` timestamps; strand `t_start_utc` / `t_end_utc`, `head_commit`, gates, deviations and agent ids from the persisted workflow run records `wf_977e0550-579.json`, `wf_cee90f83-a60.json` and `wf_12116ea4-c3e.json`; commits and merge shas from `git log`. Each row carries a `record_provenance` field naming its sources. **The evidence was never missing -- only the ledger entry was**, which is worth recording as its own observation: the defect was recoverable exactly because the client persists workflow run records and transcripts that the build's own bookkeeping does not depend on.

### M.5 SA-3 remedy (b) -- and the drift was wider than the seal named

The seal named two claim sites (ARCHITECTURE 4.1 and the `corpus_adapter` package docstring) and the session-7 census confirmed those two. Re-deriving the census on `f29bcc9` found the *claim* in **seven places across four files**, while the *importer census itself* matched L.3 exactly:

| Claim site | Owner | Disposition |
|---|---|---|
| `ARCHITECTURE.md:14` (module table) | integrator | corrected now |
| `ARCHITECTURE.md` 4.1 heading | integrator | corrected now, with a licensed-importer table added |
| `CONSTRAINTS.md` S2 evidence cell | integrator | corrected now |
| `explorer/corpus_adapter/__init__.py:3` | corpus_adapter (the seal's named target) | corrected now |
| `explorer/corpus_adapter/adapter.py:2` | corpus_adapter | **deferred to adapter round 4, batch item 4** |
| `explorer/corpus_adapter/availability.py:23` | corpus_adapter | deferred, same |
| `explorer/corpus_adapter/sql.py:9` | corpus_adapter | deferred, same |

The invariant is now stated as an **enumeration within a declared scope** (`explorer/**` + `fixtures/**`), which is the only form a static check can assert -- that is the SA-4 lesson applied at the point of writing rather than after.

**Deviation from the launch instruction, surfaced not absorbed.** The instruction said the SA-4 boundary check "allowlists `preflight/` and `tests/` by path as instruments". The on-disk census carries a fourth instrument importer, `tools/l4_gold_oracle.py:131` -- the L4 gold oracle that produces the AC-4 evidence. Allowlisting only `preflight/` and `tests/` would make the check fail on its own first run against the build's own acceptance instrument. `tools/` was therefore included in the SA-4 shape as first recorded, and the difference stated for Director disposition rather than applied silently. **Ruled, and narrowed against this build's proposal -- see M.8 item 2.**

Gates after the edit: ruff clean; `l1-index-write-check` hits=6 allowlisted=6 defects=0 PASS; pytest **262 passed**.

### M.6 The three R07 prompts, and the P-11 control run for real

| Stem | Strand | Round | sha256 | Bytes |
|---|---|---|---|---|
| `R07_lineage` | S8-B12 | lineage round 2 of 4 | `c53f2f9c2ed508cbfa30c2d589cb9553ef0ce8c118de82bab7efcac3312fafe2` | 15,142 |
| `R07_corpus_adapter` | S8-C4 | corpus_adapter round **4 of 4, the last** | `c0ce0841192189811a3332ab38c2d587b486c3233ac9dd3adab1fcdb26668f3b` | 14,810 |
| `R07_diagnostics` | S8-B13 | diagnostics round 2 of 4 | `5bf9dc7eb636f660f5ab889900e292d4c9c69591c3c5f76311feb0b4908be84c` | 11,563 |

**P-11 control, executed before dispatch and not merely asserted.** The D-12 + D-3 prefix each prompt opens with was **byte-copied** from `R03_corpus_adapter.prompt.md` rather than retyped, and that archive's prefix was first proved identical across `R03_corpus_adapter`, `R03_lineage`, `R03_web`, `R04_probe` and `R05_reader` (1,111 chars, sha256 `6a893dbb94c0207fb84727035a5908aaec3df809ae19defe4be07e53fbc175a3`). The construction the dispatch script builds at runtime was then replicated character-for-character in Python and diffed against all three new archives: **byte-identical to each**. The control was re-run after a later edit to the prompts and passed again.

**P-11 itself is now visible in the artifact.** The persisted round-3 script builds `workspace\u2019s` with a typographic apostrophe where every archive has U+0027. That is the wave-3 deviation of record, still readable in the script the client saved, and the round-4 script shows the corrected `\'`. The control was built for exactly this class and this is the first round in which it was exercised on a prompt written from scratch.

**A second authoring defect, caught by the control's neighbourhood rather than by the control.** The prompts first named a literal base commit (`f29bcc9`) while the worktrees would sit at the commit that *added the prompts themselves* -- a prompt cannot name its own commit. Each strand's first git act is a `rev-parse` of its own HEAD, so a conscientious strand would have gone to BLOCKED on a bookkeeping artifact. Corrected at `3e7521b`: the strand now RECORDS what `rev-parse` returns and verifies instead that its HEAD *contains* `f29bcc9` (`git merge-base --is-ancestor`), which is a check that can actually be run. Same class as P-15 (round-specific literals in generated prompt text); recorded as P-20's second half.

### M.7 Round 6 dispatch -- three concurrent strands, the A1.3 cap

Dispatched 2026-09-05T15:27:5xZ from `3e7521b`. Workflow run `wf_b0d40995-3c0`, task `w92gupckt`. One `parallel()` call, exactly three strands, no other strand live -- concurrency exceedance: none. Dispatch form: the section 2 L D-12 + D-3 blocks verbatim (byte-diffed, M.6) plus a sha-pinned pointer to the archived prompt, which each strand verifies by `Get-FileHash` before acting. Strand tier mid, `claude-sonnet-5`, effort `xhigh` explicit, `agent_type` general-purpose write-scoped to its own worktree; orchestrator `claude-opus-5[1m]`.

| Strand | Module round | Scope |
|---|---|---|
| S8-B12 | lineage 2 of 4 | Render every typed edge with the sentence true from the end the reader stands on, consuming the `models.relation_sentence` contract landed at `c861680`. Closes critic gate 7 on the surface that produced it, plus the lineage half of ranked issue 7. Tests must assert the RENDERED SENTENCE and derive the expectation from the fact, never from `RELATION_LABELS` -- the tautology that let the defect survive. Told explicitly that W5 will now FAIL on incoming edges, that this is an accepted BOUNDED_FAIL, and that a shim to keep W5 green is forbidden |
| S8-C4 | corpus_adapter 4 of 4 (**last**) | One batched round, four items in the Director's ruled priority order: SA-1, SA-2, the deferred amends click-through change request, then the three remaining sole-importer docstrings. Per-item `landed \| not landed` returned through the schema; unlanded is BOUNDED_FAIL; weakening an item to fit or reordering the priorities is not acceptable |
| S8-B13 | diagnostics 2 of 4 | The `sys.path.insert(0, ...)` vs S2 `append` conformance fix ONLY, with a test that asserts ORDER rather than membership (a membership test passes on the defective code). Ranked issue 10 and SA-5 named as explicitly out of scope so the narrow round cannot drift |

Round 6 consumes 3 builder rounds: 14 -> 17 of 28 at merge.

### M.8 Director rulings on the dispatch-boundary report (2026-09-05)

**Item 1 -- P-19 ratified.** The item-9 identity of record is the conjunction of the `settings.json` sha plus its `model` key, the system-prompt sentence, and the per-request `message.model` field. A2 line at terminal. Refines P-10 template item (a). Standing consequence: a halt predicate written against a `[1m]`-suffixed per-request value is unsatisfiable by construction and must not be used at any future re-entry.

**Item 2 -- SA-4 allowlist NARROWED, against this build's own proposal.** The build had proposed licensing `tools/` by path, on the reasoning that the AC-4 gold oracle would otherwise be a false defect on the check's first run. The ruled set is narrower and better:

| Allowlisted | Granularity | Licence |
|---|---|---|
| `preflight/` | by path | hand section 0 items 12/13 |
| `tests/` | by path | test code (`tests/probe/test_fixture_builder.py`) |
| `tools/l4_gold_oracle.py` | **by file** | AC-4 design: the oracle must call the module directly, because comparing the adapter against the module *is* the measurement |

`tools/` is **not** licensed as a directory. `tools/probe_corpus_explorer.py` lives there, and the probe is barred from importing either RHACO module and from opening the database at all -- it drives the product through a browser and compares against fixture sources on disk (the SA-6 property, and the reason a probe of this build can fail the adapter). A directory-level allowlist would make a probe that began importing the module, or opening the live index, **invisible to the very check written to catch that class.**

This is worth recording as more than a scoping detail. The build's proposal was reasoned and wrong in a specific way: it generalised from the one instrument it had found to the directory containing it, which is the same move -- an invariant stated more loosely than the facts require -- that produced SA-3 in the first place. The correction keeps the oracle licensed and the directory closed.

Integrator edit, no builder round. Applied to `ARCHITECTURE.md` 4.1 and recorded explicitly in `build_state.json` `sealed_audit_release.remedy_decisions.SA-4`, so the licensed set is a governance record rather than a literal buried in the check's source. **Deferred deliberately:** `explorer/corpus_adapter/__init__.py` also names `tools/` in its docstring, and that file is corpus_adapter-owned with S8-C4 in flight against that package; the one-line narrowing is held for the integrator's post-merge revalidation rather than raced against a live strand. The SA-4 check itself is built at CCX section 4 step (3), as scheduled.

**Item 3 -- GD-1 stands as a criterion-5 governance defect.** Anchors confirmed on disk: HND section 8 item 6 ("Every dispatch in `build_state.json` carries the seven governance fields ... (CMP criterion 5)"); CMP-20260903-001 criterion 5; and Guide v1.3 section 8, the fresh-instance principle -- *"the durable record is only what was written to disk ... Design every dispatch as if the only reader of its outcome will be a stranger reading the disk."*

GD-1 is now recorded as **REPAIRED, NOT CLOSED**. The reconstruction was possible only because the client persists transcripts and workflow run records that the build's own record does not depend on and that a fresh instance is not entitled to assume exist. Recovery was possible; it was not guaranteed. It is carried into the terminal closeout as a defect with the repair beside it, and flagged for the M3 ANL as **the second session-boundary record loss** -- the pattern being that a session ending without writing its own boundary records leaves a ledger disagreeing with its artifacts, and that both instances were found by a *later session*, never by a gate.

Numbering note, recorded as a reading and not as the Director's words: the ruling says "second" and does not name the first. The earliest instance of the same class on this record is the session-3 boundary, whose commit `62685da` carried a null `closed_utc` and was reconciled at the session-4 boundary.

### M.9 Rounds 6-8 -- integration, the two instruments, and the convergence rounds

**Round 6 (three concurrent strands) -- merged, integrated, closed.** S8-B12 lineage r2, S8-C4 corpus_adapter r4, S8-B13 diagnostics r2. All three COMPLETE. Merged lineage -> diagnostics -> corpus_adapter, `--no-ff`, with the full gate between each (269, 271, 275). Budgets 14 -> 17. **corpus_adapter landed all four batch items in the ruled priority order, so the module is exhausted with no BOUNDED_FAIL of its own** -- the batched-round design the boundary-3 HOLD reserved worked exactly as intended.

The one gate failure was foreseen and correctly handled: S8-C4's `mode_effective` token change broke `tests/search`, and the strand kept the failure, filed change request 4, and disclosed the rule-5(b) deviation rather than editing an out-of-scope file. On inspection one of its two consumers was **not** a stale assertion: `search.html` matched the vocabulary with exact token tuples, so under the new token BOTH evidence disclosures would have silently vanished from degraded graph results. A disclosure that disappears exactly when the mode degrades is the worst failure mode it has. Repaired at `dcb0443` by prefix matching so the check fails open (P-13 second instance, ruled and recorded).

**Semantic integration, checked rather than assumed** (A20 / P-3). Three merged changes met on one surface no single strand saw: S8-B12 suppressed the O7/O8 self-pointing amends edge in the VIEW against the pre-merge adapter, while S8-C4 changed `edges_for` so that same edge's `from_id` now resolves. On the live corpus after the merge: the same `amends` edge reads "amended by" from the parent and "amends" from the amendment; nothing double-bucketed; the amendment is now LINKED from the parent page (D-Q12); zero self-links on either end; and the supersession HEAD -- the critic's decisive derived case -- no longer reads "is superseded by" in its Incoming section. Live db `ea8f7df2...` unchanged throughout.

**Step (3) -- the two integrator instruments** (`9cfde44`), built while search round 2 was in flight, neither touching a strand's path.

*SA-4 import-boundary check*, added to `tools/l1_index_write_check.py`. The census showed the Director's narrowing was not merely safer but exactly tight: `preflight/` + `tests/` by path alone would have flagged the AC-4 gold oracle, which is why it is licensed BY FILE; and no other file under `tools/` imports either module, so licensing the directory would have been strictly looser than the facts while hiding the one case that matters -- the probe lives there and is barred from importing at all. First run: 10 hits, 10 licensed, 0 defects, 0 unused licences.

*L4 direction oracle* (`tools/l4_direction_oracle.py` v1.0), gate 7's compensating control for BF-1. Its expectations are hand-written from CONSTRAINTS O8 and deliberately NOT imported from `explorer.models`, with a committed test asserting that independence -- an oracle that read the product's own table could not disagree with the product, which is how the defect survived. First live run: 213 cards, 213 pages, **5,052 edges, 93 supersession chain heads**, 0 defects, db unchanged, PASS. **Gate 7 is therefore PASS on evidence rather than assertion** -- the build's own false PASS, the critic's only FAIL, and the one gate whose instrument had to be built before it could honestly be declared.

**Two defects in that oracle, caught on its own first runs, recorded because they are the class it exists to end.** It reported PASS having rendered ZERO pages -- `cards.yaml_path` is absolute where the URL key is the docs-root-relative `card_ref`, and `PAGE_ERROR` had been excluded from the defect count. Then, fixed, its supersession-head check was still dead code, looking for a `chain_head` key the API does not expose (the field is `chain`, a list of `{card, is_head}`), so check 3 had never once fired. Both fixed; an empty-run guard now fails any run that renders no page, compares no edge, **or exercises no chain head**, and the sampling gained a second stratum because the recency stratum alone returned zero heads. Both instruments now carry tests that BUILD violations and assert they are caught -- 15 cases between them. A control never observed to fail is worth nothing, and these two had already proved that about themselves.

**Round 7 (two strands) -- search r2 and catalog r2, both merged.** Budgets 17 -> 19. Never more than two live against the cap of 3.

*S8-B14 search* answered its item 1 better than the dispatch required: all four bare Gold v1.1 figures ATTRIBUTED AND DATED rather than removed, behind shared `GOLD_V1_1_SOURCE` / `GOLD_V1_1_EVAL_DATE` constants so a future figure inherits attribution instead of re-acquiring the defect -- plus `HYBRID_CURRENT_MEASURE`, so the page states the July 0.700 AND this build's own contradicting 0.675, each with source and date. A reader sees the disagreement rather than one side of it. It found a FIFTH undated figure in diagnostics and correctly filed rather than edited it (O-4), so **S8 is satisfied on the search page and NOT yet product-wide**, recorded that way. Before asserting "identifier mode cannot degrade" on a user-facing page it read the adapter and confirmed it, calling it in the source "a checked claim, not an assumption" -- the P-17 discipline, applied unprompted while writing new prose about behaviour.

*S8-B15 catalog* landed all four items. Its class audit came back **NEGATIVE**, which is worth as much as a positive: `show_lifecycle_control` was the only per-filter visibility gate in its module, and it disclosed that it could not audit the concurrently-edited search module rather than presuming. The integrator closed that gap after the search merge -- search has no `show_*` gates and no conditional around any filter control. So the round-6 lesson (a finding in one place is usually a population) **does not** apply here: this one is genuinely isolated across both filtering surfaces, recorded as a checked negative.

*Item-3 verification upgrade, integrator, at zero budget cost.* The catalog strand had no browser and said so, resting its column-budget fix on a CSS argument and labelling the pixel outcome UNVERIFIED. RUNNING the probe consumes no builder round, so W4 -- the preset that exists to make the CMP lifecycle column visible -- was re-run at the merge commit (`20260905T164214Z`, PRODUCT_EVIDENCE_PASS): **all nine columns render inside the 1280px viewport, CMP lifecycle state among them with real values**, page height 9,619 -> 7,219 px. Recorded with its limit: a rendered capture, not the `scrollWidth <= clientWidth` assertion the critic's round-2 question asks for, which would need a probe change the budget cannot buy.

**Director rulings of the round-6 report, all discharged.** (1) Catalog concurrent with search, web alone after both, ceiling 20 of 28. (2) The `search.html` prefix-match edit accepted as disclosed and recorded as a P-13 second instance, with both verification conditions met -- base `26816db` confirmed to contain `dcb0443` before merging, and the SA-1 DISABLE_VEC live check re-run after (`graph-degraded-semantic-seed`, both disclosures present, no false "Graph unavailable", db unchanged). (3) **BF-2 determined ADAPTER-SIDE** by reading the code -- `adapter.py:569-572`, two independent `if`s over the build's own workspace code on rows from its own `Q7_EDGES_FOR`, no RHACO module code involved -- so it does NOT join the section 7 queue beside O29 and stands as recorded.

**Round 8 (in flight at the time of writing): S8-B16 web round 2, alone**, the twentieth round of the ceiling of twenty. Alone by structure rather than scheduling: its O-2 fix deletes the duplicated `#keyhelp` block from all five page templates, the single cross-module edit it is licensed to make, scoped file by file per the boundary-3 disposition. The dispatch declares up front what the round cannot do -- close the critic's keyboard blind spot, since executing a key needs a browser and the probe is exhausted -- and requires the strand to say so rather than produce a test that appears to exercise a key and does not.

### M.10 Terminal state and the section 2 N closeout obligations

**PROMPT.md section 14 step 20 is one sentence -- "Produce an evidence-backed closeout." -- and it is copied here verbatim with the declared state, per hand section 2 N.**

## TERMINAL STATE: BOUNDED_FAIL

Declared 2026-09-05 at the convergence commit `fe4e053` (product-identical through `d17b901` but for the P-13(a) date repair and its control), on the Director's ruling, at RHACO-CCX-20260905-001 section 4 step (7).

**Why not PASS.** PROMPT.md section 12 permits PASS only if, among other things, all objective gates pass, production-path probes pass, and the critic score reaches 8.5 *if the critic scale was validly usable*. All nine gates pass, independently re-assessed by critic round 2 with no disagreement. The critic scale was **not** validly usable, so that clause is vacuous and licenses nothing -- recorded explicitly so nobody reads the `ref_score` of 8 as the cause of this state. **What is dispositive is unconditional: production-path probes must pass, and they do not** -- 11 of 12, W5 red. Section 12 offers no category for a required bar unmet for a well-understood reason; it offers exactly one, BOUNDED_FAIL, and both its conditions hold (probe budget exhausted, and critic round 2 determined NOT CONVERGED). Declaring PASS would require reading the set as 12 of 12 on the ground that the red preset is wrong -- the reading the BF-1 ruling forbids, because a build that can retire a failing acceptance check by declaring the check wrong has no acceptance gate at all.

**The six section-12 fields** are recorded in full in `build_state.json` `terminal_state.prompt_section_12_required_fields`. In brief: every required section-5 function exists and works on the production path; W5 is the single failed workflow; best observed quality is 8/10 ordinal from an uncalibrated scale; **the limitation lies in MEASUREMENT**, not UI, substrate, architecture or dependency; six unresolved alternative explanations are listed; and the next discriminating action is the critic's own three, the first two costing no builder round.

### M.10.1 Criterion-6 close pins -- CLEAN

| Check | Result |
|---|---|
| RHACO `git log` since the section 0.6 pin `0243874` | `3b860bf` (A1 filing), `4032742` (CCX-0904 filing), `fb81f3f` (CCX-0905 filing). **All three Director-directed document filings. NO BUILD COMMIT.** |
| RHACO `git status --porcelain` | empty -- no file under `docs\` created or modified by the build |
| `rhaco\RHACO_corpus_index.py` | `7548e47ebdcc24cd...` 118,123 B -- **unchanged** open-to-close |
| `tools\RHACO_tool_catalog_librarian.py` | `22e1923a131bc453...` 131,038 B -- unchanged |
| `C:\RHACO\index\corpus_index.db` | `ea8f7df2238a2643e664ced4619912984a6e506d4192e75c4035a407b5e262c7`, 115,081,216 B, mtime 2026-09-05T14:05:55.07Z -- **byte-identical to the session-8 open pin.** No change at all across the session, so no reconciliation against the librarian logs is required |

The build wrote nothing to the RHACO tree. Every oracle, probe run and integrator check of this session opened the live index read-only and re-hashed it before and after; the hash never moved.

### M.10.2 D-12 application note

The section 2 L D-12 block narrowed D-12(a) from "no process" to "the workspace interpreter only", which is the honest form for a software build on the observatory host. It was carried verbatim at the top of every strand prompt in this session (five builder strands and one critic), byte-diffed against the archive before each dispatch under the P-11 control. No strand launched a `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*` or `RHACO_tool_*` process, opened any device on any transport, or read or wrote anything under `C:\RHACO\data\`. Device safety was re-confirmed at session open (logger PID 19516, launcher 15268, five sidecars) and the production stack was untouched throughout. One strand-conduct deviation of record remains from session 4 (S4-B7's ad hoc launcher use, disclosed rather than halted); none arose this session.

### M.10.3 Pattern #3 disposition evidence pointer

The evidence for the Guide's #3 question (worktree-isolated parallel builders) is in `build_state.json` `harness_evidence` HE-1 through HE-3 and in the per-round `dispatch_records`. The load-bearing entry is **HE-3**, recorded with its counterweight rather than as a success: two strands blind to each other converged correctly on one degenerate case (S8-C4 filed CR-6 predicting a self-link its own change would create; S8-B12 had already suppressed it), **and** the same wave produced a cross-module regression no strand could see, caught only by integrator revalidation. Isolation prevented the conflict and also prevented the detection. Zero concurrency exceedances under A1.3 across sessions 3-8; the three on record are the deliberate session-2 cap probes.

### M.10.4 GD-1 -- a criterion-5 governance defect, reported not softened

Three strands (S6-B10 probe round 4, S6-B11 reader round 2, S6-CR1 critic round 1) executed with **no `dispatch_records` row at all**. CMP-20260903-001 criterion 5 and hand section 8 item 6 require every dispatch to carry the seven governance fields; a row missing entirely is the stronger form of that failure. Guide v1.3 section 8 states the principle it violates: *"the durable record is only what was written to disk."*

Recorded **REPAIRED, NOT CLOSED**, on the Director's ruling. The rows were reconstructed from primary evidence and each carries `record_provenance` naming its sources, but the reconstruction was possible only because the client persists transcripts and workflow run records that the build's own record does not depend on and that a fresh instance is not entitled to assume exist. Recovery was possible; it was not guaranteed. Flagged for the M3 ANL as the second session-boundary record loss.

The remedy was applied prospectively for the rest of the session: every dispatch from round 6 onward was written to `build_state.json` **before** its strand returned.

### M.10.5 The full dispatch record table (hand section 2 N)

Session-2 strands are tabulated in section E and omitted here. Every row below carries the seven governance fields.

| Strand | Round | Stem | Module round | Model | Effort | Pattern | Agent type | Execution | Escalation | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| S3-I1 | 0 | R00_I1_corpus_index_contracts | - | claude-sonnet-5 | xhigh | #7 | Workflow agent() / Explore | concurrent | none | - |
| S3-I2 | 0 | R00_I2_card_schema_fields | - | claude-sonnet-5 | xhigh | #7 | Workflow agent() / Explore | concurrent | none | - |
| S3-I3 | 0 | R00_I3_retrieval_dispositions | - | claude-sonnet-5 | xhigh | #7 | Workflow agent() / Explore | concurrent | none | - |
| S3-I4 | 0 | R00_I4_corpus_conventions | - | claude-sonnet-5 | xhigh | #7 | Workflow agent() / Explore | sequential | none | - |
| S3-B1 | 1 | R01_corpus_adapter | - | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S3-B2 | 1 | R01_probe | - | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S3-B3 | 1 | R01_diagnostics | - | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S4-C1 | 1C | R01C_probe | probe round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S4-C2 | 1C | R01C_corpus_adapter | corpus_adapter round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S4-B4 | 2 | R02_catalog | catalog round 1 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S4-B5 | 2 | R02_search | search round 1 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S4-B6 | 2 | R02_reader | reader round 1 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S4-B7 | 2 | R02_probe | probe round 3 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | sequential | none | COMPLETE |
| S6-B8 | 3 | R03_lineage | lineage round 1 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S6-B9 | 3 | R03_web | web round 1 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S6-C3 | 3 | R03_corpus_adapter | corpus_adapter round 3 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S6-B10 | 4 | R04_probe | probe round 4 of 4 -- the LAST round thi | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | sequential | none | COMPLETE |
| S6-B11 | 5 | R05_reader | reader round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | sequential | none | COMPLETE |
| S6-CR1 | 5 | R06_critic | critic round 1 (convergence window = 2 r | claude-opus-5[1m] | xhigh | judgment tier, read-only rev | Workflow agent() / Explore | sequential | none | COMPLETE |
| S8-B12 | 6 | R07_lineage | lineage round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S8-C4 | 6 | R07_corpus_adapter | corpus_adapter round 4 of 4 (the last) | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S8-B13 | 6 | R07_diagnostics | diagnostics round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S8-B14 | 7 | R08_search | search round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | sequential | none | COMPLETE |
| S8-B15 | 7 | R09_catalog | catalog round 2 of 4 | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | concurrent | none | COMPLETE |
| S8-B16 | 8 | R10_web | web round 2 of 4 -- the TWENTIETH builde | claude-sonnet-5 | xhigh | #3 candidate (worktree-isola | Workflow agent() / general-purpose | sequential, ALONE -- struc | none | COMPLETE |
| S8-CR2 | 9 | R11_critic | critic round 2 of the 2-round convergenc | claude-opus-5[1m] | xhigh | judgment tier, read-only rev | Workflow agent() / Explore | sequential | none | COMPLETE |

Governance-field conformance: every row from session 3 onward carries `role`, `model` (client-reported), `effort`, `pattern_primary`, `agent_type`, `execution_mode` and `tier_escalation`, plus `dispatched_utc` / `returned_utc`; from session 6 onward each also carries `orchestrator_model`. Zero tier escalations were needed in the whole build. The three rows reconstructed under GD-1 are marked with `record_provenance`.

### M.10.6 What this build is, stated once, plainly

It produced a working read-only corpus explorer whose every required function exists and is exercised on the production path against a live 1,443-document corpus, and it did so without writing one byte to that corpus or its index.

Its defects at terminal are small: one layout overflow whose cause is now measured to a single unwrapped `<code>` element, and two strings that are undated or misnamed.

What keeps it from PASS is not the product. It is that the harness measuring the product could not reliably audit its own claims -- an acceptance check whose expectation came from the artifact it checked, a probe budget spent before the surfaces it had to qualify existed, a critic scale with no calibration and no second scorer, and no layer anywhere that reads a claim and then reads the artifact it describes. An external reader found four defects that four builder waves, 262 tests, twelve green presets, two oracles and a dedicated critic all missed; and the build then reproduced that same class three times inside the instruments it built to end it. That is the finding of this campaign, and BOUNDED_FAIL is the honest name for it.

## M.11  Queue (Director-owned; opened 2026-09-05 at the A3 filing)

The handback carried no queue section before this entry; items were held in
`build_state.json` `next_action` and in RHACO-CCX-20260905-001 section 7. Opened here
so a follow-on chartered by an amendment has a home in the record that amendment cites.

| # | Item | Route | Source | State |
|---|---|---|---|---|
| Q-1 | `_index_after_canonical_write` emits one log line on success and one on swallowed failure, so the chat-side canonical write path observes itself (Charter Principle 2) and criterion-6 source 3 stops depending on mtime inference alone | `mcp` (observatory server; RHACO-CMP-20260526-001 is its home) | HND-20260903-001 Amendment A3 section A3.3, second bullet; ledger P-23 | QUEUED -- Director-owned, **not directed by A3 and not executed here** |
| Q-2 | CMP Specification v1.4 candidate: milestone leads in section 5 are authored as **bold runs** (`**Milestone N —**` or `**M<n> —**`), so that a `CMP-id M<n>` citation resolves as an anchor by construction rather than by luck of house style. Evidence: the citation auditor's `_has_milestone` accepts a heading or a bold run only, and three CMPs (20260808-001, 20260815-002, 20260903-001) wrote theirs as plain text, carrying 44 milestone findings between them — 31 unbaselined and 13 baselined — every one of which cleared on 2026-09-05 by adding four bytes per lead. The spec change makes the fix unnecessary for the next campaign instead of repeating it | `docs` (CMP Specification; RHACO_CMP_Specification_v1_3 is the current cut) | RHACO-CHG-20260905-001 section 5 Execution Record, CS-4 step-1 mechanism error; ledger P-24 neighbours it as the same class of "the record was never checked by the thing that would have caught it" | QUEUED — Director-owned, **not directed by this hand and not executed here** |

## P. Prompt-findings ledger (launch note step 8; seeded session 4)

Columns: `id | finding | source (harness-detected | external | Director) | prompt section | class (prompt-design | hand-design | harness | client | build | governance | measurement design) | disposition`.

| id | finding | source | section ref | class | disposition |
|---|---|---|---|---|---|
| P-1 | Criterion-3 sequencing unsatisfiable as written: PROMPT section 14 step 5 before step 8, but section 4 wave 1 co-dispatches probe and diagnostics, and KB1-KB4 need wave-2/3 surfaces | harness (round 1 recorded `not_implemented_yet` for KG / KB1-KB5) + Director | PROMPT section 2, section 4, section 14; CMP criterion 3 | prompt-design | AC-3 ruling (K.4); A2 at terminal; template item |
| P-2 | Probe failure predicate = capture failure only; no observed-fault predicate in the prompt; `ok = error is None` implemented literally (run 20260904T033047Z: http 404 + console error + failed request, `ok: true`) | external (chat review) -> Director invoking the PROMPT section 2 repair rule | PROMPT section 2 | prompt-design | probe round 2 (K.6); template item |
| P-3 | Contract coupling at merge caught and revalidated (4 tests invalidated, `Subtree` canonicalized, template blocks) -- zero merge conflicts is not semantic integration | harness (integrator, ARCHITECTURE A20) | PROMPT section 4 | harness | Pattern #3 evidence; M3 |
| P-4 | Refusal rule (A1.6) never triggered once the section 2 L text led every strand prompt (rounds 0-1) | harness | HND A1.4 / A1.6 | client / hand-design | evidence; M3 |
| P-5 | Builders self-reported late opening timestamps (B2, B3) rather than fabricating | harness | Guide v1.3 section 6 | harness | capture-discipline note; M3 (rule 7 now says "as your FIRST tool call") |
| P-6 | CCX-20260904-001 section 0.2 census arithmetic (1444 vs observed 1443) | external (ChatGPT R8) confirmed by chat; observed by Code at filing and at the commit | CCX section 0 | governance | section D / K.5 note; CCX not edited |
| P-7 | Launch note step 2 names two chat-side log entries and the live `mcp_validate.log`; three entries exist (one unlisted read-only census) and the pair-write entry had rotated into `mcp_validate.log.2026-09-03` | harness | launch note step 2; HND section 2 N | governance / measurement design | K.3; closeout reconciliation names the rotated logs |
| P-8 | **The probe's per-module round budget structurally equals a product module's, while the probe is rebuilt every wave.** PROMPT.md's Hard Budgets give 4 rounds per module with no distinction between a product module (built once, then repaired) and the measurement instrument (extended in wave 1, repaired at C1, extended again for wave-2 surfaces, and extended once more for wave-3 lineage): the probe consumed 3 of 4 rounds before the lineage surface it must qualify even existed, so its final round must carry Q3 AND the FINAL suite with nothing in reserve | harness (round budgets 9/28 at the round-2 boundary; probe 3/4) + Director | PROMPT.md Hard Budgets; section 2 (probe qualification); section 4 (waves) | prompt-design | Director ruling 2026-09-05 item 4: budget **not waived** -- round 4 carries Q3 + FINAL and any further need is the prompt's BOUNDED_FAIL, never a lowered bar. **Template item**: a future prompt of this shape should budget the instrument separately from the product modules, or fix the instrument's scope before the surfaces it measures are dispatched |
| P-9 | **The identity pin is re-entry-only: a mid-session client model swap is undetectable by the harness and by the running model.** HND section 2 M re-runs section 0 item 9 at re-entry and treats a model change *between* sessions as a halt; it has no provision for a change *within* one. Session 5 observed `claude-fable-5-1` at 02:21:32Z, and at 02:37Z -- after a swap the Director reports -- the orchestrator's own system prompt STILL read 'You are powered by the model named Fable 5.1. The exact model ID is claude-fable-5-1.', while `claude --version`, the client PID, and every environment variable were also unchanged. The only in-band evidence was a side effect: `~/.claude/settings.json` rewritten at 02:30:18Z with the `model` pin removed. A safeguard degradation, which writes no settings file, would have left **no** on-disk trace at all | harness / client (Director-reported swap; orchestrator could not corroborate it) | HND section 2 M, section 3 standing halts; CMP section 6 REVIEW trigger | harness / client | **Template item**: re-check identity at every ROUND boundary, not only at re-entry, and record the check; treat the settings.json sha as part of the identity pin (it is the only artefact that moved); and note that effort in force can change silently with the model (`modelSettings.claude-opus-5.effortLevel = 'medium'` against a session effort of record of xhigh). Halt of record: section D row 5 |
| P-10 | **The client's safeguard fired on the build's own tool output and silently swapped the orchestrator's model and effort, without regenerating the system prompt.** At 2026-09-05T02:25:02Z the session model changed `claude-fable-5-1` -> `claude-opus-4-8` with `apiRefusalCategory: 'cyber'`, `trigger: 'refusal'`, `direction: 'retry'`, `scope: 'session'` -- an automatic, session-wide switch with no operator action. The flagged content was this build's own material: the probe's fault-injection vocabulary (`wrong_doc_for_id`, `stale_card`, `reverse_edges`, `broken_jump`, `console_error`), its headless-browser driving, and the round-2 probe report. The notice's own text concedes the class: 'can sometimes flag legitimate coding, cybersecurity, and biology tasks'. The system prompt was NOT regenerated: it still named Fable 5.1 two model changes later | client (harness) | HND section 2 I (tier map, model of record), section 2 M (identity), section 3 (standing halts); CMP section 6 REVIEW | harness / client | **Template items:** (a) the identity check must read the CLIENT'S PER-REQUEST MODEL (the transcript `message.model` field), never the system prompt's model sentence, which is composed once per session and goes stale silently; (b) configure the switch behaviour to HALT rather than to retry-and-continue, so a governed run cannot proceed under an unratified model; (c) a measurement instrument whose vocabulary is adversarial by construction will trip broad content safeguards -- budget for it. Halt of record: section D row 5; notice verbatim in the findings file |
| P-11 | **The section 2 L text reproduced inline in a dispatched message is retyped, and drifted from the archive by one character.** The wave-3 dispatch carried a typographic apostrophe (U+2019) in "workspace's" on line 2 of the D-12 block where the canonical text has U+0027; one line of seventeen, one character. The archived prompts were byte-correct and each strand verified its own by sha256 before acting, so the instruction the strands obeyed was clean | harness (orchestrator authoring), caught by the orchestrator before any strand returned | HND section 2 L / A1.4; section 8 item 7 | governance | **CONVERTED FROM FINDING TO CONTROL** by the Director, 2026-09-05: the constructed prefix is now byte-diffed against the archived prompt BEFORE dispatch (verified at probe round 4, identical). Wave 3 keeps its recorded deviation. The control, not the glyph, is the durable outcome; **template item**: any text carried verbatim into a dispatch must be diffed against its archive at dispatch time, never trusted to retyping |
| P-12 | **The first-proposed remedy for P-11 was not executable on the actual surface: Workflow scripts have no filesystem access.** The recorded fix-forward was "build the inline L text by reading the archived prompt prefix from disk". Implementing it revealed that a Workflow script cannot read files at all, so the archive is unreachable from the place the text is assembled. The working remedy is different in kind: construct the prefix in the script from a line array with the backslash supplied by `String.fromCharCode(92)`, then extract that construction from the persisted script, execute it, and diff the output against the archive before dispatching | harness / client constraint, discovered while implementing P-11's remedy | Workflow tool contract | client | Recorded on the Director's instruction as a discovery in its own right. **Template item**: a proposed control must be shown executable on the surface that will run it before it is recorded as the remedy -- otherwise the record carries a fix that cannot happen, which is worse than carrying the open finding |
| P-13 | **Integrator scope expands under budget pressure, because the prompt defines shared ownership by COST rather than by STRUCTURE.** PROMPT.md section 4 gives the integrator "shared templates/assets when ownership cannot be isolated". The `#keyhelp` block is byte-identical markup duplicated into five builder-owned templates; isolating it would have cost five builder rounds for a documentation edit, so the integrator edited five builder-owned files instead. The Director accepted the repair because it was disclosed with a compensating control rather than absorbed silently -- but the defect the control guards is the DUPLICATION itself | harness (round-3 integration) + Director | PROMPT.md section 4; ARCHITECTURE A22 | prompt-design | Accepted as recorded, with the structural fix logged as an open item for the critic (a **web round-2 candidate, not an integrator edit**): duplicated shell markup belongs in `base.html` once. **Template items**: (a) duplicated markup belongs to the shell BY RULE, so ownership is decided structurally and never by round cost; (b) where the rule is missing, a contract test is the honest control -- and this one proved itself immediately, catching the newly merged lineage page which had copied the two-key block inside the same wave (logged as positive harness evidence). **SECOND INSTANCE, round 7 (Director ruling 2026-09-05; accepted as disclosed):** the integrator edited `explorer/search/templates/search/search.html` -- a builder-owned template -- rewriting two exact-token mode checks as prefix matches, after corpus_adapter round 4 changed `mode_effective` and correctly FILED its out-of-scope callers rather than editing them. Same shape as the keyhelp instance: a builder's ownership rule produced a correct refusal, and the cost of routing the repair through a builder round -- with `main` carrying the break in the meantime -- sent it to the integrator instead. Two differences worth recording. (i) It was not a documentation edit: the exact-token check would have silently dropped BOTH evidence disclosures from degraded graph results, so `main` was carrying a real regression, and a disclosure that disappears exactly when the mode degrades is the worst failure mode it has. (ii) PROMPT.md section 4's dependent-revalidation-on-a-contract-change clause covers it directly (the `c861680` precedent), so this instance has a rule behind it that the keyhelp instance did not. The Director accepted it with two verification conditions rather than bare -- confirm the next round's base contained the edit before merging, and re-run the SA-1 DISABLE_VEC live check after -- both discharged (base `26816db` contains `dcb0443`; the re-run is in M.9). **Template item, reinforced:** twice observed, and both times the trigger was a SHARED VOCABULARY OR SHARED MARKUP with no single owner. That is the structural gap; the integrator's judgment is not the variable. **THIRD INSTANCE, round 9 (Director ruling 2026-09-05), and the rule is amended with it:** the integrator edited `explorer/search/service.py` -- builder-owned -- to derive the page's own measurement date from `gold_v1_1_compat.json` rather than the retyped `2026-09-04`, and added a contract test that fails whenever the page and that artifact disagree. **RULE AMENDED ON THE RECORD: OMISSIONS WAIT FOR A ROUND; AFFIRMATIVELY FALSE STATEMENTS ARE REPAIRED WITH A CONTROL.** That is what separates this instance from O-4 and O-6, which stay open and unspent -- those are omissions (an undated figure, a misnamed attribute), while this was a statement affirmatively false about the artifact beside it, in a tool whose subject is provenance, and whose own scope register had already repudiated the date in the same round. The control is the amendment's teeth: the repair is not accepted on the integrator's word but on a test proven to fail when the page and the artifact diverge |
| P-14 | **Fixture representativeness: a hand-authored synthetic fixture reproduces the AUTHORS' conventions, not the CORPUS's variance.** Every card in `fixtures/corpus/` quotes its dates (`date: "2026-01-15"`); 866 of the 1,443 census cards (60.0%) do not, and an unquoted YAML date scalar resolves through `yaml.safe_load` to a native `datetime.date`. The reader therefore 500s on both `/doc/...` and `/api/doc/...` for the majority of the live corpus, while every L1 test, the whole fixture qualification path, and four rounds of green suites passed -- because the fixture cannot produce the type that breaks. Found by the probe's W6 on the production path in the probe's FINAL round; reproduced independently by the integrator | harness (probe round 4, W6) + integrator reproduction | PROMPT.md section 2 (production-path exercise), section 1 (fixture substrate); DISPATCH_PARAMETERS item F | measurement design | **Strongest H1(c)/(d) evidence of the campaign**: a qualified production-path probe found a defect that L1, the fixture path, and four green rounds structurally COULD NOT. **Template items**: (a) fixture cards must be sampled from, or derived from, the real corpus's LOADER-RESOLVED TYPES, not hand-authored to the authors' own style; (b) a fixture-vs-corpus TYPE CENSUS belongs in round-0 inspection output, before any module is built against the fixture. **Ordering finding, recorded with it**: the crash MASKED a second, silent defect of the same root cause -- a type-naive card/index comparison would mark a bare-date card as "index differs from card file" when the two agree. That false marker cannot even be measured until the 500 is fixed, so the loud defect hid the quiet one, and the quiet one is the worse of the two |
| P-15 | **The reusable strand common-rules template hardcoded a round number, so it instructed later rounds to overwrite an earlier round's report.** `body_common_rules.md` named `docs/rounds/R02_{MODULE}.report.md` in both its working-rule 1 and its rule 8, because it was first written for wave 2. Round 3's lineage strand and round 5's reader strand each received a prompt whose boilerplate said `R02_<module>.report.md` while the task body said `R03_`/`R05_`. **Both strands detected the contradiction, chose the specific and convention-consistent instruction, and left the earlier report untouched** -- the reader strand disclosed it explicitly as a deviation rather than silently resolving it | harness (orchestrator authoring), caught and reported by two independent strands | HND section 2 L dispatch assembly; PROMPT.md section 10 (round reports are evidence artifacts) | governance / harness | Template corrected to name no round number and to say explicitly never to overwrite a prior report. **This was an evidence-destruction hazard, not a cosmetic one**: a literal-minded strand would have overwritten `R02_reader.report.md`, destroying a merged round's evidence with no test able to notice. **Positive harness evidence**: the safeguard that worked was the strands' own judgment plus the instruction to disclose deviations -- neither the gates nor any test could have caught it. **Template item**: generated prompt boilerplate must carry no round-specific literal; every round-varying path is a parameter, and evidence paths should be append-only by rule |
| P-16 | **Qualification asserted on a substrate attribute, not the rendered observable.** KB3 compares the `(from_id, to_id, direction)` triple read from `data-edge-*` attributes against the fixture fact and never reads the sentence; `Edge.direction_label` was direction-blind, so every incoming edge rendered the outgoing sentence ("is superseded by: <its own predecessor>", "has campaign child: <its own parent>") while KB3 stayed green. W5's `direction_label` assertion compares the product against the constant it renders from and cannot fail on the defect it appears to guard. Critic round 1 found it (gate 7 FAIL, ranked issue 1); qualification structurally could not | Director ruling 2026-09-05T05:36:52Z (session-6 transcript `918204e5...`); orchestrator numbering 05:41:54Z; written S7 under `claude-fable-5-1`, identity halt open | PROMPT.md section 5.5 (directional display), section 2 (qualification), section 7 gate 7; CMP criterion 3 | measurement design | Contract change `c861680` (direction-aware `RELATION_LABELS`, reader revalidated); lineage round 2 for the templates; W5 recorded BOUNDED_FAIL (probe budget exhausted), compensated by the integrator L4 direction oracle (rendered sentence vs `from_id`/`to_id`). **Template item**: a qualification case asserts on the user-facing rendered observable; a substrate attribute alone qualifies nothing the operator reads. H1(g) evidence [authored session 7 under `claude-fable-5-1`, identity halt open -- session-5 precedent: mechanical records, no judgment] |
| P-17 | **Harness self-audit: the harness verifies behaviour and never audits its own claims.** Of five sealed defect items, four (SA-1 degradation notice, SA-2 non-binding 8 s budget, SA-3 false sole-importer invariant, SA-4 L1 scope) were missed by harness and critic alike; three of the four are false or unsupported prose claims about code behaviour (a notice, a docstring, an architectural invariant). SA-6 (probe compares the DOM against fixture sources on disk) was satisfied unprompted -- the counterweight, and why criterion 3 is genuinely satisfiable | Director ruling 2026-09-05T05:36:52Z (session-6 transcript `918204e5...`); orchestrator numbering 05:41:54Z; written S7 under `claude-fable-5-1`, identity halt open; classification in `build_state.json` `sealed_audit_release` | PROMPT.md section 10 (round reports), section 7 (critic); ARCHITECTURE / CONSTRAINTS prose; LAYERS section 1 L1 | measurement design | **Template item**: every docstring / notice / ARCHITECTURE sentence asserting a code behaviour gets a named check or an `UNVERIFIED` tag; the round-report rule extends to prose in code and design docs. Repairs: SA-1, SA-2 -> adapter round 4 batch; SA-3 -> ruling (b), integrator text edit (L.3); SA-4 -> L1 import-boundary check; SA-5 -> ARCHITECTURE 4.6 text fix. M3 ANL headline (H1 evidence map item (g), with P-16) [authored session 7 under `claude-fable-5-1`, identity halt open -- session-5 precedent: mechanical records, no judgment] |
| P-18 | **CCX-20260905-001 states lineage round 2 and adapter round 4 as "dispatched-or-about-to-be" (header 'Session scope'; section 4); on disk neither was dispatched.** No `R07_*` / `R08_*` prompt exists under `docs/rounds/`; `build/lineage` (`386afba`) and `build/corpus_adapter` (`7c1e9f4`) are 0 commits ahead of `main` `c861680`, both worktrees clean; the session-6 transcript's last orchestrator report (05:41:54Z) names the two rounds as NEXT and the session ended at 06:01:12Z (compaction, then close) without dispatching. Same class as P-6: a handoff stating a predicted state as an observed one (the CCX section 0 item 4 statement that `build_state.json` `next_action` "names the convergence sequence" is the same error -- on disk it named critic round 1) | harness (observed by Code at the session-7 re-entry, D row 7 / L.4); Director disposition 2026-09-05 (row directed); written S7 under `claude-fable-5-1`, identity halt open | CCX header; CCX section 4; CCX section 0 item 4 | governance | Section D row 7 / L.4; CCX not edited. **Template item**: a handoff states observed dispatch state (dispatch record id, `dispatched_utc`), never intent [authored session 7 under `claude-fable-5-1`, identity halt open -- session-5 precedent: mechanical records, no judgment] |
| P-19 | **The per-request `message.model` field strips the `[1m]` context-window suffix, so it cannot by itself distinguish `claude-opus-5` from `claude-opus-5[1m]`.** The session-8 launch instruction made `message.model == claude-opus-5[1m]` a halt condition; the observed value was `claude-opus-5`. Two controls, neither of them this session's own reading, establish that the field is truncated rather than the identity drifted: session 6 -- MOD-1 in force, settings `model: claude-opus-5[1m]` (sha `831dbfb9...`), system prompt naming `[1m]` verbatim -- logged `claude-opus-5` in **all 255** per-request fields; session 7, under settings `model: claude-fable-5-1[1m]`, logged `claude-fable-5-1` in **all 173**. No session under any `[1m]` pin has ever produced a suffixed string in that field, so the instruction as worded is unsatisfiable by construction and would halt every conformant session, including the one that ratified MOD-1 | harness (observed by Code at the session-8 re-entry, section M.2); instrument-before-artifact per RHACO CLAUDE.md section 17 rule 8 | HND section 2 M item 9; CMP section 6 REVIEW trigger | client / measurement design | **Refines P-10 template item (a)**, which had reduced the identity check to the per-request field alone precisely because the system prompt goes stale. Both surfaces are individually insufficient, in opposite ways: the system prompt is stale-but-precise (regenerated only at a client restart), the per-request field is live-but-truncated. **Template item**: the identity of record is the CONJUNCTION of `settings.json` sha + `model` key (the only surface carrying the context-window suffix), the system-prompt sentence, and the per-request field read as a family-and-generation check -- and a halt predicate must be written against a value the instrument can actually emit. Recorded in `build_state.json` `client.session_model_history` seq 6 |
| P-20 | **A false prose claim propagates by copy, so an external audit names instances while the defect is a population.** SA-3 named two sole-importer claim sites (ARCHITECTURE 4.1 and the `corpus_adapter` package docstring) and the session-7 census confirmed those two; re-deriving on `f29bcc9` found the same claim in **seven places across four files** -- `ARCHITECTURE.md:14` and 4.1, `CONSTRAINTS.md` S2, `__init__.py:3`, `adapter.py:2`, `availability.py:23`, `sql.py:9`. The importer census itself matched L.3 exactly, so the under-count was in the claim sites, not the facts. Second half, same authoring class: the three R07 prompts were first written naming a literal base commit that the worktrees could not report, because the commit adding a prompt cannot be named inside it -- a strand verifying its base by `rev-parse` would have gone to BLOCKED on a bookkeeping artifact (corrected at `3e7521b` to record `rev-parse` and assert ancestry instead) | harness (integrator re-derivation at the session-8 SA-3 remedy) | PROMPT.md section 10; ARCHITECTURE / CONSTRAINTS prose; HND section 2 L dispatch assembly | governance / measurement design | Integrator-owned sites corrected at `f29bcc9`; the three corpus_adapter-owned docstrings deferred to adapter round 4 as its lowest-priority batch item, after the ruled three, so a trivial edit cannot displace SA-1 or SA-2. **Template items**: (a) when an audit names a prose-claim defect, re-derive the claim's full extent before scoping the remedy -- the named site is a sample; (b) generated prompt text must carry no literal that names the commit containing it (P-15's rule, one step further: no round-specific AND no self-referential literals) |
| P-21 | **No layer of this harness audits an assertion against its referent, and that is the generator behind its characteristic defect class.** L1 lints and greps, L2 and L3 test behaviour, L4 compares outputs -- and nothing reads a claim, reads the artifact it describes, and compares the two. Every instance of the class the sealed audit named (SA-1 a degradation notice misnaming what degraded, SA-2 a docstring asserting a bound the code did not implement, SA-3 an architectural invariant contradicted by the tree) was found that way by an external reader, and every new instance in round 2 was found that way by the critic: an oracle reporting 213 pages rendered when it rendered none, a comment claiming no raw SQL above raw SQL, a ledger asserting five items closed while carrying still_live on one. The build repaired every named instance and reproduced the class inside the compensating controls for that class | critic round 2 (S8-CR2), as its recommended third-round action and the only one it says would settle convergence | PROMPT.md section 3 (verification layers), section 14; CMP-20260903-001 section 6 | **prompt-design (PRIMARY ROW)** | Director ruling 2026-09-05: NO claim-audit pass in-flight -- a harness layer the prompt does not define is a HARNESS REVISION under CMP section 6 and is out of scope for this hand. Recorded here as the ledger's primary prompt-design row and as an M3 template item. **Template item, and the campaign's central one**: a prompt that defines verification layers should define one that audits claims against referents, because a build whose layers only lint, test and compare outputs will produce true code and false prose about it, indefinitely, and will not be able to see that it is doing so. The critic's own framing: 'a build that reproduces the critic's named defect class inside the compensating controls for that class is still generating, not converging' |
| P-22 | **The integrator recorded a builder's UNVERIFIED result as verified, and the false entry stood until an outside reader re-derived the observable.** Web round 2's report stated TWICE, unprompted, that whether the reader page now fits 1280 px was UNVERIFIED as rendered geometry -- the strand had no browser and said so. The integrator recorded the disposition in `build_state.json` as `landed: YES ... fixed once in app.css rather than per module`, with no qualifier. The defect was in fact unchanged: 1387 px at the convergence commit, byte-identical to round 1. The ledger dropped the qualifier its own builder had insisted on keeping | harness (integrator), found by critic round 2 (its ranked issue 1, the highest of the round) | hand section 2 I / section 10 (build_state as the record); Guide v1.3 section 8 | governance / record integrity | Corrected in `build_state.json`; BF-3 carries the measured cause and this row carries the record failure, deliberately separated. **Template items**: (a) a builder's UNVERIFIED is a first-class value that MUST survive into the ledger -- an integrator may overrule it with evidence, never by omission; (b) the failure mode is asymmetric and worth naming, because the strand was more careful than the record that consumed it, so the harness LOST information at the integration step rather than at the production step, which is the opposite of where review effort is usually aimed |
| P-23 | **A reconciliation rule can name the wrong mechanism and still read as satisfied, because the instrument it names is silent rather than absent.** Amendment A2 section A2.10 routed criterion-6 reconciliation of `corpus_index.db` to the rotated MCP census logs, on the premise that a chat-side canonical pair write emits a census entry that can rotate out of the live log before the next session opens. Measured 2026-09-05 against observatory server 0.22.0: a canonical write reindexes through `_index_after_canonical_write` (`RHACO_mcp_observatory_server.py:621`, `reindex_pair` / `reindex_fts_pair`, failures swallowed by contract) and **logs nothing**, while the `MCP census summary` line is emitted only by `validate_catalog`, which by design **never reindexes**. The two paths never co-occur, so no rotated file can carry a line that was never written, and the census lines reconciled in J.5 and K.3 were explicit post-write `validate_catalog` calls by the writing chat -- a property of that chat's discipline, not of the write. Two instances: the A2 pair (write 18:27:21.83Z, index 18:27:30.73Z, zero log bytes) and the CHG-20260905-001 pair (write 18:36:26.38Z, index 18:36:35.36Z, zero log bytes) -- the second observed live, mid-task, which is why the mechanism was measured rather than inferred | harness (Code, at the 30613bc commit task) -> Director ratified same day | HND section 2 N as amended by A2 section A2.10; CMP-20260903-001 section 3 criterion 6 | measurement design | **Amendment A3 filed `5ef99bd`**, replacing A2.10 with three sources matched to their mechanisms (librarian reindex entries; explicit `validate_catalog` censuses, live and rotated; canonical writes, evidenced by pair mtimes plus an index mtime within ~10 s and the new `cards` row, with no log line at all). Queued, not directed here: an `mcp`-route CHG candidate making `_index_after_canonical_write` emit one line on success and one on swallowed failure, so the write path observes itself (Charter Principle 2) -- see M.11. **Template item**: a reconciliation rule states the mechanism that produces each evidence class, because a log-reading remedy is only ever as good as the logging that precedes it, and a silent path reads exactly like a clean one |
| P-24 | **An in-place edit reordered three ledger rows by splicing one of them at a byte offset, and the corrupted record stood undetected because the ledger's own home is outside every gate that would have seen it.** Discovered 2026-09-05 while appending P-23: line 716 of this handback held the P-20 row cut mid-word at column 727 (`...could not report, b`) with the whole P-21 row spliced into the gap, and P-20's tail (`ecause the commit adding a prompt...`) stranded two lines below, after P-22. Three rows rendered wrong at once, P-21 among them — the ledger's own PRIMARY prompt-design row, and the one the M3 template item is drawn from. The mechanism is an editor writing a replacement whose halves were reassembled in the wrong order; the reason it survived is structural, not clerical: `working\` is outside the catalog census walk (CLAUDE.md section 17 rule 7), so no librarian, no card, no auditor and no gate ever reads this file, and its only reader is the next instance that opens it. That is the census-invisibility corollary firing in the direction the rule warns about — the same direction as the 2026-07-15/16 PK near-loss — on the artifact that holds the build's findings of record. Repaired the same day by reassembly, character-conserving and proven so (5,056 characters in the three damaged lines before, 5,056 after; the join `report, b` + `ecause` verified to reconstitute `because`); no content was rewritten, only the newline positions | harness (Code, discovered while executing the P-23 append; not found by any gate, because none reads this path) | launch note step 8 (the ledger); CLAUDE.md section 17 rule 7 corollary | governance / record integrity | **Minted on Director ruling 2026-09-05** after the repair was reported at the CS-4 STOP rather than made silently. Second record-integrity row of the build after P-22, and the pair is the argument: P-22 lost a builder's qualifier at the integration step, P-24 lost three rows to an editing mechanism, and both stood because the record that consumed them is ungoverned by construction. **Template item**: an artifact that holds a build's findings of record belongs inside the walk that checks records, or it inherits none of the guarantees the corpus gives every other document — a handback is evidence, and evidence outside the census is evidence no instrument has ever read |
| P-SEAL-RELEASED | **Seal opened 2026-09-05**, release condition met (first `docs/critique/` report exists). Both files re-hashed at release and byte-identical to their session-4 pins; the companion ChatGPT file was re-hashed and **NOT opened** (the seal says "hash it, do not read it" and its On-release section gives no instruction to open it -- held for a Director instruction rather than opened on an inference). **Tally: of five sealed defect items, FOUR were MISSED by the harness and the critic alike** (SA-1 graph degradation notice, SA-2 the non-binding 8 s probe budget, SA-3 the false sole-importer invariant, SA-4 L1's scope); **one (SA-5) was caught by both**; and the SA-6 watch item is **satisfied unprompted** -- the harness built the probe's disk-comparison itself, which is why criterion 3 is genuinely and not merely nominally satisfiable. Three of the four misses share one structure: false or unsupported PROSE CLAIMS about code behaviour. The harness tests behaviour well and audits its own claims poorly | Director (sealed) | -- | measurement design | Full classification with per-item evidence in `build_state.json` `sealed_audit_release`; repairs routed to the batched adapter round (SA-1, SA-2) and to integrator text fixes (SA-3 prose, SA-4, SA-5) |
| P-SEAL | External audit sealed: `working\sealed_audit_HND-20260903-001_S4.md` -- sha256 `7400496508b823dac6f1a5f2231cb6f58cdf4efe13ab1adbe24259592e823ed9`, 6,138 B, mtime 2026-09-04T23:42:57Z; `working\RHACO_Corpus_Explorer_Build_Review_Pattern_3_Prompt_Recommendations.md` -- sha256 `2f3914e971d30bbed6a0aac9f3bc7a92f1c6e11f0de48a9cf587215ed5ec51a2`, 24,699 B, mtime 2026-09-04T23:01:11Z (both `Get-FileHash` 23:47Z; **neither opened**) | Director | -- | measurement design | release at critic round 1 (the first `docs/critique/` report); compare against the critic's issue set |

*Reno High-Altitude Cosmic Ray Observatory (RHACO) | Kris E. Granholm, Director*

## N. M3 criterion-6 verification and index reconciliation (2026-09-05)

All four items complete. Report at the boundary.

## Launch — conformant, no halt

| Leg                    | Observed                                                                                                                                                    |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Caps (process)         | `SPAWN_DEPTH=1`, `CONCURRENT_SUBAGENTS=3` — both print back                                                                                                 |
| `claude --version`     | **2.1.260**                                                                                                                                                 |
| P-19 (1) settings      | sha `06f37244dda720a925b1a237d04a30a172b11444713a792de6d25d4e153cd279`, **9,097 B**, mtime 14:36:20Z — the option-A rewrite; `"model": "claude-opus-5[1m]"` |
| P-19 (2) system prompt | sentence names `claude-opus-5[1m]`                                                                                                                          |
| P-19 (3) per-request   | **887 of 887** `claude-opus-5`, suffix-stripped, **no other value in the transcript**                                                                       |

Every §0 pin re-derived rather than trusted: A1 `167771c7` 9,698 B · A2 `34ee0c2d` 13,899 B · A3 `775d7b56` 3,962 B · CHG `adc178d1` 14,537 B, body Baseline · CMP 18,244 B, 19:23:05Z, `Current state: ACTIVE` · Guide v1.3 `fdd1c5f6` 19,842 B · head `b35d788`. **No drift.**

## Item 1 — committed and pushed, but the expected red set was wrong

`5f76a37`, subject 66 chars, ASCII, one id. Push `b35d788..5f76a37` ff; `origin/main` = `5f76a37`; 0/0. Pins in the body: `9f777b57…` 18,984 B and `de564cb1…` 3,718 B.

**The auditor went red at +3/−0, and it was not expected to.** All three are the new CCX's own citations, zero backlog (308 → 311):

| CCX line | Written                                       | Bound to | Why it fails                                                                                        |
| -------- | --------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------- |
| 10, 34   | `CHG-20260905-001 (CMP §8 …)`                 | CHG §8   | the §8 is the **CMP's**; the parser binds to the nearest preceding id                               |
| 122      | `CMP-20260903-001 §2 …; (6) RATIONALE.md §13` | CMP §13  | the §13 is **`RATIONALE.md`'s** — a workspace file, invisible as an id, so the CMP binding persists |

Same class as A1's +2 at `3b860bf` (a citation whose true target the auditor cannot see), which this arc pushed ratified-red and attribution-closed. I pushed on that precedent since the commit itself was directed and the finding is in Director-authored ratified bytes I have no license to edit — but the deviation is yours to disposition. It is also, precisely, a fourth same-week instance of **measurement defines category**: the citation means what the grammar binds, not what the author meant.

Regression: comparator run directly = **exactly the two INC members**. The gate's one-line relay again showed only one of the two.

Gate 21:05:15Z: `librarian=PASS/13.5s regression=FAIL/31.9s auditor=FAIL/19.4s structure-guard=PASS/0.1s git-status-clean=PASS/0.0s`.

## Item 2 — A3.2 reconciled; source 3 confirmed to the microsecond

| Source | Event          | Evidence                                                                                                                                                                                                                                               |
| ------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **3**  | CCX pair write | pair mtime **19:58:26.114118Z** — read back from the index's own `card_mtime_utc`, and `card_sha256` = `de564cb1…` = my independent file hash. Index mutated **19:58:34.915Z** (Δ **8.9 s**), `8448c6cf` → `f873d802`, size unchanged at 115,064,832 B |
| **2**  | census         | logged 20:52:42Z, **cards 1448, errors 0**. Your stated call 20:52:33Z — a 9 s call-to-log delta, the same shape as the 18 s one at 13:25                                                                                                              |
| **1**  | librarian      | 21:03:05Z (my census) and 21:04:24Z (the gate), both 1448                                                                                                                                                                                              |

The decisive negative: **no instrument logged any reindex between 19:36:57Z and 21:03:05Z**, yet the index file changed content at 19:58:34.915Z. Fourth live confirmation of the mechanism A3 corrected. No other chat act in the window.

## Item 3 — `working\m3_evidence\` built, no RHACO commit

37 files + `MANIFEST.md`. Payload 1,154,319 B; manifest 6,574 B, `de2fc346…`. Every copy re-hashed after writing and compared to source — verified, not assumed; all marked read-only; all 37 confirmed **tracked at `a87d259`**. Workspace re-verified first: `a87d259` = `origin/main`, tree clean, 0/0, `terminal_state BOUNDED_FAIL`, 20 of 28, register 3 entries.

13 named + 24 round reports; "oracle reports" resolved to `direction_oracle.json`, `card_agreement.json`, `gold_v1_1_compat.json`. `.gitignore:37:/working/` — invisible to git, outside the census walk, outside the auditor root, so it cannot perturb a gate member.

The manifest also names what §3 did **not** list, so their absence reads as a choice: `PROMPT.md` (28,665 B), `DISPATCH_PARAMETERS.md`, the probe-qualification `README.md`, 26 `*.prompt.md`, 60 run dirs. **`PROMPT.md` is the prompt-design layer's primary artifact** — the ANL's ledger class column leans on it, and it is not in the export.

## Item 4 — criterion-6 census, reported not asserted

**(a) 11 commits since `0243874` — 7 filings, 4 docs-tail, build = 0.**

**(b)** `rhaco\RHACO_corpus_index.py` = `7548e47ebdcc24cdc0621208d06673542889c75b000780c650628b0678a5f098`, **matches the pin**. 118,123 B, mtime 2026-07-13, last touched by `5828beb` on 2026-07-12 — untouched across the whole arc.

**(c)** 20 files under `docs\` plus `CLAUDE.md`, nothing else in the repo. Every one attributed:

| File                                                                           | +/−           | Commit                                                                            |
| ------------------------------------------------------------------------------ | ------------- | --------------------------------------------------------------------------------- |
| 14 files = 7 new pairs (A1, A2, A3, CCX-0904, CCX-0905-001, CHG, CCX-0905-002) | new           | their filings                                                                     |
| `citation_auditor_waivers.json`                                                | 16 / **0**    | `888585a` — zero deletions, the byte-preserving append proving itself in the diff |
| `citation_auditor_baseline.json`                                               | 63 / 81       | `888585a`                                                                         |
| CMP-20260808-001                                                               | 5 / 5         | `6b4017e` — exactly 5 leads                                                       |
| CMP-20260815-002                                                               | 7 / 7         | `6b4017e` — exactly 7 leads                                                       |
| CMP-20260903-001 body / card                                                   | 49 / 5, 2 / 2 | `6b4017e` + `6c95b97`                                                             |
| `CLAUDE.md`                                                                    | 2 / 2         | `6c95b97` + `6b4017e`                                                             |

**Zero unattributed lines. Nothing under `rhaco\`, `tools\`, `tests\`, `data\`.**

**(d) Index history since `61ea99c9…`, every step attributed:**

| UTC                       | Src   | Event                                                                                                 | cards  |
| ------------------------- | ----- | ----------------------------------------------------------------------------------------------------- | ------ |
| 09-04 22:55 / 23:15       | 2     | censuses                                                                                              | 1443   |
| 09-04 23:58:11            | 1     | session-4 gate (`4032742`)                                                                            | 1443   |
| 09-05 05:58:48            | 2     | census after the CCX-0905-001 write                                                                   | 1444   |
| 09-05 14:02:54, 14:05:55  | 1     | reindexes                                                                                             | 1444   |
| 09-05 18:38:17            | 1     | reindex — **absorbs two silent writes** (A2 18:27:21Z, CHG 18:36:26Z)                                 | 1446   |
| 09-05 19:02:47            | 1     | reindex — absorbs A3 18:50:15Z                                                                        | 1447   |
| 09-05 19:36:57            | 1     | discharge gate; `cards_sha` moves `7a9fdf78` → `7e568470` at **unchanged count** — the CS-1 card edit | 1447   |
| 09-05 19:58:34.915        | **3** | CCX pair — no log line anywhere                                                                       | (1448) |
| 09-05 20:52:42            | 2     | census                                                                                                | 1448   |
| 09-05 21:03:05 / 21:04:24 | 1     | this session                                                                                          | 1448   |

Current pin: `b97706e8c35772562a5fd404797a90a6ef90cf4ec63c1174ff64925aa2a2e360`, 115,056,640 B, 21:04:24.141Z.

Two findings the reconciliation itself produced, both feeding **Q-1**: consecutive source-3 writes **coalesce** — 1444 → 1446 appears as one reindex, so individual write times are unrecoverable from logs and exist only as pair mtimes; and **no instrument logs the index file hash**, so the chain from `61ea99c9…` lives only in Code reports and cannot be audited from the repository.

Tree clean, `main` = `origin/main` = `5f76a37`. Stopping here.
