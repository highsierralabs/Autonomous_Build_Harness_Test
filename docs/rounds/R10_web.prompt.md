> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S8-B16; role: module builder (web), shell de-duplication and cross-page legibility; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: sequential (ONE strand live -- this round cannot run concurrently with any other, see the licence in item 1); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1). Round 8 of the RHACO Corpus Explorer autonomous build; web module round 2 of 4 -- this strand consumes one builder round of the 28 and is the TWENTIETH of a Director-set ceiling of 20 for the convergence phase. There is no round after this one before critic round 2.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\web -- a git worktree of the workspace repository on branch build/web, checked out at the tip of `main` at dispatch. That tip is a descendant of the round-7 boundary `3f62226` and it adds this prompt file itself, so this prompt cannot name its own commit: your first git act is `git rev-parse HEAD` -- RECORD what it returns and report it as your base, do not match it against a literal here. What you can verify is that your HEAD contains `3f62226` (`git merge-base --is-ancestor 3f62226 HEAD`); if it does not, STOP and return status BLOCKED. Your round-1 work is merged there along with seven later rounds; 303 tests pass. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\web; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/web -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `explorer/web/**` and `tests/web/**`; plus your round report `docs/rounds/R10_web.report.md`. **ONE NAMED EXCEPTION, licensed by Director ruling (boundary-3 disposition item 1) and scoped to item 1 below:** you may DELETE the duplicated `#keyhelp` block from the five page templates that carry it -- `explorer/catalog/templates/catalog/catalog.html`, `explorer/search/templates/search/search.html`, `explorer/reader/templates/reader/reader.html`, `explorer/lineage/templates/lineage/lineage.html`, `explorer/diagnostics/templates/diagnostics/diagnostics.html` -- and nothing else in those files. No other edit to another module's path is licensed: not its service, not its CSS, not its tests. Everything else outside your owned paths is a **change request** recorded in your report. Do not modify `explorer/app.py`, `explorer/config.py`, `explorer/models.py`, `explorer/faults.py`, `explorer/corpus_adapter/**`, `tools/**`, `fixtures/**`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, or `docs/probe-qualification/**`.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db, and only through `RHACO_corpus_index.connect()`, never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond that list). **Never write to the live index**; never edit the RHACO module; never call the five index-writing functions named in CONSTRAINTS.md S6.
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use `fastapi.testclient.TestClient` in-process. Never launch uvicorn and never drive a browser -- the probe is the only sanctioned launcher, and it is not yours. No `run_in_background`, no detached processes, no sleep-and-poll loops. **This constraint bites hard on this round: see item 4.**
4. Git: commit on your branch in small increments with imperative ASCII subjects that describe the whole of their own diff; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests/web -q` green, then `...\.venv\Scripts\python.exe -m pytest tests -q` green as well -- **the full suite matters more than usual this round, because item 1 edits five other modules' templates and `tests/integration/test_keyhelp_contract.py` is the control that guards exactly that**; (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS (it runs TWO checks -- the index-write grep and an import boundary; both must pass). Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in any script you write; UTF-8 with LF line endings.
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and write your round report to `docs/rounds/R10_web.report.md` with the sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, in this order: `docs/critique/R06_critic_round_1.report.md` -- ranked issues 4 and 9, the `accessibility` row of the section-2 table, and the fourth bullet of section 5 ("Keyboard navigation is string-matched in three places and executed in none"); `build_state.json` `open_items` O-2 and its `director_ruling`; `tests/integration/test_keyhelp_contract.py` IN FULL, including its docstring, which is honest about what it does and does not prove; `explorer/web/templates/base.html`; `explorer/web/static/app.js` (especially `searchBox()`); `explorer/web/static/app.css`; the five page templates named in working rule 1, for their `#keyhelp` blocks only; `explorer/reader/static/reader.css` (READ ONLY -- `.doc-body table` and the `<pre>` rules) and `docs/rounds/R09_catalog.report.md`'s item 3, which identified the shared mechanism this round is asked to fix once. Then the task.

Task B16 -- put the shell in the shell, make the advertised key true, and fix the overflow once instead of per module.

**Item 1 -- the `#keyhelp` duplication (open item O-2).** The block is byte-identical markup duplicated into five page templates. The round-3 integrator repair updated all five and added `tests/integration/test_keyhelp_contract.py` as a compensating control, and the Director accepted that repair while recording that **the duplication itself is the defect** and that the structural fix is a web round-2 candidate, not another integrator edit. This is that round.

Move the block into `base.html` once, where the shell lives and where it is yours, and delete the five copies. Keep the contract test working -- it is the control that caught the lineage page copying a stale block inside the very wave that created it, and it must still guard the new arrangement. If the test needs to change shape because the source of truth moved, that is a change request to the integrator (`tests/integration/` is integrator-owned), NOT an edit: say exactly what it should assert now.

Take care with two things. Each page's `#keyhelp` may not be exactly identical any more -- verify before assuming, and if they have drifted, say which drifted and how, because that is evidence about the duplication's cost. And the block is `hidden` and toggled by `?`; make sure moving it does not change which pages have it, its id, or its toggle behaviour.

**Item 2 -- the `/` key is advertised on five pages and works on one (critic ranked issue 9).** `app.js`'s `searchBox()` resolves `document.querySelector('input[name="q"]')`, and that input exists only in the search template. On catalog, reader, lineage and diagnostics the key is silently inert, and the critic notes there is no global search affordance to focus.

Fix the mismatch. Two honest shapes, and you choose and defend: (a) give the shell a real global search affordance so `/` has something to focus everywhere -- a header search field or a link that takes the operator to `/search` -- which makes the advertisement true; or (b) advertise the key only where it works, so the help text stops promising what the page cannot do. (a) is more useful and more work; (b) is honest and cheap. What is NOT acceptable is leaving five pages advertising a key that does nothing. If you choose (a), the field must be a real navigation to the search page, not a second retrieval surface -- do not build a search box that queries anything itself.

**Item 3 -- fix the horizontal overflow ONCE, in the shell (critic ranked issue 4, plus the catalog builder's change request).** The critic measured the reader page at 1387px wide on a 1280px viewport in production captures and 1577-1585px on fixture captures, while every non-reader capture was exactly 1280. Its named likely causes: `.doc-body table` has no overflow container, and `#card-panel`'s `<pre>` blocks hold unbreakable paths and hashes.

The catalog builder, fixing its own clipped table in round 7, identified the shared mechanism explicitly and filed it to you: ranked issue 4 and the catalog's clipped header are the same gap -- there is no shared table/`<pre>` legibility rule -- and it belongs in `app.css` once rather than as a patch per module. It proposed a `.data-table`-style utility carrying `table-layout: fixed`, `overflow-wrap: anywhere`, `word-break: break-word`.

Do it in `app.css`, which is yours. **Prefer rules that fix the reader without editing reader-owned files** -- `app.css` is loaded on every page, so a well-chosen selector (for example one scoped to `.doc-body table` or to `pre` inside the document body) reaches the reader's markup without your touching `explorer/reader/**`, which you are NOT licensed to edit. If you conclude the honest fix genuinely requires a reader-side change, file it as a change request and say precisely what it is; do not reach into that module.

Do not fix overflow by hiding content: `overflow: hidden` on a document body would trade a visible problem for an invisible one, which is the failure mode this build has spent two rounds removing.

**Item 4 -- the limit you must state rather than paper over.** The critic's fourth blind spot: *"Keyboard navigation is string-matched in three places and executed in none. The `#keyhelp` contract test is honest about this in its own docstring; L2 does not fill the gap. I am the first layer to have looked."*

You cannot close it. Executing a key press needs a browser, the probe is the only sanctioned launcher, and the probe is exhausted at 4 of 4 with its budget explicitly not waived. So: do not write a test that appears to exercise the keyboard and does not. Every assertion you add about `app.js` is a STATIC assertion about source text, and must say so in its own docstring exactly as the keyhelp contract test does. In "Unresolved uncertainty", state plainly that after this round the keyboard behaviour of this build has still never been executed by any layer, and that items 1 and 2 improve the CLAIM's accuracy without adding behavioural evidence for it. That sentence is the deliverable; an unearned green test would be worse than the gap.

**Item 5 -- tests in `tests/web/`.** For item 1: that the block exists once in the shell and appears on every page, and that no page template carries its own copy (write it so a re-introduced copy FAILS, since re-introduction is exactly the defect's history). For item 2: whatever your chosen shape makes checkable -- if (a), that the affordance is present in the shell on every page; if (b), that the help text on a page without a search box does not advertise `/`. For item 3: static assertions on the emitted CSS/markup, labelled static.

**In "Result"** state the module round consumed (**web round 2 of 4**), a per-item `landed | not landed` line, and the fact that this is the twentieth round of a ceiling of twenty.

**In "Unresolved uncertainty"**, besides item 4's required sentence: say whether any OTHER shell contract is duplicated per page the way `#keyhelp` was, with the command that establishes your answer. The keyhelp block is one instance; the round-6 and round-7 lesson is that such a finding is usually a population, and once it was not -- so check rather than guess either way.

Write the report at `docs/rounds/R10_web.report.md`.
