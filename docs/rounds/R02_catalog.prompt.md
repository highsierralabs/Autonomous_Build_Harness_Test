> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S4-B4; role: module builder (catalog); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 2 = three builders, the cap); tier_escalation: none. Round 2 (wave 2) of the RHACO Corpus Explorer autonomous build; catalog module round 1 of 4; dispatched by the build orchestrator, who is also the integrator.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\catalog -- a git worktree of the workspace repository on branch build/catalog, checked out at commit c96664dd82e19da730cbd741f0e2ace43501121d (the correction-boundary commit: corpus_adapter, diagnostics, and the probe framework are merged and green -- 52+ tests, ruff clean, L1 PASS). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\catalog; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/catalog -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: explorer/catalog/** (service.py, routes.py, templates/catalog/*.html, and, if you need one, static/catalog.css served under /static/catalog/ by the app factory) and tests/catalog/**; plus your round report docs/rounds/R02_catalog.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, explorer/web/** (base.html, app.css, app.js -- the web builder's, wave 3), explorer/corpus_adapter/**, explorer/diagnostics/**, tools/**, fixtures/**, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, docs/probe-qualification/**, or any other module's directory or tests. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through the adapter (explorer.corpus_adapter.CorpusAdapter, which alone calls RHACO_corpus_index.connect()); never open the db any other way, never import RHACO_corpus_index or RHACO_tool_catalog_librarian yourself (the adapter is the only importer, ARCHITECTURE.md section 2); C:\RHACO\rhaco\RHACO_corpus_index.py and C:\RHACO\tools\RHACO_tool_catalog_librarian.py (read-only, for understanding); C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; never name or call the five index-writing functions listed in CONSTRAINTS.md S6 (your L1 check greps for them; importing `build` from fixtures.build_fixture_index in tests is allowed and is how the probe tests do it).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use fastapi.testclient.TestClient in-process against explorer.app.create_app(settings). Never launch uvicorn (the probe is the only sanctioned launcher). No run_in_background, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/catalog -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file; templates preserve every corpus character verbatim (CONSTRAINTS.md O19, O28).
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R02_catalog.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Conventions every wave-2 page follows (ARCHITECTURE.md A14, A19; the probe reads exactly these):
- Server-rendered HTML page plus a JSON twin under /api/... built with dataclasses.asdict over the same view object, so the probe can cross-check the page against the API.
- The template extends base.html and sets `{% block html_attrs %}` to `data-mode="<value>"` (and `data-vector="available|unavailable"` where a retrieval call ran); every rendered result or list row carries `data-doc-id="<doc_id>"` and, where the row is one card among several for the same id, `data-card-ref="<card_ref>"`; the open document's region carries `data-selected-doc-id="<doc_id>"`.
- The adapter is reached only through explorer.app.get_adapter(request); when it raises RuntimeError, the HTML page renders a visible notice with HTTP 503 and the API returns 503 {"error": "corpus_adapter not built"} (the diagnostics precedent, explorer/diagnostics/routes.py).
- explorer.app.ADAPTER_ABSENT is the sentinel a test places on app.state.adapter to exercise that path (ARCHITECTURE.md A20).
- status and lifecycle_state are never joined, substituted, or merged into one control or one cell (CONSTRAINTS.md S4); a frozen status (Superseded, Archived -- CardRow.frozen) is marked with the visible text "frozen" next to it, never by colour alone.
- The authority notice is already rendered by base.html's footer; do not restate rank as correctness anywhere (S8).
- Tests: build a fixture index once per test session into tmp_path_factory via `from fixtures.build_fixture_index import build` (docs root = the absolute path of fixtures/corpus in your worktree; the fixture inventory is in docs/rounds/R01_probe.report.md), construct the app with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture docs root>))`, and drive it with TestClient -- every functional assertion is against that deterministic fixture. Add at least one read-only smoke test against the live index (Settings() defaults) asserting only shape (HTTP 200 and non-empty), guarded by the same before/after `corpus_cards_sha` + mtime check tests/corpus_adapter/conftest.py uses (copy that fixture's approach into your own conftest; do not import theirs). Use a hand-written fake adapter only for states the fixture cannot produce (vector channel unavailable; adapter absent).

Read first, inside your worktree, in this order: ARCHITECTURE.md (all of it; section 4.2 in detail; section 4.1 for the adapter contract you call; A3, A5, A14, A19, A20), CONSTRAINTS.md (S4, S8, O5, O7, O14, O15, O21, O24, O27), docs/REFERENCE.md consequence 4, explorer/models.py (CardRow, CatalogFilters, CatalogPage, FacetValues, IndexMeta), explorer/config.py, explorer/app.py, explorer/corpus_adapter/adapter.py (catalog, facets, index_meta, cards_for_doc_id) and explorer/corpus_adapter/sql.py (parse_sort, build_catalog_where -- the whitelist you expose), explorer/diagnostics/routes.py and explorer/diagnostics/templates/diagnostics/diagnostics.html (the shape to follow), explorer/web/templates/base.html and explorer/web/static/app.css (what exists; you may add explorer/catalog/static/catalog.css), docs/rounds/R01_probe.report.md (fixture inventory), docs/rounds/R00_I2_card_schema_fields.report.md section 1 (card fields). Then the task.

Task B4 -- build explorer/catalog: the home / browse surface (PROMPT.md 5.1: "What is in the RHACO corpus?").

Deliver:
1. explorer/catalog/service.py: `build_view(adapter, settings, query: dict) -> CatalogView` (a dataclass you define) that parses the request's query parameters into `CatalogFilters` (doc_type, status, lifecycle_state, program, tag, project_knowledge, date_from, date_to, path_prefix -- empty strings become None; nothing else is accepted), `sort` (passed through to the adapter as `<column>[:asc|desc]`; the adapter whitelists date / doc_id / title / status and defaults to date desc), `page` (>= 1) and `page_size` (default settings.page_size, capped at 200); calls `adapter.catalog(filters, sort, page, page_size)` for the rows and the under-filter facet counts, `adapter.facets()` for the global control vocabularies (doc types, statuses, lifecycle states, programs, tags capped at the adapter's limit, project_knowledge), and `adapter.index_meta()` for the total indexed card count and the db path; computes page count and prev/next page numbers. Filtering never fabricates values: every control's options come from the index's distinct values (CONSTRAINTS.md O14), and a filter value that matches nothing yields an empty list with a visible "no cards match" line, never a fallback.
2. explorer/catalog/routes.py with register(app): `GET /` (HTML, template catalog/catalog.html extending base.html) and `GET /api/catalog` (JSON twin: dataclasses.asdict(view)). Page contents, each labelled: the total indexed cards (IndexMeta live count) and the count matching the current filter; the filter form (GET, method get, every control a labelled <select> or <input>; a "Clear filters" link); the `lifecycle_state` control is a SEPARATE control labelled "CMP lifecycle state (campaigns only)" and is rendered only when the doc_type filter is CMP or the under-filter facets report at least one lifecycle_state value; a sort control (date, doc_id, title, status; asc/desc) with the active sort shown; the sortable list as a table with headers: title, canonical id (doc_id) and doc_filename, doc_type, date, status (+ the text "frozen" for Superseded / Archived), programs, lifecycle_state cell (CMP rows only; blank otherwise; a non-CMP row carrying a value shows it with the text "informational (non-CMP)"), compact abstract (first 200 characters of `abstract`, then a visible " ..." marker; the full abstract is on the reader page), and the open action: a link to `/doc/<card_ref>` (card_ref is the CardRow.card_ref relative POSIX path). Every row element carries `data-doc-id`, `data-card-ref`, `data-status="<status>"`, and `data-lifecycle-state="<value or empty>"` so the probe can assert W4 without scraping. `<html data-mode="catalog">`. Pagination links preserve the filters. The JSON twin carries the filters in force, sort, page, page_size, total, the rows (every CardRow field plus card_ref / doc_ref / frozen), the facets, and the index totals.
3. Template: explorer/catalog/templates/catalog/catalog.html; accessible (labels for every control, table headers with scope, a `<caption>`); the hidden `#keyhelp` element as in diagnostics; no external asset; if you add CSS put it in explorer/catalog/static/catalog.css and link it as /static/catalog/catalog.css (the app factory mounts each module's static directory automatically -- verify in explorer/app.py).
4. tests/catalog/ (TestClient; fixture index built into tmp_path per the conventions above): `/` 200 lists every fixture card outside docs_archive (7 rows) with `data-doc-id` on each row and the total count; `?doc_type=CMP` returns exactly the fixture CMP row with its `lifecycle_state` cell populated and the lifecycle control rendered, and `?doc_type=HND` returns the two HND rows (the hand and its amendment share one doc_id -- both listed, distinct card_ref) with an empty lifecycle cell and the lifecycle control absent when the facets carry no lifecycle value; `?status=Superseded` returns the v1_0 reference row marked "frozen"; `?program=<a fixture program>` and `?tag=<a fixture tag>` narrow correctly; `?sort=title:asc` orders by title; `?date_from=` / `?date_to=` bound the set; a filter value present in no card yields zero rows and the "no cards match" text (no fabrication); pagination with `page_size=3` gives three pages whose rows partition the seven; `/api/catalog` returns the same total, rows and facets as the page for the same query; the adapter-absent sentinel -> 503 with the notice on the page and the JSON error on the API; a live-index smoke test: `/` 200 with a positive total, guarded by the corpus_cards_sha + mtime check. Status and lifecycle_state never appear merged: assert both `data-status` and `data-lifecycle-state` attributes exist on rows and that the CMP row's lifecycle value is not in its status cell.
Write the report at docs/rounds/R02_catalog.report.md.
