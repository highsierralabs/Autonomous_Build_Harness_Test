> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S6-C3; role: module builder (corpus_adapter), criterion-6 hard-floor round; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 3 = three builders, the cap); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1, 2026-09-05: the orchestrator identity pin is amended from session 6; recorded on every dispatch from here). Round 3 (wave 3) of the RHACO Corpus Explorer autonomous build; corpus_adapter module round 3 of 4 -- this strand consumes one builder round of the 28. Dispatched by the build orchestrator, who is also the integrator, under the Director's ruling of 2026-09-05: the second instance of the CONSTRAINTS.md O10 create-on-open hazard is a HARD FLOOR before the FINAL probe suite -- it is fixed in the next adapter round, and if no adapter round would otherwise arise, one is spent on it. This is that round, and that reading stands.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\corpus_adapter -- a git worktree of the workspace repository on branch build/corpus_adapter, checked out at commit 6a8353705a2933cdd4696391da904ed3c680b398 (the round-2 boundary; your round-1 and round-2 work is merged there; 181 tests pass). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\corpus_adapter; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/corpus_adapter -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `explorer/corpus_adapter/**` and `tests/corpus_adapter/**`; plus your round report `docs/rounds/R03_corpus_adapter.report.md`. Do not modify `explorer/app.py`, `explorer/config.py`, `explorer/models.py`, `explorer/faults.py`, `explorer/catalog/**`, `explorer/search/**`, `explorer/reader/**`, `explorer/diagnostics/**`, `explorer/web/**`, `explorer/lineage/**`, `tools/**`, `fixtures/**`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, `docs/probe-qualification/**`, or any other module's directory or tests. If you need a change there, record it in a "Change requests" section and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db, and only through `RHACO_corpus_index.connect()`, never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond that list). **Never write to the live index**; never edit the RHACO module; never call the five index-writing functions named in CONSTRAINTS.md S6 (your L1 check greps for them).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use `fastapi.testclient.TestClient` in-process, or call the adapter directly. Never launch uvicorn -- the probe is the only sanctioned launcher. No `run_in_background`, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests/corpus_adapter -q` green, then `...\.venv\Scripts\python.exe -m pytest tests -q` green as well, because the adapter sits under every other module; (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in any script you write; UTF-8 with LF line endings.
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report, the same text saved to `docs/rounds/R03_corpus_adapter.report.md`, with the sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, in this order: CONSTRAINTS.md O10 (the create-on-open hazard and its design consequence) and O4 (the vector-availability procedure this file implements); ARCHITECTURE.md section 4.1 (the `connect` line and the "Adapter rules" paragraph) and decision records A4 and A9; `explorer/corpus_adapter/availability.py` in full -- your round-1 module: `probe()`, `PROBE_BUDGET_S`, `CACHE_TTL_S`, `_result()`; `explorer/corpus_adapter/adapter.py` lines 110-160 (`_require_db_exists`, `__init__`, `connect` -- your round-2 guard) and lines 197-203 (`vector_availability`, the only caller of `availability.probe`); `tests/corpus_adapter/conftest.py` and `tests/corpus_adapter/test_connect_guard.py` (your round-2 tests and the session-scoped live-db no-mutation fixture); `docs/rounds/R01C_corpus_adapter.report.md`. Then the task.

Task C3 -- close the second instance of the O10 create-on-open hazard: `availability.probe()` opens the database directly, bypassing the adapter's per-call guard.

**The hazard, precisely.** Round 2 made `CorpusAdapter.connect()` re-check `os.path.isfile(self.db_path)` on every call, so the adapter can never re-create a removed live index. But `explorer/corpus_adapter/availability.py` line 49 calls `rhaco_index.connect(db_path)` **directly**, not through `CorpusAdapter.connect()`, and that line is reached on every hybrid or graph search and on every diagnostics page load, by way of `adapter.vector_availability()` -> `availability.probe(self._rhaco_index, self.db_path)` at `adapter.py` line 201. `RHACO_corpus_index.connect()` creates the parent directory and the database file when absent and applies its DDL idempotently (CONSTRAINTS.md O10; module lines 779-799). So if the live index were absent or mid-replacement -- exactly the external-actor scenario O10 records, and which the librarian gate and the chat-side MCP writes demonstrate really happens during this build -- one hybrid search would have the explorer write a fresh, empty `corpus_index.db` at `C:\RHACO\index\corpus_index.db`. That is a build write to the live index, forbidden by DISPATCH_PARAMETERS.md items B and C and by criterion 6 of RHACO-CMP-20260903-001. The guard must hold on every path that opens the database, not only on `CorpusAdapter.connect()`.

Deliver:

1. `explorer/corpus_adapter/availability.py`: make it impossible for `probe()` to create a database. Require `os.path.isfile(db_path)` **before** `rhaco_index.connect(db_path)` is ever called, and on failure return the existing typed outcome `VectorAvailability(available=False, reason="db_unavailable", ...)` with a `detail` naming the path and citing O10 ("the adapter never creates a database"). `probe()`'s contract is "never raises; every failure path is reported through `VectorAvailability.reason`", and this round keeps that contract exactly: do not introduce a raise here. The reason vocabulary in `explorer/models.py`'s `VectorAvailability` already contains `db_unavailable`, so no model change and no change request is needed. Keep the `VEC_DISABLE_ENV` short-circuit first, as now, so a deliberately disabled channel still reports `disabled_by_env` rather than a missing database. Update the module docstring and the `probe()` docstring to state the guard accurately.
2. Review the rest of `explorer/corpus_adapter/**` for any other path that reaches `RHACO_corpus_index.connect` -- or any other db-opening call -- without the isfile guard, and close each one the same way. If there is none, say so explicitly and give the command that establishes it (for example a grep for `connect(` across `explorer/`), so "this was the last instance" is evidenced rather than asserted. Change nothing else: this round is the guard only. In particular do **not** normalise `search_lexical`'s `doc_type_filter` argument shape (CONSTRAINTS.md O29): the Director has ruled O29 a RHACO module finding for a Director-side CHG, not a build item, and `explorer/search/service.py` already wraps the single value. Touching it here would be an unrequested contract change.
3. Tests in `tests/corpus_adapter/`, extending `test_connect_guard.py` or in a sibling file:
   - **the hazard test**: construct a `CorpusAdapter` against a temporary database file under `tmp_path` (an empty file suffices for construction), delete the file, call `adapter.vector_availability()`, and assert it returns `available=False` with `reason == "db_unavailable"` **and** that `os.listdir(tmp_path) == []` afterwards -- nothing created: no database, no `-journal`, no `-wal`. Say in the report that this is the test that would have failed before this round;
   - **the guard-is-not-a-break test**: `vector_availability()` against the live index, through the adapter, read-only, still returns a `VectorAvailability` whose `reason` is one of the documented values, with the live database's `corpus_cards_sha` and mtime unchanged across it;
   - **the short-circuit test**: call `availability.probe(fake_module, missing_path)` directly with a hand-written fake `rhaco_index` whose `connect` raises `AssertionError("connect must not be called")`, proving the guard short-circuits **before** `connect` is reached rather than merely catching its failure afterwards;
   - the existing `tests/corpus_adapter` suite stays green (17 at your base commit; expect 20 with the three above). The session-scoped live-db no-mutation fixture in `conftest.py` stays in force: print its `[criterion-6] live db BEFORE:` and `AFTER:` lines in the report as this round's criterion-6 evidence.
4. In "Result" state the module round consumed (**corpus_adapter round 3 of 4**). In "Unresolved uncertainty" state whether any db-opening path outside `explorer/corpus_adapter/` remains unguarded -- for instance in `tools/` or `fixtures/` -- with the evidence for your answer, because the integrator needs to know whether the O10 class is now closed workspace-wide or only inside the adapter.

Write the report at `docs/rounds/R03_corpus_adapter.report.md`.
