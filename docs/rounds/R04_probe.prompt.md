> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S6-B10; role: module builder (probe), Q3 lineage cases and the FINAL suite; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: sequential (dispatched alone after the wave-3 merge; one strand live); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1). Round 4 of the RHACO Corpus Explorer autonomous build; **probe module round 4 of 4 -- the LAST round this module gets**. Dispatched by the build orchestrator, who is also the integrator, under Director ruling AC-3 (progressive qualification) and the Director's ruling of 2026-09-05 item 4.

**Read this budget statement before you plan anything.** The probe's per-module budget is four rounds and you are round four. The Director has ruled it **NOT waived**: there is no round five. Any probe need that emerges after you finish is the prompt's `BOUNDED_FAIL`, never a lowered bar (PROMPT.md Hard Budgets: "Do not lower acceptance criteria because a round budget is exhausted"). So: implement completely, and if something cannot be done, say so precisely rather than approximating it. **Never weaken an assertion, a fault, or a preset to make a run pass.** A recorded FAIL with the missing observable named is worth more to this build than a green run that proves nothing -- that is the entire reason this instrument exists.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe -- a git worktree of the workspace repository on branch build/probe. It currently sits at your round-3 commit `a0dacd7`; **your first git act is to bring it to the round-3 boundary**: `git fetch` is not needed (same repo), run `git -C C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe merge --ff-only d9e71adb3c2f791cf0f6d94321e8d524843baa62` -- if that is not a fast-forward, STOP and report BLOCKED with the actual `git log --oneline -1` of both. At that commit the lineage and web modules are merged, the keyhelp contract test exists, and the suite is 247 tests. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe. Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q`. **Always `cd` explicitly**: the outer checkout contains a same-named `explorer` package and Python puts the current directory first on `sys.path`, which silently resolved the wrong package for another strand this wave.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `tools/probe_corpus_explorer.py`, `fixtures/**`, `tests/probe/**`, `docs/probe-qualification/**`; plus your round report `docs/rounds/R04_probe.report.md`. Do not modify `explorer/**` (any module), `tools/l1_index_write_check.py`, `tools/l4_gold_oracle.py`, `tests/` other than `tests/probe/`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, or another module's directory or tests. Do not edit, rename or delete any existing run directory under `docs/probe-qualification/runs/`. If a page lacks an attribute or an API field you need, do NOT scrape around it: record a change request and mark that assertion UNVERIFIED.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus -- production presets compare page text against these files); C:\RHACO\index\corpus_index.db, and ONLY through the explorer server your probe launches (never open it yourself, never call `RHACO_corpus_index.connect()` in this round); C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\. Never write to the live index. The five index-writing names may appear in exactly one file you own, `fixtures/build_fixture_index.py`, and only `reindex_full` may be called there, only against a fixture db path after its refusal assertion.
3. Processes: only the venv interpreter running workspace code. **Your probe is the ONLY sanctioned launcher of the explorer server** (CONSTRAINTS O12): it spawns `<venv python> -m uvicorn explorer.app:create_app --factory --host 127.0.0.1 --port N` as a foreground child of its own process, waits on `/healthz`, and tears it down in a `finally`. Do not launch uvicorn by any other route, not even a throwaway script to inspect an endpoint -- a prior probe round did exactly that and it is a recorded strand deviation. If you need to see an `/api/*` shape, add a temporary preset and run the probe, or read the route source. No `run_in_background`, no detached processes, no sleep-and-poll beyond the probe's own bounded readiness wait.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge except the single `--ff-only` above. Leave the worktree clean and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests -q` green (the WHOLE suite; 247 at your base commit, and your new probe tests on top); (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths, line numbers and run directories; every "tested" claim names the test or the run; anything not verified is UNVERIFIED. ASCII-only stdout; `PYTHONIOENCODING=utf-8` for child processes; UTF-8 with LF line endings.
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report, saved to `docs/rounds/R04_probe.report.md`, sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, after the fast-forward: `tools/probe_corpus_explorer.py` in full (your round-3 framework: `Session`, `evaluate_page`, `_collect_basic_reasons`, `_finish_workflow`, `_touched_presets`, `derive_qualification`, the ledger, `PRESET_REGISTRY`, `FAULT_CLASS_TABLE`, `required_classes`); `docs/probe-qualification/README.md` and `qualification_ledger.json`; `docs/rounds/R02_probe.report.md`; then the NEW surfaces: `explorer/lineage/routes.py`, `explorer/lineage/service.py`, `explorer/lineage/templates/lineage/lineage.html`, and `docs/rounds/R03_lineage.report.md`; plus `explorer/web/static/app.js`, `explorer/web/templates/base.html` and `docs/rounds/R03_web.report.md`; and `explorer/faults.py`. Then the task.

**The lineage contract you will drive** (from the merged module; verify each against the source before relying on it):
- Routes `GET /lineage/{card_ref:path}` with optional `?relation=` and `?hops=1|2`, and the twin `GET /api/lineage/{card_ref:path}` = `dataclasses.asdict(LineageView)`.
- `LineageView` fields: `card_ref, doc_id, center, relation, relation_options, hops, outgoing, incoming, unresolved, has_relationships, superseded_banner, chain, subtree, graph_nodes, graph_edges, graph_truncated, reasoning_trail`.
- Each edge (`LineageEdgeView`): `from_id, to_id, relation, direction_label, direction` (`outgoing|incoming|unresolved`), `resolved, note, hop, anchor_card_ref, from_card_refs, to_card_refs, target_card_refs`.
- DOM: `<html data-mode="lineage">`; `#lineage-center` carries `data-selected-doc-id` and `data-card-ref`; `.edge-list li` carries `data-relation`, `data-edge-from`, `data-edge-to`, `data-edge-direction`; the `.lineage-graph` `<svg>`'s `<line>` elements carry the same four; `.reasoning-trail li` carries `data-doc-id`, `data-card-ref`, `data-relation`, `data-edge-direction`; `.supersession-chain` entries carry `data-chain-head`; the superseded banner is marked `data-superseded-banner`; `.subtree-nodes` entries carry `data-depth`; the page carries `data-lineage-state`.

Task B10 -- implement Q3 (KB3, W5, W6 and the known-good typed-edge-direction case), run the FINAL suite, and refresh the production acceptance set.

Deliver:

1. **KB3 (`--fault reverse_edges`, fixture).** The last unqualified known-bad class. `explorer/faults.py` swaps an edge's `from_id`/`to_id` inside `CorpusAdapter.edges_for`, so the lineage page renders the reversed direction faithfully. Add a pure `_kb3_verdict(...)` predicate beside the other attribution rules, fed by the runner's own DOM and `/api/lineage` observations, and unit-test it. Use the frozen fixture fact as the known-correct expectation rather than a second server run: on `/lineage/reports/RHACO-CMP-20260115-001_Fixture_Campaign.card.yaml` the campaign_child edge to `RHACO-ANL-20260115-002` must be `data-edge-from="RHACO-CMP-20260115-001"`, `data-edge-to="RHACO-ANL-20260115-002"`, `data-edge-direction="outgoing"`, label "has campaign child". Under the fault, from and to are swapped and the direction flips. FAIL with `fault_detected=true`, `fault_class="KB3"` when the observed triple contradicts the known-correct one; state in the JSON exactly which observable carried the detection, and assert the graph `<line>` and the `.edge-list` row agree with each other.
2. **KG extension -- the Q3 known-good half.** Extend the existing `KG` runner (fixture, no fault) with the typed-edge-direction case AC-3 names: on the same CMP page the two campaign_child edges point at the ANL and the HND with `direction="outgoing"`; on `/lineage/handoffs/RHACO-HND-20260115-003_Fixture_Handoff_Amendment_A1.card.yaml` the `amends` edge carries `data-edge-from` equal to the amendment's filename stem and `data-edge-to="RHACO-HND-20260115-003"`; on `/lineage/reference/RHACO_Fixture_Reference_v1_0.card.yaml` the `supersedes` edge points to `v1_1`, the chain marks `v1_1` as head, and the superseded banner is present; the HND's unresolved `cites` to `RHACO-ANL-20260101-099` renders with `data-edge-direction="unresolved"` and its marker. Page and `/api/lineage` must agree on every one.
3. **W5 -- reasoning lineage (production).** Centre on `RHACO-CMP-20260903-001` (this build's own campaign; its lineage is dense and stable). Expected: typed directional edges rendered, including `campaign_child` edges to its members with `direction="outgoing"`; every edge's `direction_label` matches its relation per `explorer/models.py` `RELATION_LABELS`; the reasoning trail is chronological by card date; the page and `/api/lineage` agree on the full edge set. **Independent check:** for at least two resolved edges, read the relevant `.card.yaml` from `C:\RHACO\docs` yourself and confirm the rendered relation corresponds to a relation actually declared in the card (for example a `depends_on` entry behind a `cites` edge, or a `parent` behind a `campaign_child`). If the card YAML does not make a given relation derivable from disk, say so and mark that specific assertion UNVERIFIED rather than inventing a correspondence. Do not open the index to check.
4. **W6 -- supersession and amendment (production).** Two centres. (a) Supersession: `RHACO_Card_YAML_Schema_Specification_v1_8`, whose card is `Superseded` -- expect the superseded banner marked `data-superseded-banner`, the supersession chain rendered with `RHACO_Card_YAML_Schema_Specification_v1_9` carrying `data-chain-head="true"` and the visible text "current head", the historical v1_8 document itself still openable and readable through the reader (the historical artifact remains visible -- PROMPT.md W6), and the replacement relationship clear. (b) Amendment: the A1 amendment card of `RHACO-HND-20260903-001` -- expect its outgoing `amends` edge to the parent id, resolved and linked. **Known, dispositioned limitation you must RECORD and must NOT fail on:** on the PARENT's own lineage page the incoming `amends` edge renders as typed directional text with no link, because an `amends` edge's `from_id` is a filename stem which the adapter resolves through `cards_for_doc_id` (SCOPE.md row, round-3 change request from lineage). Visit the parent page too, record the observation in the preset JSON as an explicit `known_limitation` field naming the SCOPE row, and assert only what the SCOPE row says still holds: the relationship is present, typed, directional, and NOT marked unresolved.
5. **FINAL suite (AC-3's last gate).** One fixture run exercising the complete known-good set plus all five known-bad classes, and the production acceptance set W1-W10 with `healthz` and `diagnostics`, so that every preset in `PRESET_REGISTRY` is real and exercised. After this round there must be no `not_implemented_yet` preset left. The qualification ledger must hold **KB1, KB2, KB3, KB4, KB5**. Refresh the production runs at this commit (the earlier ones predate the lineage and web modules): W1, W2, W3, W4, W5, W6, W7 (`--disable-vec`), W8, W9, W10, healthz, diagnostics. Record `db_meta_before` / `db_meta_after` on every production run, read through `/api/diagnostics`, never by opening the db; the live index must be byte-identical before and after every run.
6. **W10 must now cover the new surfaces.** Extend the composite no-LLM workflow to include a lineage navigation step and to assert that every recorded browser request is on 127.0.0.1.
7. `tests/probe/`: unit tests for `_kb3_verdict` over synthetic observations (correct direction, swapped direction, missing edge), plus a subprocess run of `--fault reverse_edges --preset KB3` against a tmp fixture db with a tmp `--ledger`, asserting `QUALIFICATION_PASS` and that the tmp ledger gains KB3. Keep every existing probe test green. Update the module docstring VERSION HISTORY (v1.3) and `docs/probe-qualification/README.md`: the preset-reality table, the Q3 rows, the FINAL row, and the per-preset `required_classes` for W5 and W6.

In "Result" state the module round consumed (**probe round 4 of 4, the last**), the ledger's final class set, and whether the FINAL suite is complete. In "Unresolved uncertainty" list every assertion left UNVERIFIED with its reason, and state plainly whether anything remains that would need a fifth probe round -- the integrator must know that, because there is no fifth round and it would be reported as `BOUNDED_FAIL`.

Write the report at `docs/rounds/R04_probe.report.md`.
