> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S6-B8; role: module builder (lineage); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 3 = three builders, the cap); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1, 2026-09-05: the orchestrator identity pin is amended from session 6; recorded on every dispatch from here). Round 3 (wave 3) of the RHACO Corpus Explorer autonomous build; lineage module round 1 of 4 -- this strand consumes one builder round of the 28. Dispatched by the build orchestrator, who is also the integrator, under Director ruling AC-3 (criterion 3 read progressively): this wave makes the Q3 lineage surface observable, so the probe's KB3 case (reversed relation direction) and its known-good typed-edge-direction case can be qualified in probe round 4.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\lineage -- a git worktree of the workspace repository on branch build/lineage, checked out at commit 6a8353705a2933cdd4696391da904ed3c680b398 (the round-2 boundary: corpus_adapter, catalog, search, reader, diagnostics, the web base and the probe framework are all merged and green there; 181 tests pass). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\lineage; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/lineage -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: explorer/lineage/** (service.py, routes.py, templates/lineage/*.html, and, if you need one, static/lineage.css served under /static/lineage/ by the app factory) and tests/lineage/**; plus your round report docs/rounds/R02_lineage.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, explorer/web/** (base.html, app.css, app.js -- the web builder's, wave 3), explorer/corpus_adapter/**, explorer/diagnostics/**, tools/**, fixtures/**, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, docs/probe-qualification/**, or any other module's directory or tests. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through the adapter (explorer.corpus_adapter.CorpusAdapter, which alone calls RHACO_corpus_index.connect()); never open the db any other way, never import RHACO_corpus_index or RHACO_tool_catalog_librarian yourself (the adapter is the only importer, ARCHITECTURE.md section 2); C:\RHACO\rhaco\RHACO_corpus_index.py and C:\RHACO\tools\RHACO_tool_catalog_librarian.py (read-only, for understanding); C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; never name or call the five index-writing functions listed in CONSTRAINTS.md S6 (your L1 check greps for them; importing `build` from fixtures.build_fixture_index in tests is allowed and is how the probe tests do it).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use fastapi.testclient.TestClient in-process against explorer.app.create_app(settings). Never launch uvicorn (the probe is the only sanctioned launcher). No run_in_background, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/lineage -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file; templates preserve every corpus character verbatim (CONSTRAINTS.md O19, O28).
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R02_lineage.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Conventions every wave-2 page follows (ARCHITECTURE.md A14, A19; the probe reads exactly these):
- Server-rendered HTML page plus a JSON twin under /api/... built with dataclasses.asdict over the same view object, so the probe can cross-check the page against the API.
- The template extends base.html and sets `{% block html_attrs %}` to `data-mode="<value>"` (and `data-vector="available|unavailable"` where a retrieval call ran); every rendered result or list row carries `data-doc-id="<doc_id>"` and, where the row is one card among several for the same id, `data-card-ref="<card_ref>"`; the open document's region carries `data-selected-doc-id="<doc_id>"`.
- The adapter is reached only through explorer.app.get_adapter(request); when it raises RuntimeError, the HTML page renders a visible notice with HTTP 503 and the API returns 503 {"error": "corpus_adapter not built"} (the diagnostics precedent, explorer/diagnostics/routes.py).
- explorer.app.ADAPTER_ABSENT is the sentinel a test places on app.state.adapter to exercise that path (ARCHITECTURE.md A20).
- status and lifecycle_state are never joined, substituted, or merged into one control or one cell (CONSTRAINTS.md S4); a frozen status (Superseded, Archived -- CardRow.frozen) is marked with the visible text "frozen" next to it, never by colour alone.
- The authority notice is already rendered by base.html's footer; do not restate rank as correctness anywhere (S8).
- Tests: build a fixture index once per test session into tmp_path_factory via `from fixtures.build_fixture_index import build` (docs root = the absolute path of fixtures/corpus in your worktree; the fixture inventory is in docs/rounds/R01_probe.report.md), construct the app with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture docs root>))`, and drive it with TestClient -- every functional assertion is against that deterministic fixture. Add at least one read-only smoke test against the live index (Settings() defaults) asserting only shape (HTTP 200 and non-empty), guarded by the same before/after `corpus_cards_sha` + mtime check tests/corpus_adapter/conftest.py uses (copy that fixture's approach into your own conftest; do not import theirs). Use a hand-written fake adapter only for states the fixture cannot produce (vector channel unavailable; adapter absent).

Module-specific addition to rule 1: your one JavaScript file is `explorer/lineage/static/lineage.js`, served at `/static/lineage/lineage.js` (the app factory mounts `/static/lineage` from `explorer/lineage/static/`). It is progressive enhancement ONLY. The page must be completely legible and navigable with JavaScript disabled: the graph is an inline `<svg>` you render server-side, and the reasoning trail is a plain server-rendered list. No `fetch`, no XHR, no external asset, no CDN.

Read first, inside your worktree, in this order: ARCHITECTURE.md (section 4.5 in full; section 4.1 for the adapter contract; A5 card_ref-as-URL-key, A14 page-plus-JSON-twin, A19 the DOM state convention); CONSTRAINTS.md (S1, S4, S8, O1, O7, O8, O13, O22, O25); explorer/models.py (Edge with from_id / to_id / relation / source_field / resolved / direction_label / target_cards / source_cards; EdgeSet with outgoing / incoming / unresolved / chain_head; Subtree with nodes as (CardRow, depth) pairs, counts_by_doc_type, truncated; CardRow; RELATION_LABELS, which maps cites -> "cites", supersedes -> "is superseded by", amends -> "amends", campaign_child -> "has campaign child"); explorer/config.py; explorer/app.py (get_adapter, ADAPTER_ABSENT, the per-module static mounts, register); explorer/faults.py; explorer/corpus_adapter/adapter.py -- in particular `edges_for` (note the `if self.active_fault == "reverse_edges"` swap at line 506, which is the KB3 fault: your page must render whatever direction the adapter returns, faithfully, so the fault becomes VISIBLE; never re-correct it, never normalise it away), `supersession_chain` (returns oldest predecessor first through to the head), `campaign_subtree` (root-first (CardRow, depth) pairs), `dangling_references`, `card`; explorer/reader/routes.py and explorer/reader/templates/reader/reader.html (the reader already renders `<a href="/lineage/<card_ref>">lineage view</a>` and uses `Edge.direction_label`; your route must satisfy that link and your labels must agree with the reader's); explorer/diagnostics/routes.py (the adapter-absent 503 precedent); explorer/web/templates/base.html and static/app.css; docs/rounds/R01_probe.report.md, the fixture inventory table. Then the task.

Fixture facts you will need (from that inventory, verified when the fixture was built): `RHACO-CMP-20260115-001` has two outgoing `campaign_child` edges, to `RHACO-ANL-20260115-002` and `RHACO-HND-20260115-003`. The amendment `RHACO-HND-20260115-003_Fixture_Handoff_Amendment_A1` has one `amends` edge whose `from_id` is its own filename stem and whose `to_id` is `RHACO-HND-20260115-003`. The reference pair has one resolved `supersedes` edge from `RHACO_Fixture_Reference_v1_0` to `RHACO_Fixture_Reference_v1_1`. The HND carries one UNRESOLVED `cites` edge to the deliberately absent `RHACO-ANL-20260101-099`.

Task B8 -- build explorer/lineage: the relationship and reasoning-navigation surface (PROMPT.md 5.5, "How did this work develop, and what reasoning artifacts connect it?").

Deliver:

1. `explorer/lineage/service.py`: `build_view(adapter, settings, card_ref, relation, hops) -> LineageView | None` (dataclasses you define; None when `adapter.card(card_ref)` returns None). Selected-document-centered. Everything comes from the adapter; nothing is inferred (PROMPT.md invariant: "Never infer a relationship not recorded in RHACO data"). The view carries:
   - the center `CardRow`;
   - the typed edges from `adapter.edges_for(center)`: outgoing, incoming and unresolved, each rendered directionally using `Edge.direction_label` from `explorer/models.py` (do not re-derive labels). An unresolved edge, which only `cites` can be, shows the literal `to_id` marked "unresolved" and is never dropped, guessed at, or resolved;
   - a relation filter (`?relation=cites|supersedes|amends|campaign_child`) whose control lists only the relations actually present around the center, with counts;
   - one hop by default, `?hops=1|2` for controlled expansion (hops=2 expands each resolved neighbour's own `edges_for` one further step). Cap the rendered node count at 200 and set a `truncated` flag when the cap bites, mirroring the adapter's cap style;
   - the supersession chain from `adapter.supersession_chain(center.doc_id)`, shown whenever it has more than one member, with the LAST element marked "current head" (O22);
   - the campaign subtree from `adapter.campaign_subtree(center.doc_id, depth=1)` for a CMP center only, with `counts_by_doc_type` and `truncated`;
   - a "reasoning trail": the connected cards in scope, ordered chronologically by `CardRow.date` ascending, undated cards last and stably ordered, each linking to `/doc/<card_ref>`. This is the chronological fallback PROMPT.md 5.5 requires and it must work with JavaScript disabled.

2. `explorer/lineage/routes.py` with `register(app)`: `GET /lineage/{card_ref:path}` (HTML, template `lineage/lineage.html` extending base.html, optional `?relation=` and `?hops=`) and `GET /api/lineage/{card_ref:path}` (the JSON twin via `dataclasses.asdict`, carrying the center card, every edge as `{from_id, to_id, relation, direction_label, direction, resolved, target_card_refs}` where `direction` is `"outgoing" | "incoming" | "unresolved"` relative to the center, the supersession chain with the head flagged, the campaign subtree, the active relation filter and hops). An unknown card_ref, a `docs_archive` card_ref, or a `..` traversal returns HTTP 404 with a visible page notice ("no card at <card_ref> in the index") and `{"error": "card not found"}` on the API. Adapter-absent returns 503 on both, following `explorer/diagnostics/routes.py`.

3. Template `explorer/lineage/templates/lineage/lineage.html` extending base.html:
   - `{% block html_attrs %}` sets `data-mode="lineage"`;
   - the center region carries `data-selected-doc-id` and `data-card-ref`;
   - **Graph view**: a server-rendered inline `<svg>` with the center node, one node per in-scope related card, and one edge element per edge carrying an arrowhead marker that points from the edge's `from` to its `to`, so the drawn direction matches the data. Each node carries `data-doc-id` and `data-card-ref`; **each edge element carries `data-relation`, `data-edge-from`, `data-edge-to` and `data-edge-direction`** -- this is the contract the probe reads in round 4 to detect KB3, so it must reflect the adapter's returned from/to exactly. Nodes are wrapped in `<a href="/doc/<card_ref>">` so click-through works without JavaScript;
   - **Reasoning trail**: the chronological list, each row carrying `data-doc-id`, `data-card-ref`, `data-relation` and `data-edge-direction`, each linking to the reader;
   - **Supersession chain** and **campaign subtree** as their own labelled sections; the chain marks the head with `data-chain-head="true"` and the visible text "current head"; a `Superseded` center shows the banner "This version is superseded -- current head: <link>", matching the reader's O22 banner;
   - unresolved edges listed with the literal id and a visible "unresolved" marker; an explicit "no recorded relationships" state when the center has none;
   - accessible: landmarks, labelled controls, headings in order, the `<svg>` carrying `role="img"` with a `<title>` summarising it and the reasoning trail as its text equivalent; the hidden `#keyhelp` element as on the other pages. `status` and `lifecycle_state` are never joined (S4); nothing anywhere restates a relationship or a rank as correctness (S8).

4. `explorer/lineage/static/lineage.js`: optional, tiny, inert if absent.

5. `tests/lineage/` (TestClient; build the fixture index into `tmp_path` via `from fixtures.build_fixture_index import build`, docs root = `fixtures/corpus` in your worktree, following `tests/search/conftest.py`; never touch `fixtures/fixture_index.db`):
   - the CMP page: 200, `data-mode="lineage"`, `data-selected-doc-id="RHACO-CMP-20260115-001"`, both `campaign_child` edges present as outgoing with the label "has campaign child", and the subtree section listing both children with `counts_by_doc_type`;
   - the amendment page: the `amends` edge with `data-edge-from` equal to the amendment's filename stem and `data-edge-to="RHACO-HND-20260115-003"`;
   - the HND page: the unresolved `cites` edge to `RHACO-ANL-20260101-099` visible with the "unresolved" marker and `data-edge-direction="unresolved"`, plus the incoming `campaign_child` and the incoming `amends`;
   - the reference v1_0 page: the `supersedes` edge to v1_1, the chain listing v1_0 then v1_1 with v1_1 marked `data-chain-head="true"`, and the "This version is superseded" banner;
   - **the KB3 observable**: with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture root>, fault="reverse_edges"))`, assert explicitly that a named edge's `data-edge-from` and `data-edge-to` are SWAPPED relative to the clean render and that `data-edge-direction` flips accordingly. Capture the clean values first and compare against them, so the test states the swap rather than merely asserting a fault is present. The fault is honoured because the db path lies outside the RHACO trees (`explorer/faults.py`);
   - `docs_archive` and `..` card_refs both 404; adapter-absent 503 via the `ADAPTER_ABSENT` sentinel;
   - `/api/lineage/...` parity with the page for edges, directions, chain and subtree;
   - one live-index smoke on `/lineage/reports/RHACO-CMP-20260903-001_Corpus_Explorer_Autonomous_Build_Harness.card.yaml`, asserting shape only (200, non-empty, `data-selected-doc-id` correct, its campaign children present), guarded by the `corpus_cards_sha` plus mtime before/after check copied from `tests/corpus_adapter/conftest.py` into your own conftest (copy the approach, do not import theirs).

Write the report at `docs/rounds/R03_lineage.report.md` with the sections Header, Change, Evidence, Material alternatives, Decisions, Result (state "lineage round 1 of 4"), Unresolved uncertainty, Change requests, Assumptions.
