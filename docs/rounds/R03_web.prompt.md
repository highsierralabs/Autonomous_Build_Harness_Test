> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S6-B9; role: module builder (web -- the shared shell, stylesheet, keyboard navigation and accessibility pass); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 3 = three builders, the cap); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1, 2026-09-05: the orchestrator identity pin is amended from session 6; recorded on every dispatch from here). Round 3 (wave 3) of the RHACO Corpus Explorer autonomous build; web module round 1 of 4 -- this strand consumes one builder round of the 28. Dispatched by the build orchestrator, who is also the integrator.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\web -- a git worktree of the workspace repository on branch build/web, checked out at commit 6a8353705a2933cdd4696391da904ed3c680b398 (the round-2 boundary: corpus_adapter, catalog, search, reader, diagnostics and the probe framework are merged and green there; 181 tests pass). The lineage module is being built concurrently in a separate worktree and is NOT present in yours. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\web; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `explorer/web/**` (`templates/base.html`, new `templates/_partials/*.html`, `static/app.css`, `static/app.js`) and `tests/web/**`; plus your round report `docs/rounds/R03_web.report.md`. Do not modify `explorer/app.py`, `explorer/config.py`, `explorer/models.py`, `explorer/faults.py`, `explorer/corpus_adapter/**`, `explorer/catalog/**`, `explorer/search/**`, `explorer/reader/**`, `explorer/diagnostics/**`, `explorer/lineage/**` (another builder's, this wave), `tools/**`, `fixtures/**`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, `docs/probe-qualification/**`, or any other module's directory or tests. **You own the shared shell, so every other module's page renders through your template and your stylesheet: a change of yours that breaks another module's contract is your defect, not theirs.** If you need a change in a module template, record it in a "Change requests" section (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db, and only through the adapter (`explorer.corpus_adapter.CorpusAdapter`, the sole caller of `RHACO_corpus_index.connect()`) -- never open the db any other way, never import `RHACO_corpus_index` or `RHACO_tool_catalog_librarian` yourself; C:\RHACO\rhaco\RHACO_corpus_index.py and C:\RHACO\tools\RHACO_tool_catalog_librarian.py, read-only, for understanding; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond that list). Never write to the live index; never edit the RHACO module; never name or call the five index-writing functions listed in CONSTRAINTS.md S6.
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use `fastapi.testclient.TestClient` in-process against `explorer.app.create_app(settings)`. Never launch uvicorn -- the probe is the only sanctioned launcher. No `run_in_background`, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests -q` green -- **the WHOLE suite, not just tests/web**, because you own the shell every page renders through. 181 tests pass at your base commit and your round must leave all of them passing; paste the output tail. (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in any script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file; templates preserve every corpus character verbatim (CONSTRAINTS.md O19, O28).
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report, the same text saved to `docs/rounds/R03_web.report.md`, with the sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, in this order: ARCHITECTURE.md (section 4.7 in detail; sections 1 and 2 for ownership; A2 no-HTMX, A14 page-plus-JSON-twin, A19 the DOM state convention, A21 the `head_extra` hook); CONSTRAINTS.md (S1, S4, S5, S8, O11, O19, O28); your seed files `explorer/web/templates/base.html`, `static/app.css`, `static/app.js`, which the integrator wrote and you own from this round; then every consuming template, to learn the class and attribute vocabulary you must style and must not break -- `explorer/catalog/templates/catalog/catalog.html` (`.catalog-filters`, `.table-scroll`, `table`, `.frozen`, `.informational`, `.sr-only`, `.pagination`, `.counts`, `.lede`, `#keyhelp`, and rows carrying `data-doc-id` / `data-card-ref` / `data-status` / `data-lifecycle-state`), `explorer/search/templates/search/search.html` (`.result-card`, `.search-results`, `.identifier-results`, `.mode-badge`, the `.evidence` `<details>`, `.degradation`, `.frozen-marker`), `explorer/reader/templates/reader/reader.html` (`#document`, `#card-panel`, the TOC, the numbered source view with `#L<n>` / `.hit` / `.line-text`, and its body-level `<link rel="stylesheet" href="/static/reader/reader.css">`), `explorer/diagnostics/templates/diagnostics/diagnostics.html` (`#diagnostics-root`, the freshness form, the authority notice); `explorer/app.py` (the per-module static mounts and the `MODULES` order, which fixes template lookup precedence); `explorer/models.py` (`AUTHORITY_NOTICE`, which base.html currently renders through `authority_notice.split('. ', 1)[1]`); PROMPT.md sections 5.1 to 5.6 and section 7 (the critic's ten criteria, which include accessibility and performance sufficient for local interactive use). Then the task.

Task B9 -- build explorer/web: the shared shell, stylesheet, keyboard navigation and accessibility pass that every other page renders through.

Deliver:

1. `explorer/web/templates/base.html`. Keep every existing block name and hook -- `title`, `html_attrs`, `main_attrs`, `head_extra`, `content`, `scripts` -- because merged templates already depend on them and renaming one breaks a module. Within that contract:
   - the primary nav stays **Catalog / Search / Diagnostics**. Do NOT add a bare "Lineage" nav link: that surface is document-scoped (`/lineage/<card_ref>`) and has no index route, so a global link would 404. Record the reasoning as a decision; the reader already links "lineage view" per document;
   - mark the active nav item with `aria-current="page"`, supplied through an existing hook or through a NEW additive block with an empty default. A new block is allowed only if every existing template still renders unchanged without it, and you must prove that with the full suite;
   - keep the skip-link, the `header` / `nav` / `main` / `footer` landmarks, and the footer authority notice. Make the notice robust: the current `authority_notice.split('. ', 1)[1]` silently depends on the sentence shape of `explorer/models.py`'s `AUTHORITY_NOTICE`. Render the sentence PROMPT.md 5.6 requires, bolded lead included, without a fragile split and without editing `models.py`;
   - add an `aria-live="polite"` status region for the keyboard navigation to announce into, empty and invisible until used;
   - `templates/_partials/*.html` are yours to add, but include them only from `base.html` itself: you cannot edit another module's template to include one, so a partial nothing includes is dead code. Prefer none over decoration.

2. `explorer/web/static/app.css`, the real stylesheet pass -- the current file is a 1.8 KB integrator seed. No external asset of any kind: no font CDN, no image URL, no `@import`. Cover:
   - layout for all five page types, including the reader's two regions `#document` and `#card-panel`, which must stay visually and structurally distinct and never collapse into one another (PROMPT.md 5.4); a side-by-side grid that stacks at narrow widths is the obvious shape;
   - wide content scrolls inside its own container and never the page body: the catalog table (`.table-scroll`), the reader source view, and the lineage `<svg>` container all need `overflow-x: auto`;
   - the epistemic distinctions legible at a glance and never by colour alone: `.frozen` / `.frozen-marker`, the `.degradation` banner, the `.informational` non-CMP lifecycle flag, the `.evidence` disclosure, the "from index" labels, the authority notice;
   - `#4B286D` as the accent (O28), a system font stack for body text, high-contrast `:focus-visible` rings on every interactive element, and WCAG AA contrast for body and muted text on both the page and panel backgrounds -- state the ratios you checked and how you checked them;
   - `prefers-reduced-motion` respected if you add any transition, and a sensible `@media print`;
   - do not restyle another module's own stylesheet (`catalog.css`, `reader.css` are theirs). Yours is the base they sit on: avoid selectors that fight theirs, and say in the report which shared selectors you took ownership of.

3. `explorer/web/static/app.js`: vanilla, no framework, no network call, inert when a page has none of its hooks. Keep `/` (focus the search box) and `?` (toggle `#keyhelp`), and add the result-list keyboard navigation ARCHITECTURE.md 4.7 specifies. The rows are exactly the elements carrying `data-doc-id` (A19: catalog `<tr>`, search `<li>`, lineage nodes and trail rows). `j` / `k` and Down / Up move a visible selection, `Enter` opens the selected row's first `<a href>`, `Escape` clears it. The selection must be visible through a class you style in `app.css` and announced through the `aria-live` region, and must never hijack typing in an input, textarea, select or contenteditable. Guard every DOM read so a page with no rows is a no-op. The keys you implement must match the help text the merged pages already show in their `#keyhelp` blocks -- you cannot edit those pages, so if your key list differs you must file a change request naming the exact text each page should carry.

4. `tests/web/` (TestClient against `create_app`; fixture index built into `tmp_path` via `from fixtures.build_fixture_index import build`, docs root = `fixtures/corpus` in your worktree, as `tests/search/conftest.py` does; never `fixtures/fixture_index.db`):
   - the shell renders on every merged page (`/`, `/search?q=fixture`, `/doc/reports/RHACO-ANL-20260115-002_Fixture_Analysis.card.yaml`, `/diagnostics`): 200, skip-link present, the three landmarks present, the three nav links present, `aria-current="page"` on exactly the active one, the authority-notice sentence present verbatim as PROMPT.md 5.6 words it, and both `/static/web/app.css` and `/static/web/app.js` linked;
   - `/static/web/app.css` and `/static/web/app.js` each served 200 with a non-empty body;
   - a block-contract regression guard: every block name other templates rely on still exists in `base.html`, and each merged page's own `data-mode` still reaches the `<html>` tag through `html_attrs`;
   - file-content assertions on `app.js` (no `fetch`, no `XMLHttpRequest`, no external import; binds `j`, `k`, `Enter`, `Escape`, `/`, `?`; early-returns inside form fields) and on `app.css` (no `@import`, no `url(http`, the accent present, `overflow-x` on the wide-content containers) -- label these honestly as file-content assertions in the report, because TestClient runs no JavaScript and applies no CSS;
   - one live-index smoke asserting shape only (`/` returns 200, non-empty, shell present), guarded by the `corpus_cards_sha` plus mtime before/after check copied from `tests/corpus_adapter/conftest.py` into your own conftest.

Be explicit in the report about what your tests can and cannot show. No test in this suite executes JavaScript or lays out CSS, so keyboard navigation and visual legibility are UNVERIFIED at L1 and are claims for the L2 probe (round 4) and the L3 critic to test. Do not describe a file-content assertion as behavioural evidence.

Write the report at `docs/rounds/R03_web.report.md`; in "Result" state "web round 1 of 4" and the full-suite result.
