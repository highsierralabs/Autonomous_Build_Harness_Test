> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S4-B7; role: module builder (probe), wave-2 runners; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: sequential (dispatched alone after the three wave-2 builders returned and merged; one strand live); tier_escalation: none. Round 2 (wave 2) of the RHACO Corpus Explorer autonomous build; probe module round 3 of 4 -- this strand consumes one builder round of the 28. Dispatched by the build orchestrator, who is also the integrator, under Director ruling AC-3 (criterion 3 read progressively: Q1 search surfaces and Q2 reader are qualified in this wave; Q3 lineage in wave 3; FINAL before the critic).

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe -- a git worktree of the workspace repository on branch build/probe, checked out at commit 2d83808630c84b2017426cedc72ba31175e67a49 (main after the wave-2 merge: catalog, search, reader, diagnostics, corpus_adapter and your round-2 probe framework are all present and green). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: tools/probe_corpus_explorer.py, fixtures/** (fixtures/build_fixture_index.py and fixtures/corpus/** -- you may ADD fixture documents and cards if a workflow needs one; never change an existing fixture fact that a test or report relies on), tests/probe/**, and docs/probe-qualification/** (runs, README.md, qualification_ledger.json); plus your round report docs/rounds/R02_probe.report.md. Do not modify explorer/** (any module), requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, or any other module's directory or tests. Do not edit, rename, or delete existing run directories under docs/probe-qualification/runs/. If a page lacks an attribute or an API field you need, do not work around it by scraping: record a change request (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and mark the affected preset's assertion UNVERIFIED in the report.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus -- the production-path presets compare page text against these files); C:\RHACO\index\corpus_index.db -- only through the explorer server the probe launches (never opened by the probe itself, never through RHACO_corpus_index.connect() in this round); C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; the five index-writing functions named in CONSTRAINTS.md S6 may appear in exactly one file you own, fixtures/build_fixture_index.py, and only reindex_full may be called there, only against a fixture db path after the path assertion (CONSTRAINTS O2/O3; ARCHITECTURE A12).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). Your probe is the ONLY sanctioned launcher of the explorer server: it spawns `<venv python> -m uvicorn explorer.app:create_app --factory --host 127.0.0.1 --port N` as a foreground child of its own process, waits on /healthz, and tears it down in a finally block. No run_in_background, no detached processes, no sleep-and-poll loops other than the readiness wait bounded at --timeout inside the probe.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test or the run directory; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file.
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R02_probe.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Read first, inside your worktree, in this order: tools/probe_corpus_explorer.py (all of it -- your round-2 framework: evaluate_page, the qualification-state derivation, the ledger, required_classes), docs/probe-qualification/README.md and qualification_ledger.json, docs/rounds/R01C_probe.report.md; then the surfaces you drive: explorer/catalog/routes.py + service.py + its template, explorer/search/routes.py + service.py + its template, explorer/reader/routes.py + service.py + its template, explorer/diagnostics/routes.py + its template (each page's data-* attributes and its /api twin -- the contracts are in docs/rounds/R02_catalog.report.md, R02_search.report.md, R02_reader.report.md); explorer/faults.py; docs/probe-qualification/gold_v1_1_compat.json (which Gold v1.1 rows hit today -- pick W3 from a row that hits); C:\RHACO\working\eval\gold_queries.yaml (rows 9-14 are S2 title-fragment lexical targets; rows 15-26 S3 semantic); PROMPT.md section 8 (the ten workflows, verbatim expectations). Then the task.

Task B7 -- implement the acceptance-workflow and qualification runners W1-W10, KG, KB1-KB5 under the AC-3 progressive reading; run Q1 and Q2 qualification on the fixture; run the production-path presets on the live index.

Deliver:
1. Replace the not_implemented stubs with real runners, each a page-kind preset (or a short sequence of page navigations recorded as one preset with sub-steps) declaring its URL(s), its assertions on the data-* observations and the /api twin, its required_classes (the table in the README), and its fault-attribution rules. Presets that need the lineage surface (W5, W6; KB3 and its KG edge-direction case) stay `not_implemented` with the reason "lineage lands in wave 3 (Q3)"; a run selecting them is INCOMPLETE by the round-2 rule.
   - KG (fixture, no fault): identifier search `E000777` -> the EVT card row first (`data-doc-id="RHACO-EVT-20260115-004"`); lexical search `quartz lattice lantern` -> the ANL first with `line_no` 15 and an excerpt containing the phrase (page and /api/search agree); reader `/doc/reports/RHACO-ANL-20260115-002_Fixture_Analysis.card.yaml` -> `data-body-sha256` equals the sha256 of that fixture file read from disk, `data-card-index-mismatch=""`, and the /api/doc `lines` equal the file's lines; card panel identity (doc_id, title, status) equals the fixture card file parsed from disk. (The typed-edge-direction case waits for Q3.)
   - KB1 (`--fault wrong_doc_for_id`): identifier search `RHACO-HND-20260115-003` -> under the fault the first listed card_ref is the amendment's instead of the hand's (`handoffs/RHACO-HND-20260115-003_Fixture_Handoff.card.yaml` expected first); also the reader for the hand's card_ref: its `depends_on` links / lineage may resolve to the rotated row. The preset FAILS with fault_detected=True / fault_class KB1 when the observed first card_ref is not the expected one; document exactly which observable carries the detection.
   - KB2 (`--fault stale_card`): reader for the ANL card_ref -> `data-card-index-mismatch` non-empty (title, status) and the displayed index-row status "Draft" / title ending "[STALE]" disagree with the card file the probe reads from disk. FAIL with KB2 when either the page's own mismatch marker is set or the probe's independent file comparison disagrees; record both observations.
   - KB4 (`--fault broken_jump`): reader for the ANL card_ref -> at least one heading `data-line-verified="false"`; independently, for each heading in /api/doc `headings`, the probe checks that `lines[line_no - 1]` begins with a markdown heading marker followed by the heading text; a violation is FAIL with KB4.
   - KB5 (`--fault console_error`): every page-kind preset of the run reports the page error (your round-2 rule; extend the run to the search / catalog / reader pages so KB5 is demonstrated on a wave-2 surface as well as diagnostics).
   - W1 (production path, live db): identifier search `RHACO-CMP-20260903-001` -> exactly one result row with that `data-doc-id`; follow its open link; the reader shows `data-selected-doc-id="RHACO-CMP-20260903-001"` and the card panel's doc_id / doc_type CMP / lifecycle state present; page and /api agree.
   - W2 (production path): lexical search with a Gold v1.1 S2 phrase whose target is a frozen document (row 10 `tungsten putty seal` -> `RHACO-MTN-20260520-001`, or another S2 row you verify from gold_queries.yaml): the target appears among the results with a `line_no` and an excerpt; the probe reads the target document from C:\RHACO\docs (path from /api/search) and confirms the excerpt line is that file's line `line_no` verbatim.
   - W3 (production path): a Gold v1.1 S3 semantic query in hybrid mode chosen from a row that `gold_v1_1_compat.json` records as a hit today; expected: `data-mode="hybrid"` with `data-vector="available"` (if the vector channel is unavailable on this host at run time the preset records the degraded state and is INCOMPLETE for W3, not FAIL -- say so in the JSON), the row's target among the returned ids in the same order the L4 oracle recorded for that row, and every result's evidence carrying the component-rank-not-exposed line.
   - W4 (production path): catalog `/?doc_type=CMP` -> every row has `data-status` and `data-lifecycle-state`, the lifecycle control is rendered, and both filters narrow independently (`&lifecycle_state=<a value from the facets>` and `&status=<a value>` each reduce the set while both attributes remain on every row); the CMP row's lifecycle value never appears inside its status cell.
   - W7 (production path with `--disable-vec`): search hybrid -> `data-mode="hybrid-degraded-lexical"`, the exact notice text, no "Hybrid" active-mode label; diagnostics page `data-mode="hybrid-degraded-lexical"` and `data-vector="unavailable"`; identifier and lexical searches still return results.
   - W8 (fixture): copy fixtures/corpus into a temporary directory, build a fixture index from the copy, then add one more card+document to the copy (a new fixture-dated id) without rebuilding; drive diagnostics and submit the "Check freshness" form (Playwright click; the only POST) -> `data-freshness-status="DRIFT"` and the added id listed; the run records that the explorer reported, and did not repair, the drift.
   - W9 (production path): open the W2 hit's link `/doc/<card_ref>?line=<line_no>` -> `data-jump-line` equals the line, the source line marked `hit` contains the phrase, the "Document" and "Card (catalog metadata)" regions are both present and distinct, `data-selected-doc-id` set, the card panel shows the doc_id.
   - W10 (production path): the composite standard workflow -- catalog home, W1, W2, W4, W9, diagnostics -- in one browser context with every network request recorded; PASS iff every step passes and every recorded request URL is on 127.0.0.1 (no external host; the only model dependency is the server-side local embedder, which the browser never contacts).
   - Fault attribution rules for KB1, KB2, KB4 go in the one attribution table next to PRESET_REGISTRY; `required_classes` per preset stay as the README table; W3 / W7 / W10 record `qualified_failure_classes` coverage through the ledger as the round-2 rule defines.
2. Qualification runs, committed under docs/probe-qualification/runs/<utc stamp>/ (fresh fixture index built first; explicit free `--port` per run; `--ledger` at the default worktree path so the committed ledger gains the classes): Q1/Q2 known-good `--preset KG`; `--fault wrong_doc_for_id --preset KB1`; `--fault stale_card --preset KB2`; `--fault broken_jump --preset KB4`; `--fault console_error --preset KB5` (extended to the wave-2 surfaces); `--preset W8`. Expected: each KB run QUALIFICATION_PASS with its class; KG FRAMEWORK_SMOKE_PASS; the ledger holding KB1, KB2, KB4, KB5 with run paths. If any known-bad case is NOT detected, do not weaken the assertion or the fault: record the run as FAIL, keep the evidence, and state in the report which observable is missing (the integrator dispositions it).
3. Production-path runs on the live index, committed likewise: `--db C:\RHACO\index\corpus_index.db --docs-root C:\RHACO\docs --preset <W1|W2|W3|W4|W9|W10|healthz|diagnostics>` and `--disable-vec --preset W7`. Expected: each is PRODUCT_EVIDENCE_PASS only when the ledger covers its required classes (W1 needs KB1+KB5; W2 KB1+KB2+KB5; W3 KB1+KB5; W4 KB2+KB5; W7 KB5; W9 KB4+KB5; W10 KB1+KB2+KB4+KB5; diagnostics KB5), otherwise FRAMEWORK_SMOKE_PASS with `product_evidence_blocked_by`; a real assertion failure is FAIL. The live db is read only through the server; record in the report the db's mtime before and after your runs (os.stat metadata, read from the diagnostics page's "Database modified" value -- not by opening the file), expecting no change.
4. tests/probe/: unit tests for each new attribution rule over synthetic observations (KB1 first-card_ref mismatch; KB2 mismatch marker and file disagreement; KB4 heading/line disagreement; W10 non-localhost request -> FAIL); a subprocess test running `--preset KG` against a tmp fixture db (free port, tmp `--ledger`) asserting FRAMEWORK_SMOKE_PASS; a subprocess test running `--fault stale_card --preset KB2` asserting QUALIFICATION_PASS and the tmp ledger gaining KB2; the round-2 tests kept green. Update the module docstring VERSION HISTORY (v1.2, 2026-09-04/05: wave-2 runners, Q1/Q2 qualification, production presets) and the README (which presets are real, which wait for Q3).
Write the report at docs/rounds/R02_probe.report.md; in "Result" state the module round consumed (probe round 3 of 4) and the qualification ledger state; in "Unresolved uncertainty" list every preset still not_implemented and every assertion left UNVERIFIED with its reason.
