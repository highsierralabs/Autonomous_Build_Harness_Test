> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S8-B14; role: module builder (search), standing-figure honesty and identifier-mode evidence; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: sequential (one strand live); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1). Round 7 of the RHACO Corpus Explorer autonomous build; search module round 2 of 4 -- this strand consumes one builder round of the 28 (17 consumed before it, 11 left, and the TOTAL is now the binding constraint rather than any per-module figure). Dispatched under RHACO-CCX-20260905-001 section 4 step (2).

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\search -- a git worktree of the workspace repository on branch build/search, checked out at the tip of `main` at dispatch. That tip is a descendant of the round-6 boundary `6aaea9d` and it adds this prompt file itself, so this prompt cannot name its own commit: your first git act is `git rev-parse HEAD` -- RECORD what it returns and report it as your base, do not match it against a literal here. What you can verify is that your HEAD contains the round-6 boundary (`git merge-base --is-ancestor 6aaea9d HEAD`); if it does not, STOP and return status BLOCKED. Your round-1 work is merged there, along with six later rounds; 275 tests pass, tests/search 22 of them. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\search; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/search -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `explorer/search/**` and `tests/search/**`; plus your round report `docs/rounds/R08_search.report.md`. Do not modify `explorer/app.py`, `explorer/config.py`, `explorer/models.py`, `explorer/faults.py`, `explorer/corpus_adapter/**`, `explorer/catalog/**`, `explorer/reader/**`, `explorer/lineage/**`, `explorer/diagnostics/**`, `explorer/web/**`, `tools/**`, `fixtures/**`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, **`SCOPE.md`**, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, `docs/probe-qualification/**`, or any other module's directory or tests. If you need a change there, record it in a "Change requests" section and code against the current contract. **`SCOPE.md` is named in bold because task item 3 will make you want to edit it. Do not. It is integrator-owned.**
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db, and only through `RHACO_corpus_index.connect()`, never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond that list). **Never write to the live index**; never edit the RHACO module; never call the five index-writing functions named in CONSTRAINTS.md S6 (your L1 check greps for them).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use `fastapi.testclient.TestClient` in-process, or call the service directly. Never launch uvicorn -- the probe is the only sanctioned launcher. No `run_in_background`, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests/search -q` green, then `...\.venv\Scripts\python.exe -m pytest tests -q` green as well; (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in any script you write; UTF-8 with LF line endings.
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and write your round report to `docs/rounds/R08_search.report.md` with the sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, in this order: `docs/critique/R06_critic_round_1.report.md` -- section 3 ranked issues 2 and 5, and the "Notes attached to the gates I passed" paragraph on gate 2 (the critic's reasoning is more precise than any summary of it); CONSTRAINTS.md **S8** (no hidden epistemic uplift), **O6** (component evidence not exposed), **O16** (compatibility gate, not re-evaluation); `SCOPE.md` -- the row "Gold v1.1 compatibility: Recall@10 >= 0.700 aggregate through the adapter's hybrid path", ALL of it, including its Consequence cell; `docs/REFERENCE.md` section 1 and section 3; `explorer/search/service.py` in full -- your round-1 module, especially `MODE_LABELS`, `HYBRID_RERANK_NOTE`, `MODE_EXPLANATIONS` and `_run_identifier`; `explorer/search/templates/search/search.html` (note: the integrator changed its evidence-panel mode checks to prefix matching at round 6 -- read the comment there before you touch anything near it); `tests/search/` in full; `docs/probe-qualification/gold_v1_1_compat.json`. Then the task.

Task B14 -- stop the search page asserting July's numbers as today's standing, and give the identifier mode the evidence disclosure every other mode has.

**Item 1 (the critic's ranked issue 2, and it is a population, not an instance).** The critic found one bare figure: `explorer/search/service.py:93` presents `Recall@10 0.700 aggregate on Gold v1.1` as the hybrid mode's present standing. Re-derive before you scope your fix -- **there are more**. On the current tree the same file also carries `Recall@10 0.200 aggregate on Gold v1.1` (lexical), `Recall@10 0.575 on Gold v1.1` (graph), and inside `HYBRID_RERANK_NOTE` a `0.575 vs threshold 0.700`. Establish the real set yourself with a grep and say what you found; do not take this list as complete either.

Two things make the hybrid figure the worst of them, and you should say both in the report:

- It is **stale**: every one of these numbers comes from RHACO-ANL-20260712-001, evaluated 2026-07-12 on the corpus as it stood then. The page presents them with no date and no source, as present standing.
- It is **contradicted by this build's own measurement**: `tools/l4_gold_oracle.py` scored the adapter's hybrid path at **0.675** (27/40) on today's corpus, twice, and `SCOPE.md` carries that as an evidenced regression whose cause is corpus evolution rather than the adapter. So the page states 0.700 as the operational default's standing while the build's own oracle, on the corpus the page is searching, says 0.675.

The rule to apply is CONSTRAINTS.md **S8**: no hidden epistemic uplift. A retrieval figure on this page is either **attributed and dated** -- naming its source ANL of record and the date it was evaluated, so a reader can tell it is a July measurement and not a claim about today -- or it is **removed**. Bare is not an option. Choose per figure, justify each choice in "Decisions", and state plainly which you removed and which you kept in attributed form. If you conclude a figure belongs on the diagnostics page instead (which may carry it attributed), that is a **change request** to diagnostics, not an edit: `explorer/diagnostics/**` is not yours.

Note for your judgment, not an instruction: the mode explanations exist to help an operator choose a mode, and a comparative sense of the modes is genuinely useful. Removing every number and leaving nothing may be a worse page. Attributed-and-dated may serve the operator better than silence. Decide it, and defend the decision.

**Item 2 (the critic's ranked issue 5).** Identifier-mode results carry no Evidence disclosure. `_run_identifier` builds `identifier_rows` and returns a `SearchView` with no `Evidence`, so the one retrieval mode whose answer is *unambiguous* -- an exact alias lookup, no ranking, no fusion -- is the only one that says nothing about where its answer came from. Every other mode discloses its mode, its rank, its degradation state and its component-rank limitation.

Give identifier results an evidence disclosure of the same shape as the others, honest to what the mode actually does: an exact alias match, `resolve_identifier`, no ranking and therefore no meaningful rank, no fusion and therefore no component rank in the O6 sense, no degradation (the mode does not depend on the vector channel at all -- verify that claim in the adapter before you assert it). The point is not to manufacture symmetry: it is that "this answer is an exact identifier match, not a ranked guess" is *the most trustworthy thing this page can say about any result*, and right now it says nothing.

Be careful about one thing: an identifier can legitimately resolve to several card rows (amendments share their parent's `doc_id` -- `SCOPE.md` row "Unique document per identifier", CONSTRAINTS.md O7). The disclosure must not imply the page picked one.

**Item 3 -- the `SCOPE.md` correction, which you will REPORT and NOT make.** The Gold-compatibility row's Consequence cell currently ends: *"The search page states neither number as a quality claim (S8)."* That is false today -- the page states 0.700 as the hybrid mode's standing -- and it is the second half of the critic's ranked issue 2: a scope register describing a product that does not exist. After your change it may become true, or it may need different wording.

Write the exact replacement text you believe that cell should carry, in your "Change requests" section, as a quotable block the integrator can apply verbatim. Do not edit `SCOPE.md`. Say explicitly whether, after your item-1 changes, the original sentence would have become true on its own.

**Item 4 -- tests in `tests/search/`.** At minimum: a test that FAILS on the current code and passes after your change, asserting that no bare retrieval figure appears on the rendered search page (write it so it would catch a NEW bare figure added later, not just the four that exist today -- a regex over the rendered page for a `Recall@10` figure without an adjacent attribution is the shape to consider); and tests for the identifier evidence disclosure, including the several-rows-per-identifier case. Say which test is the one that would have caught the critic's finding.

**In "Result"** state the module round consumed (**search round 2 of 4**) plainly.

**In "Unresolved uncertainty"** state whether any OTHER page in the product presents a retrieval figure, a threshold, or any other measured quantity without attribution, with the command that establishes your answer. You own only the search page, but the integrator needs to know whether S8 is satisfied product-wide or only where you looked -- the whole lesson of the round-6 sealed-audit work is that a claim found in one place is usually in several.

Write the report at `docs/rounds/R08_search.report.md`.
