> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S8-B12; role: module builder (lineage), direction-aware rendering round; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (round 6 = three builders, the A1.3 cap); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1). Round 6 of the RHACO Corpus Explorer autonomous build; lineage module round 2 of 4 -- this strand consumes one builder round of the 28 (14 consumed before this wave). Dispatched by the build orchestrator, who is also the integrator, under the RHACO-CCX-20260905-001 section 4 convergence sequence, to close critic round 1's single objective-gate FAIL on the surface that produced it.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\lineage -- a git worktree of the workspace repository on branch build/lineage, checked out at commit f29bcc97f4ba7de455fa32f3591e5d3ccf837976 (current main; your round-1 work is merged there, plus rounds 4 and 5, the models contract change, and the SA-3 text edit; 262 tests pass, tests/lineage 22 of them). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\lineage; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/lineage -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `explorer/lineage/**` and `tests/lineage/**`; plus your round report `docs/rounds/R07_lineage.report.md`. Do not modify `explorer/app.py`, `explorer/config.py`, `explorer/models.py`, `explorer/faults.py`, `explorer/corpus_adapter/**`, `explorer/catalog/**`, `explorer/search/**`, `explorer/reader/**`, `explorer/diagnostics/**`, `explorer/web/**`, `tools/**`, `fixtures/**`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, `docs/probe-qualification/**`, or any other module's directory or tests. If you need a change there, record it in a "Change requests" section and code against the current contract. **`explorer/models.py` is not yours this round and does not need to be: the contract you need already landed there.**
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db, and only through `RHACO_corpus_index.connect()`, never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond that list). **Never write to the live index**; never edit the RHACO module; never call the five index-writing functions named in CONSTRAINTS.md S6 (your L1 check greps for them).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use `fastapi.testclient.TestClient` in-process, or call the service directly. Never launch uvicorn -- the probe is the only sanctioned launcher. No `run_in_background`, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests/lineage -q` green, then `...\.venv\Scripts\python.exe -m pytest tests -q` green as well; (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in any script you write; UTF-8 with LF line endings.
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and write your round report to `docs/rounds/R07_lineage.report.md` with the sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, in this order: `docs/critique/R06_critic_round_1.report.md` -- ALL of section 1 "Gate 7 -- the disagreement" and ranked issues 1 and 7 (this is the finding you are repairing, and the critic's reasoning is more precise than any summary of it); `explorer/models.py` lines 21-100 -- `RELATION_LABELS`, the NEW `RELATION_LABELS_INCOMING`, the NEW `relation_sentence(relation, direction)` function, and `Edge.direction_label`'s revised docstring; `git show c861680` -- the integrator contract change that added them, including its commit message, which states what was deliberately left for you; `explorer/reader/service.py` lines 440-480 -- how the reader was revalidated against the new contract, as a worked example of the call you are being asked to make; `explorer/lineage/service.py` in full -- your round-1 module; `explorer/lineage/templates/lineage/lineage.html` in full; `tests/lineage/` in full; CONSTRAINTS.md O8 (edge direction semantics, and why an `amends` edge's `from_id` is a filename stem); `SCOPE.md` row "Click-through from an `amends` edge...". Then the task.

Task B12 -- render every typed edge with the sentence that is true from the end the reader is standing on.

**The finding, precisely.** Critic round 1 recorded objective gate 7 ("typed relation direction preserved") as FAIL, contradicting `build_state.json`, and the build has accepted the critic's reading in full. The machine-readable direction was and is perfect: `data-edge-from` / `data-edge-to` / `data-edge-direction` are correct, the outgoing and incoming buckets come straight from the adapter and are never recomputed, the SVG arrowheads always run from->to, and the probe's KB3 fault genuinely detects a from/to swap. The human-readable sentence was not. `RELATION_LABELS` held one label per relation; `Edge.direction_label` returned it with no reference to direction; `explorer/lineage/service.py` lines 273, 283 and 292 copy that same label onto the outgoing view, the incoming view and the unresolved view alike; and `lineage.html` line 112 renders it verbatim under the heading `Incoming`.

The critic observed the consequence three ways, in descending order of directness: in a screenshot (`runs/20260905T050401Z/W5.png`, Incoming section: `has campaign child: RHACO-CMP-20260628-001`, where that card is the *parent*); in probe JSON (`runs/20260905T050330Z/W6.json`, `parent_api_incoming_amends` reading `amends` from the parent's own page, so the parent claims to amend its own amendment); and by derivation for the case that matters most -- on the current head of a supersession family, the incoming edge from its predecessor renders "is superseded by <predecessor>", reporting the head as superseded by the document it replaced.

PROMPT.md 5.5 requires that directional relationships be displayed directionally. A section heading plus a sentence stating the converse is not that.

**What has already been done for you, and what is deliberately left.** The integrator landed the shared contract at `c861680`: `RELATION_LABELS_INCOMING` and `relation_sentence(relation, direction)` now exist in `explorer/models.py`, and `Edge.direction_label` is kept only for callers that already know they hold an outgoing edge, with a docstring that now says so. The reader was revalidated in the same commit. Lineage was NOT, because `explorer/lineage/**` is yours and an integrator edit there would widen the integrator's footprint into builder-owned files for a change that is properly a builder round. That is this round.

Deliver:

1. **`explorer/lineage/service.py`**: every place a sentence is built for a human, build it from `(relation, direction)` via `models.relation_sentence`, not from `Edge.direction_label`. That is at least lines 273 (outgoing), 283 (incoming), 292 (unresolved) and 421 (the chronological reasoning trail). Decide and justify in "Decisions" what an unresolved edge should read as -- it is an outgoing edge whose target does not resolve, so the outgoing sentence is probably right, but say so rather than inheriting it by accident. Line 244's `RelationOption` label is a different question: it labels a *filter over both directions*, so "is superseded by" is a poor chip label for a filter that also returns incoming supersession edges. Decide it explicitly, change it or keep it with a stated reason, and do not leave it unaddressed.
2. **`explorer/lineage/templates/lineage/lineage.html`**: lines 95, 112, 129 and 183 render `direction_label`. Keep the field name or rename it -- your call, it is your view model -- but make each rendered sentence the one that is true from the centre card's end. Do not change the `data-edge-*` attributes, the bucket assignment, or the SVG arrow direction: those are correct today, they are what KB3 detects a swap on, and altering them would break a qualified known-bad case to fix a rendering defect.
3. **Critic ranked issue 7, the lineage half**: shared `doc_id`s render as runs of identical link text, and an amendment's own page shows a self-pointing `amends` edge (the adapter's `edges_for` double-classifies an amendment's own `amends` edge as both outgoing and incoming -- your own round-1 open issue, first made visible by this surface). The self-pointing edge is an adapter behaviour and the corpus_adapter builder is being dispatched concurrently with you on its last round, so **do not attempt to fix it in the adapter**; make the lineage surface honest about it instead -- either suppress the degenerate self-edge in the view with a stated rule, or label it so a reader is not misled -- and state which you chose. For the identical-link-text runs, make each link distinguishable (the card row's own identity is available to you: `yaml_path` or filename stem distinguishes rows that share a `doc_id`; CONSTRAINTS.md and SCOPE.md both record that an identifier can legitimately resolve to several rows). Do not silently collapse the rows: the UI's standing rule is that it lists all rows and never picks one.
4. **Tests in `tests/lineage/`** that would have failed before this round -- this is the part that matters most, because the defect survived four builder waves, 262 tests, twelve green production presets and a dedicated critic, and the reason it survived is that the only check pointed at it compared the product against the constant the product renders from. Your tests must assert on the RENDERED SENTENCE, and must derive the expected sentence from the *fact*, not from `models.RELATION_LABELS`:
   - a supersession-family HEAD's lineage page: the incoming edge from its predecessor must NOT read "is superseded by", and must read the sentence that is true (a superseding relationship, from the head's end);
   - a campaign parent's page and a campaign member's page rendered from the SAME edge, asserting the two sentences differ and that each is correct for its end;
   - an `amends` edge seen from the amendment and from the parent, likewise;
   - at least one test that would still fail if someone reverted `relation_sentence` to a direction-blind lookup, and say in the report which test that is.
   Write at least one of these against the fixture corpus so it is deterministic, and say plainly which assertions are fixture-backed and which touch the live index.
5. **In "Result"** state the module round consumed (**lineage round 2 of 4**) plainly.
6. **In "Unresolved uncertainty"** state whether any surface outside `explorer/lineage/**` still renders a direction-blind sentence, with the command that establishes your answer (a grep for `direction_label` across `explorer/` is the obvious one). The integrator needs to know whether the class is closed product-wide or only on your surface.

**One thing you must NOT do.** The probe's W5 preset asserts every edge's `direction_label` against `RELATION_LABELS` across all three buckets. Once you render direction-aware sentences, **W5 will fail on incoming edges.** That is expected, it was accepted deliberately when the contract changed, and it is recorded as a BOUNDED_FAIL: the probe's budget is spent at 4 of 4 and cannot be repaired, and an integrator-owned L4 direction oracle will supply the evidence instead. Do not weaken your rendering, do not add a compatibility shim that keeps the old sentence alive somewhere for W5's benefit, and do not touch `tools/probe_corpus_explorer.py` (it is not yours). A check that verifies something no one displays is the exact pathology this round exists to end.

Write the report at `docs/rounds/R07_lineage.report.md`.
