> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S8-C4; role: module builder (corpus_adapter), the single batched LAST round; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (round 6 = three builders, the A1.3 cap); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1). Round 6 of the RHACO Corpus Explorer autonomous build; corpus_adapter module round 4 of 4 -- **the last round this module gets.** There is no round 5, the budget is not waived, and this dispatch is the one batched round the Director's boundary-3 HOLD reserved for exactly this moment.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\corpus_adapter -- a git worktree of the workspace repository on branch build/corpus_adapter, checked out at commit f29bcc97f4ba7de455fa32f3591e5d3ccf837976 (current main; your rounds 1-3 are merged there; 262 tests pass, tests/corpus_adapter 20 of them). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\corpus_adapter; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/corpus_adapter -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: `explorer/corpus_adapter/**` and `tests/corpus_adapter/**`; plus your round report `docs/rounds/R07_corpus_adapter.report.md`. Do not modify `explorer/app.py`, `explorer/config.py`, `explorer/models.py`, `explorer/faults.py`, `explorer/catalog/**`, `explorer/search/**`, `explorer/reader/**`, `explorer/diagnostics/**`, `explorer/web/**`, `explorer/lineage/**`, `tools/**`, `fixtures/**`, `requirements.txt`, `ruff.toml`, `.gitignore`, `PROMPT.md`, `RATIONALE.md`, `DISPATCH_PARAMETERS.md`, `CONSTRAINTS.md`, `ARCHITECTURE.md`, `SCOPE.md`, `build_state.json`, `docs/LAYERS.md`, `docs/REFERENCE.md`, `docs/probe-qualification/**`, or any other module's directory or tests. If you need a change there, record it in a "Change requests" section and code against the current contract. Note that `explorer/lineage/**` is being edited by a concurrent strand this round; do not read it as a moving target and do not depend on its current text.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db, and only through `RHACO_corpus_index.connect()`, never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond that list). **Never write to the live index**; never edit the RHACO module; never call the five index-writing functions named in CONSTRAINTS.md S6 (your L1 check greps for them).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use `fastapi.testclient.TestClient` in-process, or call the adapter directly. Never launch uvicorn -- the probe is the only sanctioned launcher. No `run_in_background`, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only, never -A; no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `...\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `...\.venv\Scripts\python.exe -m pytest tests/corpus_adapter -q` green, then `...\.venv\Scripts\python.exe -m pytest tests -q` green as well, because the adapter sits under every other module; (c) `...\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in any script you write; UTF-8 with LF line endings.
7. Timestamps and identity: run `(Get-Date).ToUniversalTime().ToString("o")` as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, plus the per-item `landed | not landed` disposition required below; and write your round report to `docs/rounds/R07_corpus_adapter.report.md` with the sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.

Read first, inside your worktree, in this order: `explorer/corpus_adapter/adapter.py` line 56 (`GRAPH_DEGRADED_NOTICE`) and lines 445-485 (`search_hybrid` and `search_graph`, where `mode_effective` is decided); `explorer/corpus_adapter/availability.py` in full -- especially the module docstring's "Probe budget note" and `probe()`'s `ThreadPoolExecutor` block; CONSTRAINTS.md O4 (the availability procedure) and O8 (edge direction semantics, and why an `amends` edge's `from_id` is a filename stem); ARCHITECTURE.md 4.1 including its NEW licensed-importer table; `SCOPE.md` row "Click-through from an `amends` edge to the amendment, on the lineage graph and trail"; `explorer/corpus_adapter/sql.py` D-Q3 and D-Q7; `docs/rounds/R01_corpus_adapter.report.md` "Unresolved uncertainty" (the round-1 builder's own honest flag about the probe budget); `docs/critique/R06_critic_round_1.report.md` sections 0, 1 and 5. Then the task.

Task C4 -- one batched round, four items, in this priority order. **The order is a Director ruling, not a suggestion.**

Work items 1, 2 and 3 in that sequence. Item 4 is trivial and goes last. If you run out of room, the items you did not land are declared BOUNDED_FAIL on the record -- which is an honest, accepted outcome. **What is not acceptable is weakening an item to make it fit, or landing a later item at the cost of an earlier one.** Report each of the four as `landed` or `not landed`, individually, with a one-line reason for anything not landed.

**Item 1 (highest priority) -- SA-1: the graph-degraded notice describes a degradation that does not happen.**

`GRAPH_DEGRADED_NOTICE` reads "Graph unavailable: hybrid seed channel degraded to lexical. Result ordering is lexical-only." Both halves are false for graph mode. `RHACO_corpus_index.search_graph`'s 1-hop expansion reads only the `edges` table and is entirely unaffected by `VecUnavailable`; what degrades is the *seed* channel that feeds the expansion, which falls back to identifier resolution plus FTS. So the graph is NOT unavailable, and the ordering is NOT lexical-only -- the surviving graph expansion still shapes the result. `mode_effective` compounds it by reading `graph-degraded`, which names no surviving channel.

Fix both: make the notice state what actually degraded and what survived, and make `mode_effective` name the surviving channel (the sealed audit suggests a shape like `graph-degraded-semantic-seed`; you may choose the exact token, but it must name the channel, and you must say in "Decisions" what you chose and why). Then check `HYBRID_DEGRADED_NOTICE` against the same standard -- for hybrid the old wording may well be correct, since the fused ordering really does collapse to lexical there; say explicitly whether it is right or wrong rather than leaving it unexamined. Anything that consumes `mode_effective` as a literal string is a caller you do not own: search for such consumers, and if changing the token would break one, file it as a change request rather than editing their file. This is the highest-priority item because it is a false statement the product makes to a scientist about the provenance of the results in front of them, and no layer of this harness ever exercised graph mode under `--disable-vec`.

**Item 2 -- SA-2: the 8 s probe budget is not a wall-clock bound.**

`probe()` submits `embed_query_cpu` to a `ThreadPoolExecutor` inside a `with` block and calls `future.result(timeout=PROBE_BUDGET_S)`. The timeout fires correctly -- and then the `with` block exits, `Executor.__exit__` calls `shutdown(wait=True)`, and that JOINS the worker thread. So a hung embedder blocks the caller until `embed_query_cpu` itself returns or fails, which the module bounds at `OLLAMA_TIMEOUT_S = 300`, not at 8 s. Every hybrid or graph search and every diagnostics page load reaches this path. The module docstring's claim of a "one-shot daemon-thread call" is false as well: `ThreadPoolExecutor` workers are not daemon threads and are joined.

Round 1's builder recorded "verified by code reading, not an observed slow run", and that honest UNVERIFIED flag has been carried in `build_state.json` ever since. It was the precursor to the finding, not the finding.

Fix it so the bound is real: submit to an executor that is not joined on the timeout path (a long-lived single-worker executor used without the context manager, or `shutdown(wait=False)`, or a plain thread you do not join -- your call, justified in "Decisions", including what happens to the abandoned worker and why that is safe). The embed call is a read-only network request with no state it can corrupt, which is what makes abandoning it acceptable; say so, and say what you considered instead. Correct the docstring's daemon-thread claim in the same change.

**Qualify it, do not assert it.** Add a test with a fake `rhaco_index` whose `embed_query_cpu` sleeps well past the budget, and assert the CALLER'S OWN ELAPSED TIME is bounded near `PROBE_BUDGET_S` -- not merely that the returned reason is `embedder_unreachable`, which the current code already achieves while blocking for five minutes. Measuring the caller's elapsed wall clock is the whole point: the defect is invisible to any assertion about the return value. Keep the test's own sleep short enough that the suite stays fast (a reduced `PROBE_BUDGET_S` via monkeypatch is fine and is the obvious approach) and say in the report what the test would have measured against the old code.

**Item 3 -- the deferred amends click-through change request.**

`SCOPE.md`'s row records it: `CorpusAdapter.edges_for` resolves `source_cards` / `target_cards` through `cards_for_doc_id`, which matches `doc_id` only, while an `amends` edge's `from_id` is the amendment's **filename stem** (CONSTRAINTS.md O8). The stem resolves to no card row, so on a parent card's lineage page the incoming `amends` edge renders as typed, directional text with no link. The row's own "path to addressing" names the change: a stem-aware lookup in `edges_for`, for example keyed on `cards.doc_filename`, additive -- no caller depends on the list being empty and no existing test should change. This round is the one the row was deferring to.

Two cautions. First, the same `edges_for` behaviour double-classifies an amendment's own `amends` edge as both outgoing and incoming on the amendment's own page (recorded as a lineage round-1 open issue and as the critic's ranked issue 7). If your stem-aware resolution makes that degenerate self-edge worse or better, say so explicitly -- the lineage builder is concurrently making its surface honest about it, and the integrator needs to know whether your change moved the ground under that. Second, `SCOPE.md` is not yours: if the row should now change status, file it as a change request and let the integrator edit it.

**Item 4 (last, trivial) -- three module docstrings still claim `adapter.py` is the sole importer.**

The integrator corrected `ARCHITECTURE.md` 4.1, `CONSTRAINTS.md` S2 and `explorer/corpus_adapter/__init__.py` at commit `f29bcc9`, enumerating the three licensed product-tree importers (`adapter.py`, `explorer/diagnostics/service.py` under O17, `fixtures/build_fixture_index.py` under O3). Three claims in files you own were left for you because they are yours: `adapter.py` lines 2-3 ("The only module in the workspace that imports either"), `availability.py` line 23 ("the sole importer"), `sql.py` line 9 ("adapter.py is the sole importer"). Correct all three to agree with the new 4.1 -- the point each was making about *its own* file not importing the modules stays true and should stay; it is the parenthetical claim about the workspace that is false. Docstrings only; no code.

**In "Result"** state the module round consumed (**corpus_adapter round 4 of 4, the last**) and give the four-item `landed | not landed` table.

**In "Unresolved uncertainty"** state what a fifth round would have done if one existed. That is not a request for one -- there is none -- it is the record of what this module is shipping without, and the closeout depends on it being honest.

Write the report at `docs/rounds/R07_corpus_adapter.report.md`.
