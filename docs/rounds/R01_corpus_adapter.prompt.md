> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id B1; role: module builder (corpus_adapter); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 1 = three builders, the cap); tier_escalation: none. Round 1 of the RHACO Corpus Explorer autonomous build; dispatched by the build orchestrator, who is also the integrator.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\corpus_adapter -- a git worktree of the workspace repository on branch build/corpus_adapter, checked out at commit 4987cca1b6e4b771a00697e7cd65f948c3d6ec56. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\corpus_adapter; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/corpus_adapter -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: explorer/corpus_adapter/** and tests/corpus_adapter/**; plus your round report docs/rounds/R01_corpus_adapter.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, or any other module's directory. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through RHACO_corpus_index.connect(), never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; never call the five index-writing functions named in CONSTRAINTS.md S6 (your L1 check greps for them).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use fastapi.testclient.TestClient in-process, or call the adapter directly. No run_in_background, no detached processes, no sleep-and-poll loops other than a readiness wait bounded at 30 s inside the probe.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/corpus_adapter -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file.
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") when you start and when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R01_corpus_adapter.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Read first, inside your worktree, in this order: ARCHITECTURE.md (all of it; your module's section in detail), CONSTRAINTS.md, docs/REFERENCE.md, explorer/models.py, explorer/config.py, explorer/app.py, explorer/faults.py, docs/rounds/R00_I1_corpus_index_contracts.report.md and docs/rounds/R00_I2_card_schema_fields.report.md (the substrate you wrap). Then the task.

Task B1 -- build explorer/corpus_adapter: the read-only adapter over RHACO_corpus_index and the catalog librarian, and the ONLY module that imports either.

Deliver, exactly as ARCHITECTURE.md section 4.1 specifies (names, signatures, return models from explorer/models.py):
1. explorer/corpus_adapter/__init__.py exporting CorpusAdapter and ConfigurationError.
2. explorer/corpus_adapter/paths.py: sys.path entries from explorer.config.RHACO_PATH_ENTRIES (append, so workspace code stays first); junction-aware normalization (os.path.realpath + os.path.normcase); both_spellings(path) producing the junction (C:\RHACO\...) and physical (C:\highsierralabs\RHACO\...) spellings of a path under the docs root (CONSTRAINTS O13); is_under_docs_root(path, docs_root) accepting both spellings and rejecting `..` escapes; card_ref (relative POSIX path of a .card.yaml under the docs root) <-> absolute yaml_path conversion.
3. explorer/corpus_adapter/sql.py: the documented queries D-Q1..D-Q11 and nothing else, as named constants / small builders with bound parameters; D-Q5 / D-Q6 assemble WHERE clauses only from the fixed whitelist (doc_type, status, lifecycle_state, project_knowledge, date_from, date_to, program, tag, path_prefix) and ORDER BY only from the whitelist (date, doc_id, title, status; asc|desc); values are always bound, never interpolated.
4. explorer/corpus_adapter/availability.py: the VectorAvailability probe of CONSTRAINTS O4 / ARCHITECTURE A4: (a) RHACO_corpus_index.VEC_DISABLE_ENV set -> disabled_by_env; (b) `SELECT vec_version()` on a connection from RHACO_corpus_index.connect(db_path) fails -> sqlite_vec_not_loaded; (c) get_meta(conn, "vec_rows__nomic-embed-text") missing -> vec_table_missing; (d) a one-item RHACO_corpus_index.embed_query_cpu(CPU_FLOOR_TAG, ["probe"]) raising VecUnavailable -> embedder_unreachable (the module's own timeout is 300 s; if you cannot bound the probe below 10 s through the public API, document exactly what you did and why); result cached for at most 5 s; probe_ms recorded.
5. explorer/corpus_adapter/adapter.py: class CorpusAdapter(settings) with attributes db_path, docs_root, active_fault and every method in section 4.1: db_path = settings.db_path or RHACO_corpus_index.DEFAULT_DB, checked with os.path.isfile BEFORE any connect() (raise ConfigurationError -- never create a database); docs_root = settings.docs_root or RHACO_corpus_index.BODY_SCAN_ROOT; active_fault = explorer.faults.active_fault(settings.fault, db_path) once at construction; one connect() per public call, closed in `finally`; index_meta() and vector_availability() cached <= 5 s; exclusion_sets() from the module and librarian constants; search_lexical / search_hybrid / search_graph / resolve_identifier wrapping the module functions of the same purpose and labelling mode_effective + degradation_notice per docs/REFERENCE.md consequence 5 (exact notice text: "Hybrid unavailable: using lexical retrieval. Result ordering is lexical-only."), with ResultItem.cards filled by cards_for_doc_id (all rows per id) and Evidence filled per section 4.3 / models.py (component_rank always the not-exposed constant for hybrid and graph; lexical hits carry line_no, line, context); lexical_excerpt(q, doc_ref) via search_fts with path_glob targeting one document, labelled excerpt_source="lexical-lookup"; edges_for by doc_id AND by filename stem (the `amends` from_id is a stem -- CONSTRAINTS O8) with target/source cards attached and unresolved edges separated; supersession_chain / campaign_subtree / dangling_references via D-Q9..D-Q11 with the documented caps and a Python visited-set cycle guard; read_document(card) reading the file from disk under the confinement check, returning DocumentText with sha256, lines, kind (markdown|text|binary by suffix .md/.txt/other), and headings (level, text, GFM slug = lowercased, spaces to hyphens, punctuation stripped except hyphens, duplicates suffixed -1, -2, ...; line_no 1-based); read_card_text(card); freshness() = RHACO_tool_catalog_librarian.scan_library(docs_root, <output_path>, <logger>) followed by RHACO_corpus_index.freshness_check(scan, db_path) -> FreshnessView with computed_at_utc and scan_seconds -- read scan_library's signature and body at C:\RHACO\tools\RHACO_tool_catalog_librarian.py (around line 1665) FIRST and choose arguments that make it write nothing (a NullHandler logger; an output_path value that is only read); document the choice.
   Fault hooks (fixture-only by construction of active_fault): wrong_doc_for_id -> cards_for_doc_id returns the row that follows the correct one in yaml_path order; stale_card -> card() returns a copy whose title ends with " [STALE]" and whose status is "Draft"; reverse_edges -> edges_for swaps from/to on every edge; broken_jump -> every heading line_no + 50. console_error is a template concern, not yours.
6. tests/corpus_adapter/ (pytest, against the LIVE index read-only at the module default path): (a) missing db path -> ConfigurationError and the tmp directory stays empty; (b) index_meta() has the keys listed in ARCHITECTURE section 3 IndexMeta and live counts > 0; (c) cards_for_doc_id("RHACO-HND-20260903-001") returns exactly 2 rows (the hand and its Amendment A1) and card(card_ref) round-trips for each; (d) catalog(CatalogFilters(doc_type="CMP"), "date", 1, 50) returns only CMP rows with lifecycle_state populated, and facets() is non-empty; (e) search_lexical("firmware transfer function") returns items with line_no and excerpt_source "lexical-hit"; (f) resolve_identifier("RHACO-CMP-20260903-001") == ["RHACO-CMP-20260903-001"]; (g) search_hybrid("firmware transfer function", top_k=5) returns 5 items whose mode_effective is consistent with vector_availability().available; (h) edges_for(the hand card) has an incoming campaign_child edge from RHACO-CMP-20260903-001, and edges_for(the A1 card) has an outgoing amends edge whose from_id equals the A1 filename stem; (i) supersession_chain on some superseded document found through dangling-free D-Q7 data (pick any edge with relation supersedes via edges_for on a card you locate through the catalog -- do not add SQL); (j) read_document(the hand card) returns headings with slugs and is_under_docs_root rejects C:\RHACO\data\x.md and a `..` path; (k) a child-process test: run the venv interpreter with RHACO_CORPUS_DISABLE_VEC=1 (subprocess, foreground, bounded 120 s) executing a tiny script that constructs the adapter and prints search_hybrid(...).mode_effective and .degradation_notice; assert "hybrid-degraded-lexical" and the exact notice. A session-scoped fixture records the live db's sha256 + mtime at start and asserts both unchanged at the end of the test session; print both values in your report (criterion-6 evidence).
Write the report at docs/rounds/R01_corpus_adapter.report.md.
