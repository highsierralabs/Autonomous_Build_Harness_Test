> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id B2; role: module builder (probe); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 1 = three builders, the cap); tier_escalation: none. Round 1 of the RHACO Corpus Explorer autonomous build; dispatched by the build orchestrator, who is also the integrator.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe -- a git worktree of the workspace repository on branch build/probe, checked out at commit 4987cca1b6e4b771a00697e7cd65f948c3d6ec56. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: tools/probe_corpus_explorer.py, fixtures/** (fixtures/build_fixture_index.py and fixtures/corpus/**), tests/probe/**, and docs/probe-qualification/** (evidence output; add a README.md there describing the layout); plus your round report docs/rounds/R01_probe.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, or any other module's directory. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through RHACO_corpus_index.connect(), never opened any other way; C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; the five index-writing functions named in CONSTRAINTS.md S6 may appear in exactly one file you own, fixtures/build_fixture_index.py, and only reindex_full may be called there, only against the fixture db path after the path assertion (CONSTRAINTS O2/O3; ARCHITECTURE A12).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). Your probe is the ONLY sanctioned launcher of the explorer server: it spawns `<venv python> -m uvicorn explorer.app:create_app --factory --host 127.0.0.1 --port N` as a foreground child of its own process, waits on /healthz, and tears it down in a finally block. Unit tests other than the probe smoke test use fastapi.testclient.TestClient in-process. No run_in_background, no detached processes, no sleep-and-poll loops other than a readiness wait bounded at 30 s inside the probe.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file.
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") when you start and when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R01_probe.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Read first, inside your worktree, in this order: ARCHITECTURE.md (all of it; your module's section in detail), CONSTRAINTS.md, docs/REFERENCE.md, explorer/models.py, explorer/config.py, explorer/app.py, explorer/faults.py, docs/rounds/R00_I1_corpus_index_contracts.report.md sections 4-5 and docs/rounds/R00_I2_card_schema_fields.report.md section 1 (card schema). Then the task.

Task B2 -- build the production-path probe framework and the fixture substrate for probe qualification.

Deliver:
1. fixtures/corpus/ -- a small synthetic RHACO-shaped corpus: documents plus sibling .card.yaml files conforming to Card Schema v1.9 as summarized in docs/rounds/R00_I2_card_schema_fields.report.md section 1 (read C:\RHACO\docs\reference\RHACO_Card_YAML_Schema_Specification_v1_9.md for the exact key shapes; read a real card, e.g. C:\RHACO\docs\reports\RHACO-CMP-20260903-001_Corpus_Explorer_Autonomous_Build_Harness.card.yaml, as a model -- copy its structure, never its content). Layout: fixtures/corpus/reports/, fixtures/corpus/handoffs/, fixtures/corpus/reference/, fixtures/corpus/docs_archive/ (one document that must NOT be indexed). Contents: one CMP (status Active, lifecycle_state OPEN) with two children (an ANL and an HND whose cards depends_on the CMP); a reference pair RHACO_Fixture_Reference_v1_0.md (status Superseded, superseded_by -> the v1_1 card id) and RHACO_Fixture_Reference_v1_1.md (Active) -- note the librarian keys uniqueness on canonical id, so give each its own card and confirm the builder's walk accepts the pair (if it does not, move v1_0 to docs_archive and record why); an amendment child of the HND named <HND filename stem>_Amendment_A1.md with a card carrying parent: {id: <HND id>, note: ...}, amendment_seq "A1", and the parent's doc_type/date/seq; one EVT document whose filename and title carry a logger id E000777; one document containing the unique phrase "quartz lattice lantern" on a known line (record the line number); one .txt body; a card with two programs and three tags; one depends_on that points at a non-existent id (an unresolved reference). Use fixture ids dated 2026-01-xx so nothing collides with real RHACO ids; use only the vocabularies the schema allows (statuses, programs, doc types).
2. fixtures/build_fixture_index.py -- the only allowlisted file: CLI `--docs-root fixtures/corpus --db fixtures/fixture_index.db [--force]` plus an importable build(docs_root, db_path, force=False) -> dict summary. Imports RHACO_corpus_index and RHACO_tool_catalog_librarian via explorer.config.RHACO_PATH_ENTRIES. Order of operations: (ii) resolve the db path (realpath); assert it is not RHACO_corpus_index.DEFAULT_DB and lies under neither C:\RHACO\ nor C:\highsierralabs\RHACO\ (compare realpath + normcase of both spellings) -- on failure exit 2 / raise before touching anything; (i) produce the librarian scan for the fixture docs root (read scan_library / walk_library at C:\RHACO\tools\RHACO_tool_catalog_librarian.py around lines 798 and 1665 first; choose arguments that write nothing); set RHACO_corpus_index.BODY_SCAN_ROOT = <absolute fixture docs root> in-process (never on disk) because reindex_full calls iter_body_docs() without a root; call reindex_full(scan, db_path=<fixture db>); restore BODY_SCAN_ROOT in a finally block; print an ASCII summary (cards, fts_docs, edges, aliases, db path). Do not call reindex_vec (no vector set for the fixture: the fixture exercises identifier / lexical / graph, and search_hybrid on it degrades to FTS -- itself a useful known state; document it). Deterministic given the fixture files.
3. tools/probe_corpus_explorer.py per ARCHITECTURE.md section 4.8: argparse (--db, --docs-root, --port default 8765, --preset NAME|all, --out DIR default docs/probe-qualification/runs/<utc stamp>, --fault, --disable-vec, --timeout default 30); child env RHACO_EXPLORER_DB / RHACO_EXPLORER_DOCS_ROOT (+ RHACO_EXPLORER_FAULT, + RHACO_CORPUS_DISABLE_VEC=1 when asked) and PYTHONIOENCODING=utf-8; readiness = GET /healthz returning status "ok" (poll every 0.5 s up to --timeout); Playwright sync API, chromium headless, viewport 1280x900; per preset record url, viewport, title, active retrieval mode (read the root element's data-mode attribute when present, else the /api twin, else null), selected document id, observed result ids (elements with data-doc-id when present), console errors (type "error"), failed requests (requestfailed events and responses with status >= 400), timings, then save <out>/<preset>.png (full page) and <out>/<preset>.json; always write <out>/run_summary.json; terminate the child in a finally block (terminate, then kill after 5 s); exit 0 only when every preset collected all its required observations and none failed. Presets this round: `healthz` (JSON check only) and `diagnostics` (GET /diagnostics -- until the diagnostics module merges it may be 404: record the status as an observation, do not crash, mark the preset "route absent"). Also lay down the preset REGISTRY for W1-W10 and the qualification cases KG, KB1-KB5 as entries whose runner reports `not_implemented_yet` cleanly (they are filled in wave 2). ASCII-only stdout.
4. tests/probe/: test_fixture_builder.py builds into tmp_path via build() and asserts: cards count equals the number of fixture cards outside docs_archive; the docs_archive document is absent from cards and fts_docs; the amendment shares its parent doc_id (two cards rows for that id); a supersedes edge exists from the v1_0 id to the v1_1 id; an unresolved cites edge exists (resolved = 0); RHACO_corpus_index.search_fts("quartz lattice lantern", db_path=<tmp db>) returns the fixture document at the recorded line_no; the refusal path: build() with a db path under C:\RHACO\ and with RHACO_corpus_index.DEFAULT_DB raises before any write (assert no file was created and, for DEFAULT_DB, that the live file's sha256 is unchanged). test_probe_smoke.py runs the probe as a subprocess against a tmp fixture db with `--preset healthz` and asserts run_summary.json exists and reports success (this test launches the server only through the probe). Read-only queries in tests go through RHACO_corpus_index.connect(tmp_db) + plain SELECTs on the FIXTURE db only.
Write the report at docs/rounds/R01_probe.report.md including the fixture inventory table (file, id, purpose, known facts such as the phrase line number).
