> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S6-B11; role: module builder (reader), production-path defect repair; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: sequential (dispatched alone; one strand live); tier_escalation: none; orchestrator_model: claude-opus-5[1m] (Director ruling MOD-1). Round 5 of the RHACO Corpus Explorer autonomous build; reader module round 2 of 4 -- this strand consumes one builder round of the 28. Dispatched by the build orchestrator, who is also the integrator, to repair a defect the probe's W6 acceptance workflow found on the PRODUCTION path and the integrator then reproduced independently.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\reader -- a git worktree of the workspace repository on branch build/reader. **Your first git act** is to create or fast-forward it to the base commit: the integrator has placed it at e36d9d69ec5e915bd554b658c3cbcc82fe5c1f4f. Confirm with `git -C C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\reader rev-parse HEAD` and STOP as BLOCKED if it differs. Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe. Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\reader; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/reader -q`. **Always `cd` explicitly**: the outer checkout holds a same-named `explorer` package and Python puts the current directory first on `sys.path`, which silently resolved the wrong package for another strand this wave.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: explorer/reader/** (service.py, routes.py, templates/reader/*.html, and, if you need one, static/reader.css served under /static/reader/ by the app factory) and tests/reader/**; plus your round report docs/rounds/R02_reader.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, explorer/web/** (base.html, app.css, app.js -- the web builder's, wave 3), explorer/corpus_adapter/**, explorer/diagnostics/**, tools/**, fixtures/**, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, docs/probe-qualification/**, or any other module's directory or tests. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through the adapter (explorer.corpus_adapter.CorpusAdapter, which alone calls RHACO_corpus_index.connect()); never open the db any other way, never import RHACO_corpus_index or RHACO_tool_catalog_librarian yourself (the adapter is the only importer, ARCHITECTURE.md section 2); C:\RHACO\rhaco\RHACO_corpus_index.py and C:\RHACO\tools\RHACO_tool_catalog_librarian.py (read-only, for understanding); C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; never name or call the five index-writing functions listed in CONSTRAINTS.md S6 (your L1 check greps for them; importing `build` from fixtures.build_fixture_index in tests is allowed and is how the probe tests do it).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use fastapi.testclient.TestClient in-process against explorer.app.create_app(settings). Never launch uvicorn (the probe is the only sanctioned launcher). No run_in_background, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/reader -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file; templates preserve every corpus character verbatim (CONSTRAINTS.md O19, O28).
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R02_reader.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Conventions every wave-2 page follows (ARCHITECTURE.md A14, A19; the probe reads exactly these):
- Server-rendered HTML page plus a JSON twin under /api/... built with dataclasses.asdict over the same view object, so the probe can cross-check the page against the API.
- The template extends base.html and sets `{% block html_attrs %}` to `data-mode="<value>"` (and `data-vector="available|unavailable"` where a retrieval call ran); every rendered result or list row carries `data-doc-id="<doc_id>"` and, where the row is one card among several for the same id, `data-card-ref="<card_ref>"`; the open document's region carries `data-selected-doc-id="<doc_id>"`.
- The adapter is reached only through explorer.app.get_adapter(request); when it raises RuntimeError, the HTML page renders a visible notice with HTTP 503 and the API returns 503 {"error": "corpus_adapter not built"} (the diagnostics precedent, explorer/diagnostics/routes.py).
- explorer.app.ADAPTER_ABSENT is the sentinel a test places on app.state.adapter to exercise that path (ARCHITECTURE.md A20).
- status and lifecycle_state are never joined, substituted, or merged into one control or one cell (CONSTRAINTS.md S4); a frozen status (Superseded, Archived -- CardRow.frozen) is marked with the visible text "frozen" next to it, never by colour alone.
- The authority notice is already rendered by base.html's footer; do not restate rank as correctness anywhere (S8).
- Tests: build a fixture index once per test session into tmp_path_factory via `from fixtures.build_fixture_index import build` (docs root = the absolute path of fixtures/corpus in your worktree; the fixture inventory is in docs/rounds/R01_probe.report.md), construct the app with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture docs root>))`, and drive it with TestClient -- every functional assertion is against that deterministic fixture. Add at least one read-only smoke test against the live index (Settings() defaults) asserting only shape (HTTP 200 and non-empty), guarded by the same before/after `corpus_cards_sha` + mtime check tests/corpus_adapter/conftest.py uses (copy that fixture's approach into your own conftest; do not import theirs). Use a hand-written fake adapter only for states the fixture cannot produce (vector channel unavailable; adapter absent).

Read first, inside your worktree, in this order: `explorer/reader/service.py` (all of it; especially the card-file parse at line 234 and everything downstream of it), `explorer/reader/routes.py`, `explorer/reader/templates/reader/reader.html` (especially the `card_parsed | tojson` use), `docs/rounds/R02_reader.report.md` (your round-1 report), `docs/rounds/R04_probe.report.md` (the W6 evidence), `docs/probe-qualification/runs/20260905T043147Z/W6.json` (the failing observation itself), CONSTRAINTS.md S1 and O19/O28, ARCHITECTURE.md 4.4. Then the task.

Task B11 -- repair the card-YAML native-type defect that breaks the reader on most of the live corpus.

**The defect, established by evidence, not conjecture.** The probe's W6 production run (`docs/probe-qualification/runs/20260905T043147Z`) recorded `unexpected_http_status[reader_v1_8]: 500` for `/doc/reference/RHACO_Card_YAML_Schema_Specification_v1_8.card.yaml`, with the matching console error and failed request; the lineage steps in the same run returned 200, so the failure is the reader's alone. The integrator then reproduced it in-process, read-only, against the live index:

- `GET /doc/reference/RHACO_Card_YAML_Schema_Specification_v1_8.card.yaml` -> **500**
- `GET /api/doc/reference/RHACO_Card_YAML_Schema_Specification_v1_8.card.yaml` -> **500** (both surfaces, not just the page)
- Terminal exception: `TypeError: Object of type date is not JSON serializable`
- Root cause: `yaml.safe_load` resolves an unquoted YAML date scalar to a native `datetime.date`. That card yields `identity.date = datetime.date(2026, 6, 24)` and `identity.last_human_review = datetime.date(2026, 6, 24)`. Those objects reach `reader.html`'s `card_parsed | tojson` (and the API's JSON encoding) and cannot be serialized.
- **Blast radius, counted by the integrator over the live corpus:** 872 of 1449 `.card.yaml` files (60.2%) carry an unquoted `date:` and/or `last_human_review:` scalar.
- **Why no test caught it:** every card in `fixtures/corpus/` quotes its dates (`date: "2026-01-15"`), so the fixture can never produce a `datetime.date`. This is a fixture-representativeness gap, and it is why PROMPT.md section 2 requires production-path exercise in addition to fixture qualification.

Deliver:

1. **The serialization repair**, in `explorer/reader/**`. Make the parsed-card view JSON-safe before it reaches either surface, converting temporal scalars to the string form the card file itself uses: `datetime.date` and `datetime.datetime` to ISO-8601 text, `datetime.time` likewise. Requirements on the fix:
   - It must be **fidelity-preserving**: the ISO string is what the file literally says, so the displayed value must be identical to the file's own characters. Do not reformat, localise, or re-zone anything.
   - It must be **total**: any other non-JSON-native leaf `yaml.safe_load` can produce must also be rendered rather than crash the page. Convert what you can name; for anything else fall back to `str()` and surface it plainly. **A reader page must never 500 because of a card's contents** -- the card is the record (S1), and the reader's job is to show it, including when it is odd.
   - Apply it to BOTH surfaces: the HTML page's `card_parsed` and the `/api/doc` twin must agree, and both must return 200.
   - The `<details>` "Card source (.card.yaml)" block must keep showing the file's RAW text byte-for-byte, unchanged by any of this (O19, O28).

2. **Check, and fix if present, the second-order bug of the same root cause.** The reader compares card-file values against the index row to compute `data-card-index-mismatch` (title / status / lifecycle_state / date / doc_type). If the card-file side is a native `datetime.date` while the index side is a string, that comparison can report a mismatch that does not exist -- which would mark a large fraction of the corpus as "index row differs from card file" when the two in fact agree. That is a **fidelity error, not a crash**, and it is worse than the 500 because it is silent and wrong rather than loud and obvious. Determine by direct test whether the current code has this bug; if it does, fix it so like is compared with like, and say in the report exactly how many of the mismatch fields were affected. If it does not, show the evidence that it does not.

3. **Regression tests** in `tests/reader/`, at three levels, because the fixture alone provably cannot catch this class:
   - a **unit test** of the conversion helper over `date`, `datetime`, `time`, nested dicts and lists, and a non-temporal exotic leaf;
   - an **end-to-end fixture test** that copies `fixtures/corpus` into `tmp_path`, rewrites ONE card's `date:` (and `last_human_review:`) to the **unquoted** form, builds an index from that copy, and drives both `/doc/...` and `/api/doc/...`, asserting HTTP 200 on each and that the rendered value equals the ISO text the file carries. Write only under `tmp_path`: `fixtures/**` is another module's path and must not be modified;
   - a **live-index smoke** on the exact document that failed, `reference/RHACO_Card_YAML_Schema_Specification_v1_8.card.yaml`, asserting 200 on both surfaces and a non-empty card panel, guarded by the `corpus_cards_sha` plus mtime before/after check your conftest already uses.

4. In the report, state plainly: the module round consumed (**reader round 2 of 4**), whether the mismatch bug of item 2 existed, and whether any card-parse path outside `explorer/reader/**` shares the defect. `explorer/reader/service.py` is the only card-YAML parse inside the application, but `tools/probe_corpus_explorer.py` also parses card files for its own independent comparisons; you do not own `tools/` -- if you believe it shares the bug, file a change request rather than editing it.

Do not widen the change beyond this defect and its tests. In particular do not restructure the card panel, do not touch another module, and do not alter the fixture corpus.

Write the report at `docs/rounds/R05_reader.report.md`, sections Header, Change, Evidence, Material alternatives, Decisions, Result, Unresolved uncertainty, Change requests, Assumptions.
