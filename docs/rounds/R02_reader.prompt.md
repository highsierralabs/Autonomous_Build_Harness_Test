> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S4-B6; role: module builder (reader); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 2 = three builders, the cap); tier_escalation: none. Round 2 (wave 2) of the RHACO Corpus Explorer autonomous build; reader module round 1 of 4; dispatched by the build orchestrator, who is also the integrator.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\reader -- a git worktree of the workspace repository on branch build/reader, checked out at commit c96664dd82e19da730cbd741f0e2ace43501121d (the correction-boundary commit: corpus_adapter, diagnostics, and the probe framework are merged and green). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\reader; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/reader -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: explorer/reader/** (service.py, routes.py, templates/reader/*.html, and, if you need one, static/reader.css served under /static/reader/ by the app factory) and tests/reader/**; plus your round report docs/rounds/R02_reader.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, explorer/web/** (base.html, app.css, app.js -- the web builder's, wave 3), explorer/corpus_adapter/**, explorer/diagnostics/**, tools/**, fixtures/**, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, docs/probe-qualification/**, or any other module's directory or tests. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through the adapter (explorer.corpus_adapter.CorpusAdapter, which alone calls RHACO_corpus_index.connect()); never open the db any other way, never import RHACO_corpus_index or RHACO_tool_catalog_librarian yourself (the adapter is the only importer, ARCHITECTURE.md section 2); C:\RHACO\rhaco\RHACO_corpus_index.py and C:\RHACO\tools\RHACO_tool_catalog_librarian.py (read-only, for understanding); C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; never name or call the five index-writing functions listed in CONSTRAINTS.md S6 (your L1 check greps for them; importing `build` from fixtures.build_fixture_index in tests is allowed and is how the probe tests do it).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use fastapi.testclient.TestClient in-process against explorer.app.create_app(settings). Never launch uvicorn (the probe is the only sanctioned launcher). No run_in_background, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/reader -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file; templates preserve every corpus character verbatim (CONSTRAINTS.md O19, O28).
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R02_reader.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Conventions every wave-2 page follows (ARCHITECTURE.md A14, A19; the probe reads exactly these):
- Server-rendered HTML page plus a JSON twin under /api/... built with dataclasses.asdict over the same view object, so the probe can cross-check the page against the API.
- The template extends base.html and sets `{% block html_attrs %}` to `data-mode="<value>"` (and `data-vector="available|unavailable"` where a retrieval call ran); every rendered result or list row carries `data-doc-id="<doc_id>"` and, where the row is one card among several for the same id, `data-card-ref="<card_ref>"`; the open document's region carries `data-selected-doc-id="<doc_id>"`.
- The adapter is reached only through explorer.app.get_adapter(request); when it raises RuntimeError, the HTML page renders a visible notice with HTTP 503 and the API returns 503 {"error": "corpus_adapter not built"} (the diagnostics precedent, explorer/diagnostics/routes.py).
- explorer.app.ADAPTER_ABSENT is the sentinel a test places on app.state.adapter to exercise that path (ARCHITECTURE.md A20).
- status and lifecycle_state are never joined, substituted, or merged into one control or one cell (CONSTRAINTS.md S4); a frozen status (Superseded, Archived -- CardRow.frozen) is marked with the visible text "frozen" next to it, never by colour alone.
- The authority notice is already rendered by base.html's footer; do not restate rank as correctness anywhere (S8).
- Tests: build a fixture index once per test session into tmp_path_factory via `from fixtures.build_fixture_index import build` (docs root = the absolute path of fixtures/corpus in your worktree; the fixture inventory is in docs/rounds/R01_probe.report.md), construct the app with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture docs root>))`, and drive it with TestClient -- every functional assertion is against that deterministic fixture. Add at least one read-only smoke test against the live index (Settings() defaults) asserting only shape (HTTP 200 and non-empty), guarded by the same before/after `corpus_cards_sha` + mtime check tests/corpus_adapter/conftest.py uses (copy that fixture's approach into your own conftest; do not import theirs). Use a hand-written fake adapter only for states the fixture cannot produce (vector channel unavailable; adapter absent).

Read first, inside your worktree, in this order: ARCHITECTURE.md (all of it; section 4.4 in detail; 4.1 for the adapter contract; A1, A5, A6, A10, A13, A14, A19, A20), CONSTRAINTS.md (S1, S4, S8, O1, O7, O8, O13, O15, O19, O21, O22, O23, O24, O28), explorer/models.py (CardRow, DocumentText, Heading, Edge, EdgeSet, RELATION_LABELS), explorer/config.py, explorer/app.py, explorer/faults.py (the stale_card and broken_jump hooks act inside the adapter; your page must make their effects visible, not mask them), explorer/corpus_adapter/adapter.py (card, cards_for_doc_id, read_document, read_card_text, edges_for, supersession_chain; note read_document raises ConfigurationError for an orphan card or a path outside the docs root and lets OSError propagate for a missing file), explorer/diagnostics/routes.py and its template (the shape to follow), explorer/web/templates/base.html and app.css, docs/rounds/R01_probe.report.md (fixture inventory; the ANL's line 15 phrase; the amendment sharing its parent's id; the superseded reference pair; the unresolved cites target), docs/rounds/R00_I2_card_schema_fields.report.md section 1 (card keys: identity.*, abstract, tags, depends_on, see_also, parent, superseded_by, location, notes). Then the task.

Task B6 -- build explorer/reader: the canonical document and card reader (PROMPT.md 5.4: "What does this artifact actually say, and what is its catalog identity?").

Deliver:
1. explorer/reader/service.py: `build_view(adapter, settings, card_ref: str, line: int | None) -> ReaderView | None` (dataclasses you define; None when `adapter.card(card_ref)` returns None). The view carries three distinct things and never blends them: (a) the **card file** as the record (S1): the raw text from `adapter.read_card_text(card)` and its parsed form via `yaml.safe_load` (identity.*, abstract, tags, depends_on, see_also, parent, superseded_by, location.local_path, notes -- whatever keys are present; unknown keys are shown raw, never dropped); (b) the **index row** (the CardRow the adapter returned) labelled "as indexed", with `indexed_at_utc`, `card_sha256`, and a per-field comparison of title / status / lifecycle_state / date / doc_type against the card file -- any difference is listed under "index row differs from card file" and exposed as `data-card-index-mismatch="<comma-separated field names>"` (empty when they agree; this is how a stale index row becomes visible instead of silently displayed); (c) the **document**: `adapter.read_document(card)` -> DocumentText; when read_document raises ConfigurationError (orphan card, or a path outside the docs root) or OSError (file missing), the page still renders the card with a visible notice naming the path and `data-body-missing="true"` (HTTP 200: the card exists; the record is the card file). Rendering: `.md` bodies through markdown-it-py (`MarkdownIt("commonmark", {"html": False})` with `table` and `strikethrough` enabled; raw HTML is escaped, so no sanitizer is needed; code fences verbatim); `.txt` bodies as preformatted text; `binary` kind (docx / pdf / ipynb, O15) -> card + path only with a notice. Every rendered heading gets `id="<slug>"` and `data-line="<line_no>"` taken from **DocumentText.headings** (the adapter's contract), matched to the rendered heading tokens by order and text -- never from the markdown renderer's own token map, because the adapter's headings are the backend-provided jump targets the page is accountable for; a heading whose `data-line` does not point at a source line that begins that heading's text is marked `data-line-verified="false"` and listed under "anchor mismatch" (O23: explicit, never a silent landing). Beside the rendered view, a **source view**: every line of DocumentText.lines numbered, each in an element with `id="L<n>"` and `data-line="<n>"`, verbatim (autoescaped, characters preserved, O19/O28). The `?line=N` request marks line N with the class `hit` and sets `data-jump-line="N"` on the document region, and the page states "jumped to line N"; N outside 1..line_count renders the visible text "line N not found (document has M lines)" with `data-jump-line=""` -- never a silent top-of-page landing. A table of contents lists the headings (level, text) linking to `#<slug>` and to `?line=<data-line>` ("go to source line"). The body is never summarized, truncated, or rewritten (PROMPT.md invariants).
2. Card panel content (labelled, `<dt>/<dd>`): identity (doc_id, doc_type, date, seq, title), status with the text "frozen" for Superseded / Archived, `lifecycle_state` shown for CMP cards only under its own label "CMP lifecycle state" (a non-CMP card carrying one shows it flagged "informational (non-CMP)"), programs, tags, project_knowledge, schema / naming-convention versions, last_human_review and reviewer, the abstract in full, dependencies: `depends_on` and `see_also` entries (id + note) each linked to `/doc/<card_ref>` when `adapter.cards_for_doc_id(id)` resolves it (link every matching card row when there are several -- O7) and otherwise shown as the literal id with the text "unresolved in index"; supersession / amendment state from `adapter.edges_for(card)`: an outgoing `supersedes` edge -> "superseded by <link>", incoming -> "supersedes <link>", `is_amendment` / an `amends` edge -> "amendment of <parent link>" and, on a parent, "amended by <link(s)>", incoming `campaign_child` -> "campaign child of <link>", and every unresolved edge as a literal id marked "unresolved"; when the card's status is Superseded, a banner "This version is superseded -- current head: <link>" using the last element of `adapter.supersession_chain(card.doc_id)` (O22); the local path (`location.local_path` from the card file, else the card's yaml_path, junction spelling) in a `<code>` element plus a read-only text input for copying; incoming / outgoing lineage links (typed, directional, using Edge.direction_label from explorer/models.py) as a list linking to `/doc/<card_ref>` of the related cards, with a link to `/lineage/<card_ref>` labelled "lineage view" (the lineage module lands in wave 3; the link may 404 until then -- do not build lineage here); the raw `.card.yaml` text inside a `<details>` element "Card source (.card.yaml)". Index-derived fields are labelled "from index" (S1).
3. explorer/reader/routes.py with register(app): `GET /doc/{card_ref:path}` (HTML; template reader/reader.html extending base.html; `?line=N` optional) and `GET /api/doc/{card_ref:path}` (JSON twin: dataclasses.asdict(view) including the document's `sha256`, `size_bytes`, `kind`, `line_count`, `headings`, `lines` (the verbatim lines -- the probe compares them to the fixture source), the card file text and parsed form, the index row, the mismatch list, the edges, and the jump state). Unknown or out-of-root card_ref -> HTTP 404 with a visible notice on the page ("no card at <card_ref> in the index") and `{"error": "card not found"}` on the API. Adapter-absent -> 503 as in diagnostics. Page structure: two landmarks -- `<section aria-labelledby="document-heading" data-selected-doc-id="<doc_id>" data-card-ref="<card_ref>" data-body-sha256="<sha256>" data-jump-line="...">` headed "Document", and `<aside aria-labelledby="card-heading">` headed "Card (catalog metadata)" -- visually and structurally distinct (PROMPT.md 5.4: "Do not visually collapse the two"). `<html data-mode="reader">`.
4. Template: explorer/reader/templates/reader/reader.html; accessible (landmarks, labels, headings in order; the source view in a `<pre>`-like block that scrolls horizontally inside its own container); the hidden `#keyhelp` element; optional explorer/reader/static/reader.css served as /static/reader/reader.css.
5. tests/reader/ (TestClient; fixture index built into tmp_path per the conventions; fixture docs root = fixtures/corpus in your worktree): `/doc/reports/RHACO-ANL-20260115-002_Fixture_Analysis.card.yaml` 200 with `data-selected-doc-id="RHACO-ANL-20260115-002"`, `data-body-sha256` equal to the sha256 of that fixture .md file on disk, the "Document" and "Card (catalog metadata)" headings both present, every fixture heading rendered with an `id` slug and a `data-line`, and no `data-line-verified="false"`; `?line=15` marks line 15 (which contains "quartz lattice lantern") with the `hit` class and `data-jump-line="15"`; `?line=9999` renders the "not found" text with an empty `data-jump-line`; the amendment card (`handoffs/RHACO-HND-20260115-003_Fixture_Handoff_Amendment_A1.card.yaml`) shows "amendment of" linking to the parent's card_ref, and the parent shows "amended by"; the HND page lists the incoming campaign_child from `RHACO-CMP-20260115-001` with the direction label and shows the unresolved cites target `RHACO-ANL-20260101-099` marked "unresolved"; the v1_0 reference page carries the Superseded banner linking to the v1_1 card_ref and the text "frozen"; the CMP page shows "CMP lifecycle state" with OPEN and the ANL page shows no lifecycle label; the docs_archive card_ref and a `..` traversal card_ref both 404; `data-card-index-mismatch=""` on the clean fixture; with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture root>, fault="stale_card"))` the same ANL page shows a non-empty mismatch attribute naming title and status and the "differs from card file" list (the fault is honoured because the db path is outside the RHACO trees -- explorer/faults.py); with `fault="broken_jump"` at least one heading is marked `data-line-verified="false"` and the anchor-mismatch list is non-empty; `/api/doc/...` carries the same sha256, lines, headings and mismatch fields as the page; adapter-absent -> 503; a live-index smoke: `/doc/handoffs/RHACO-HND-20260903-001_Corpus_Explorer_Autonomous_Build_Dispatch.card.yaml` 200 with `data-selected-doc-id="RHACO-HND-20260903-001"` and an "amended by" link, guarded by the corpus_cards_sha + mtime check.
Write the report at docs/rounds/R02_reader.report.md.
