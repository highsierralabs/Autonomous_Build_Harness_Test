> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S4-C1; role: module builder (probe), correction round; tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (correction round = two builders, under the cap of three); tier_escalation: none. Probe module round 2 of 4 -- this strand consumes one builder round of the 28. Dispatched by the build orchestrator, who is also the integrator, under Director ruling AC-3 (criterion 3 read progressively) and PROMPT.md section 2's repair rule: "If the probe cannot distinguish known-good from known-bad behavior, repair the probe before trusting downstream evaluation."

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe -- a git worktree of the workspace repository on branch build/probe, checked out at commit 62685dad6f40f91c6b1b84500a213f0a43ea8673 (main at the round-1 boundary; your round-1 work is merged there, and the diagnostics and corpus_adapter modules are present). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\probe; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: tools/probe_corpus_explorer.py, fixtures/** (fixtures/build_fixture_index.py and fixtures/corpus/**), tests/probe/**, and docs/probe-qualification/** (evidence output and README.md); plus your round report docs/rounds/R01C_probe.report.md. Do not modify explorer/** (any module, including explorer/diagnostics and explorer/faults.py), requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, or any other module's directory or tests. Do not edit, rename, or delete the existing run directories docs/probe-qualification/runs/20260904T033047Z and 20260904T034711Z (they are historical evidence; the integrator reclassifies them in build_state.json). If you need a change outside your paths, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through RHACO_corpus_index.connect(), never opened any other way, and not at all in this round (every run this round is against a fixture db); C:\RHACO\rhaco\RHACO_corpus_index.py; C:\RHACO\tools\RHACO_tool_catalog_librarian.py; C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; the five index-writing functions named in CONSTRAINTS.md S6 may appear in exactly one file you own, fixtures/build_fixture_index.py, and only reindex_full may be called there, only against the fixture db path after the path assertion (CONSTRAINTS O2/O3; ARCHITECTURE A12).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). Your probe is the ONLY sanctioned launcher of the explorer server: it spawns `<venv python> -m uvicorn explorer.app:create_app --factory --host 127.0.0.1 --port N` as a foreground child of its own process, waits on /healthz, and tears it down in a finally block. Unit tests other than the probe subprocess tests use fastapi.testclient.TestClient in-process or call pure functions. No run_in_background, no detached processes, no sleep-and-poll loops other than the readiness wait bounded at --timeout inside the probe.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/probe -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file.
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R01C_probe.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Read first, inside your worktree, in this order: ARCHITECTURE.md sections 4.8 and 6 and decision records A13, A19; CONSTRAINTS.md O12 and O19; docs/probe-qualification/README.md; tools/probe_corpus_explorer.py (all of it -- your round-1 module); explorer/faults.py; explorer/diagnostics/templates/diagnostics/diagnostics.html (the console_error hook at its end and the data-mode / data-vector attributes it sets on <html>); explorer/diagnostics/service.py (_find_last_probe_run reads the newest run_summary.json as an opaque JSON object -- keep run_summary.json a JSON object); docs/rounds/R01_probe.report.md (your round-1 report); docs/probe-qualification/runs/20260904T033047Z/diagnostics.json and docs/probe-qualification/runs/20260904T034711Z/run_summary.json (the two existing runs). Then the task.

Task C1 -- repair the probe's verdict predicate and add the qualification-state vocabulary (PROMPT.md section 2 repair rule; Director ruling AC-3).

Observed defect (the reason for this round): tools/probe_corpus_explorer.py::run_page_preset sets `ok = error is None`, so a page preset passes with an HTTP 404, console errors, and failed requests all recorded -- see docs/probe-qualification/runs/20260904T033047Z/diagnostics.json (http_status 404, one console error, one failed request, ok: true). The probe therefore cannot distinguish a known-good page from a known-bad one. Root cause noted for the ledger: PROMPT.md section 2's only stated failure rule is "fail explicitly if required observations cannot be collected", which supplies no observed-fault predicate; round 1 implemented the text literally. You repair the predicate; you do not edit PROMPT.md.

Deliver:
1. An observed-fault predicate for page presets: a pure function `evaluate_page(observation: dict, spec: PresetSpec) -> Verdict` where Verdict is a small dataclass (ok: bool, reasons: list[str], fault_detected: bool, fault_class: str | None), applied by run_page_preset after extraction and recorded into the preset JSON as "verdict" ("PASS" | "FAIL"), "reasons", "fault_detected", "fault_class"; keep "ok" as the boolean mirror of the verdict so existing readers keep working. Rules, all of them: (a) a navigation or extraction error -> FAIL (unchanged); (b) http_status >= 400 -> FAIL unless the spec declares `expected_status` equal to that status (a new optional PresetSpec field; no preset declares one this round; "route absent" is no longer an accepted state -- the diagnostics module is merged); (c) any console error -> FAIL; (d) any failed request (a requestfailed event, or a response with status >= 400) -> FAIL; (e) preset-specific assertions on the data-* observations, declared on the spec in a small declarative form (e.g. `data_mode_in={"hybrid", "hybrid-degraded-lexical"}`, `require_title=True`) -> FAIL with the assertion's name in reasons. Uncaught page exceptions: in Playwright an uncaught `throw` inside a page script arrives on `page.on("pageerror")`, not necessarily on the console event -- register a pageerror listener, record its messages in the observation (a `page_errors` list, and also mirror them into console_errors with type "pageerror" so rule (c) covers them), and verify empirically which event carries the diagnostics template's throw; state the finding in the report. A FAIL whose reason is attributable to an injected fault sets fault_detected=True and fault_class: this round, a page error or console error whose text contains "RHACO_EXPLORER_FAULT console_error" (the exact string explorer/diagnostics/templates/diagnostics/diagnostics.html throws when the fault is active) -> "KB5". Keep the fault-class mapping in one table next to PRESET_REGISTRY (KB1 wrong_doc_for_id, KB2 stale_card, KB3 reverse_edges, KB4 broken_jump, KB5 console_error) so wave 2 can add the attribution rules for KB1-KB4 without restructuring.
2. The qualification-state vocabulary in run_summary.json (Director ruling AC-3): `qualification_state` in {FRAMEWORK_SMOKE_PASS, QUALIFICATION_PASS, PRODUCT_EVIDENCE_PASS, FAIL, INCOMPLETE}; `product_evidence_eligible` (bool); `qualified_failure_classes` (list of KB class names); plus `run_kind` in {"qualification", "smoke", "product"} and the existing `fault`. Derivation, exactly:
   - run_kind = "qualification" when --fault is given; else "product" when the resolved --db path lies under a RHACO tree (reuse explorer.faults.is_under_rhaco_tree); else "smoke".
   - Any selected preset still reporting not_implemented_yet makes the run INCOMPLETE (record the list under `incomplete_presets`); a run with stubs is never a PASS of any kind.
   - qualification run: QUALIFICATION_PASS iff every page-kind preset the injected fault touches (for console_error: every page-kind preset run) reported FAIL with fault_detected=True and fault_class equal to the injected fault's class, and every other preset passed; then qualified_failure_classes = [that class] and product_evidence_eligible = false. Otherwise FAIL (the probe did not detect a known-bad) -- record why under `reasons`.
   - smoke run (fixture db, no fault): FRAMEWORK_SMOKE_PASS iff every preset passed; qualified_failure_classes = []; product_evidence_eligible = false. Any FAIL -> FAIL.
   - product run (live db, no fault): PRODUCT_EVIDENCE_PASS only when every preset passed AND every preset run is covered by the qualification ledger (below) for every failure class it requires; otherwise, if every preset passed, FRAMEWORK_SMOKE_PASS (mechanism evidence only) with product_evidence_eligible = false and the uncovered presets listed under `product_evidence_blocked_by`; any FAIL -> FAIL.
   - Per-preset required failure classes, declared on the spec (`required_classes`): healthz -> []; diagnostics -> ["KB5"]; W1 -> ["KB1","KB5"]; W2 -> ["KB1","KB2","KB5"]; W3 -> ["KB1","KB5"]; W4 -> ["KB2","KB5"]; W5 -> ["KB3","KB5"]; W6 -> ["KB3","KB5"]; W7 -> ["KB5"]; W8 -> ["KB2","KB5"]; W9 -> ["KB4","KB5"]; W10 -> ["KB1","KB2","KB4","KB5"]; KG -> []; KB1..KB5 -> [] (they are qualification cases themselves). Record this table and the AC-3 reading in docs/probe-qualification/README.md: Q0 probe mechanism (server lifecycle, screenshot, DOM capture, console/network/page-error capture, the observed-fault predicate) -- this round; Q1 search surfaces (KB1, KB2, known-good exact-id and lexical phrase) -- wave 2; Q2 reader (KB4, known-good body-and-card-match) -- wave 2; Q3 lineage (KB3, known-good typed edge direction) -- wave 3; FINAL the complete known-good + known-bad suite before the critic's first round. State in the README that runs 20260904T033047Z and 20260904T034711Z predate this vocabulary and are classified FRAMEWORK_SMOKE_PASS by the integrator (mechanism evidence only; not qualification, not product evidence).
   - The qualification ledger: docs/probe-qualification/qualification_ledger.json, a JSON object of the form {"classes": {"KB5": {"run": "<run dir path relative to the workspace root>", "recorded_utc": "..."}}, "runs": [{"run": "...", "qualification_state": "...", "classes": [...]}]}. A QUALIFICATION_PASS run appends its class (and its run entry) to the ledger -- the only file the probe writes outside its --out directory; it is evidence and is committed. A product run reads the ledger and never writes it. Add `--ledger PATH` (default <workspace root>/docs/probe-qualification/qualification_ledger.json, resolved from the probe file's own location, so a worktree run writes the worktree's ledger) so tests can point it at a temporary path. Ledger writes are idempotent and ASCII.
   - Exit code: 0 iff qualification_state is FRAMEWORK_SMOKE_PASS, QUALIFICATION_PASS, or PRODUCT_EVIDENCE_PASS; 1 for FAIL; 3 for INCOMPLETE. Keep `verdict` in run_summary.json as "PASS" for the three pass states, "FAIL", or "INCOMPLETE" (the existing healthz smoke test expects "PASS" for a clean healthz-only smoke run and must stay green). Print the qualification_state on stdout.
3. Preset changes this round: the diagnostics preset now expects HTTP 200, data-mode present with a value in {"hybrid", "hybrid-degraded-lexical"}, data-vector in {"available", "unavailable"}, and a non-empty title; required_classes ["KB5"]. The healthz preset keeps its rule (status == "ok"). W1-W10, KG, KB1-KB5 stay not_implemented_yet stubs this round (wave 2 fills them), but a run that selects them is INCOMPLETE, never a PASS. Update the module docstring (PURPOSE / USAGE / OUTPUT / VERSION HISTORY: add v1.1 dated 2026-09-04 with this round's reason: observed-fault predicate, page-error capture, AC-3 vocabulary, ledger).
4. Q0 qualification evidence, committed under docs/probe-qualification/runs/<utc stamp>/ (each directory written by one probe invocation; both against a fixture index you build first with `fixtures/build_fixture_index.py --docs-root fixtures/corpus --db fixtures/fixture_index.db --force` -- the db is gitignored and rebuilt on demand): (i) clean fixture, `--preset diagnostics` -> expect run_kind smoke, diagnostics verdict PASS with data-mode "hybrid-degraded-lexical" (the fixture has no vector set), qualification_state FRAMEWORK_SMOKE_PASS; (ii) `--fault console_error --preset diagnostics` -> expect diagnostics verdict FAIL with fault_detected true / fault_class "KB5", qualification_state QUALIFICATION_PASS, qualified_failure_classes ["KB5"], and the ledger gaining KB5 with that run's path. Optionally (iii) `--preset all` on the clean fixture to demonstrate INCOMPLETE (exit 3). Each run passes an explicit free `--port`; the probe spawns uvicorn as its foreground child and tears it down. Paste every run_summary.json in your report. Do not run against the live index this round (the integrator runs the production path after the merge).
5. Tests (tests/probe/), all free of the five index-writing names (import `build` from fixtures.build_fixture_index as the existing tests do): unit tests for evaluate_page covering each rule (a)-(e), the expected_status exception, and the fault attribution (a synthetic observation carrying the exact console_error text as a page error -> fault_detected True / KB5; an unrelated console error -> FAIL with fault_detected False); a unit test for the qualification-state derivation over synthetic preset results (qualification with detection -> QUALIFICATION_PASS; qualification without detection -> FAIL; smoke clean -> FRAMEWORK_SMOKE_PASS; product with a preset not covered by the ledger -> FRAMEWORK_SMOKE_PASS with product_evidence_eligible False and product_evidence_blocked_by populated; product fully covered by a synthetic ledger -> PRODUCT_EVIDENCE_PASS; any not_implemented preset -> INCOMPLETE); the existing subprocess healthz smoke test kept green; one new subprocess test running `--preset diagnostics --fault console_error --ledger <tmp path>` against a tmp fixture db (free port, bounded timeout) asserting the run_summary fields of item 4(ii), exit code 0, and that the tmp ledger records KB5 -- the committed ledger must never be touched by tests.
Write the report at docs/rounds/R01C_probe.report.md. In "Result" state the module round consumed (probe round 2 of 4) and, in "Unresolved uncertainty", what remains for wave 2 (real W1-W10 / KG / KB1-KB5 runners; KB1, KB2, KB4 attribution rules) and wave 3 (KB3, W5, W6).
