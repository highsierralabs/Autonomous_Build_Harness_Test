> Device-safety prohibition (RHACO Guide v1.3 D-12, applied to a
> non-device build). You may launch only the workspace's own Python
> interpreter running workspace code — tests, lint, the explorer server
> via the probe. You must not launch, spawn, import as a process, or
> invoke any `RHACO_logger_*`, `RHACO_sidecar_*`, `RHACO_launcher*`,
> `RHACO_tool_*` (other than importing `RHACO_tool_catalog_librarian` for
> read-only walk/scan calls), or anything under `C:\RHACO\rhaco\` other
> than importing `RHACO_corpus_index`. You must not open any device on
> any transport (USB, BLE, serial). You must not read or write anything
> under `C:\RHACO\data\`. "Verify" means re-derive from files on disk or
> from the fixture; it never means run an acquisition or restart a
> production process. Violation is a halt, not a retry.

> Foreground only (RHACO Guide v1.3 D-3). Do not use `run_in_background`,
> do not detach, do not sleep-and-poll a background job. Work that exceeds
> your budget is walked in sequential foreground invocations or returned as
> WAITING. A backgrounded process is a halt, not a retry.

---
Dispatch record (RHACO-HND-20260903-001 section 2 I as amended by A1.3): strand_id S4-B5; role: module builder (search); tier: mid; model requested: Claude Sonnet 5; agent_type: general-purpose (write-scoped to the worktree below); pattern_primary: #3 candidate (worktree-isolated builder); execution_mode: concurrent (wave 2 = three builders, the cap); tier_escalation: none. Round 2 (wave 2) of the RHACO Corpus Explorer autonomous build; search module round 1 of 4; dispatched by the build orchestrator, who is also the integrator.

Your worktree: C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\search -- a git worktree of the workspace repository on branch build/search, checked out at commit c96664dd82e19da730cbd741f0e2ace43501121d (the correction-boundary commit: corpus_adapter, diagnostics, and the probe framework are merged and green). Your interpreter: C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe (the workspace's own venv: Python 3.13; fastapi, uvicorn, jinja2, playwright + chromium, pytest, httpx, pyyaml, sqlite-vec, markdown-it-py, mdit-py-plugins, ruff). Run every command with the worktree as the current directory, e.g. `cd C:\highsierralabs\RHACO_Corpus_Explorer\.worktrees\search; C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/search -q`.

Working rules:
1. Write only inside your worktree, and there only under your owned paths: explorer/search/** (service.py, routes.py, templates/search/*.html, and, if you need one, static/search.css served under /static/search/ by the app factory) and tests/search/**; plus your round report docs/rounds/R02_search.report.md. Do not modify explorer/app.py, explorer/config.py, explorer/models.py, explorer/faults.py, explorer/web/** (base.html, app.css, app.js -- the web builder's, wave 3), explorer/corpus_adapter/**, explorer/diagnostics/**, tools/**, fixtures/**, requirements.txt, ruff.toml, .gitignore, PROMPT.md, RATIONALE.md, DISPATCH_PARAMETERS.md, CONSTRAINTS.md, ARCHITECTURE.md, SCOPE.md, build_state.json, docs/LAYERS.md, docs/REFERENCE.md, docs/probe-qualification/**, or any other module's directory or tests. If you need a change there, put a "Change requests" section in your report (requestor, affected contract/path, evidence, compatibility impact, migration, invalidated tests/probes) and code against the current contract.
2. Outside the worktree you may only READ: C:\RHACO\docs\** (the corpus); C:\RHACO\index\corpus_index.db -- and only through the adapter (explorer.corpus_adapter.CorpusAdapter, which alone calls RHACO_corpus_index.connect()); never open the db any other way, never import RHACO_corpus_index or RHACO_tool_catalog_librarian yourself (the adapter is the only importer, ARCHITECTURE.md section 2); C:\RHACO\rhaco\RHACO_corpus_index.py and C:\RHACO\tools\RHACO_tool_catalog_librarian.py (read-only, for understanding); C:\RHACO\working\eval\gold_queries.yaml and gold_queries_v1_2.yaml. Nothing else under C:\RHACO\ or C:\highsierralabs\RHACO\ (C:\RHACO is a junction to C:\highsierralabs\RHACO -- one tree, both spellings out of bounds beyond the list). Never write to the live index; never edit the RHACO module; never name or call the five index-writing functions listed in CONSTRAINTS.md S6 (your L1 check greps for them; importing `build` from fixtures.build_fixture_index in tests is allowed and is how the probe tests do it).
3. Processes: only the venv interpreter running workspace code (pytest, ruff, your own scripts). No server process at all: tests use fastapi.testclient.TestClient in-process against explorer.app.create_app(settings). Never launch uvicorn (the probe is the only sanctioned launcher). No run_in_background, no detached processes, no sleep-and-poll loops.
4. Git: commit on your branch in small increments with imperative ASCII subjects; `git add` specific paths only (never -A); no amend, no rebase, no push, no checkout of another branch, no merge. Leave the worktree clean (everything committed) when you finish and report `git rev-parse HEAD`.
5. Gate before you finish: (a) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m ruff check explorer tools tests --output-format concise` clean; (b) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe -m pytest tests/search -q` green; (c) `C:\highsierralabs\RHACO_Corpus_Explorer\.venv\Scripts\python.exe tools/l1_index_write_check.py` PASS. Paste the exact commands and their output tails in the report.
6. Evidence discipline: cite file paths and line numbers; every "tested" claim names the test; anything not verified is UNVERIFIED. ASCII-only stdout in every script you write (set PYTHONIOENCODING=utf-8 for child processes); UTF-8 with LF line endings for every file; templates preserve every corpus character verbatim (CONSTRAINTS.md O19, O28).
7. Timestamps and identity: run (Get-Date).ToUniversalTime().ToString("o") as your FIRST tool call and again when you finish; copy the sentence from your own system prompt that names your model.
8. Return your result through the StructuredOutput tool: the header fields by name, and `report` = the markdown of your round report (the same text saved to docs/rounds/R02_search.report.md, which must have sections: Header; Change; Evidence (checks actually run and their results); Material alternatives; Decisions; Result; Unresolved uncertainty; Change requests; Assumptions).

Conventions every wave-2 page follows (ARCHITECTURE.md A14, A19; the probe reads exactly these):
- Server-rendered HTML page plus a JSON twin under /api/... built with dataclasses.asdict over the same view object, so the probe can cross-check the page against the API.
- The template extends base.html and sets `{% block html_attrs %}` to `data-mode="<value>"` (and `data-vector="available|unavailable"` where a retrieval call ran); every rendered result or list row carries `data-doc-id="<doc_id>"` and, where the row is one card among several for the same id, `data-card-ref="<card_ref>"`; the open document's region carries `data-selected-doc-id="<doc_id>"`.
- The adapter is reached only through explorer.app.get_adapter(request); when it raises RuntimeError, the HTML page renders a visible notice with HTTP 503 and the API returns 503 {"error": "corpus_adapter not built"} (the diagnostics precedent, explorer/diagnostics/routes.py).
- explorer.app.ADAPTER_ABSENT is the sentinel a test places on app.state.adapter to exercise that path (ARCHITECTURE.md A20).
- status and lifecycle_state are never joined, substituted, or merged into one control or one cell (CONSTRAINTS.md S4); a frozen status (Superseded, Archived -- CardRow.frozen) is marked with the visible text "frozen" next to it, never by colour alone.
- The authority notice is already rendered by base.html's footer; do not restate rank as correctness anywhere (S8).
- Tests: build a fixture index once per test session into tmp_path_factory via `from fixtures.build_fixture_index import build` (docs root = the absolute path of fixtures/corpus in your worktree; the fixture inventory is in docs/rounds/R01_probe.report.md), construct the app with `create_app(Settings(db_path=<tmp db>, docs_root=<fixture docs root>))`, and drive it with TestClient -- every functional assertion is against that deterministic fixture. Add at least one read-only smoke test against the live index (Settings() defaults) asserting only shape (HTTP 200 and non-empty), guarded by the same before/after `corpus_cards_sha` + mtime check tests/corpus_adapter/conftest.py uses (copy that fixture's approach into your own conftest; do not import theirs). Use a hand-written fake adapter only for states the fixture cannot produce (vector channel unavailable; adapter absent).

Read first, inside your worktree, in this order: ARCHITECTURE.md (all of it; section 4.3 in detail; 4.1 for the adapter contract; A4, A7, A14, A19, A20), CONSTRAINTS.md (S2, S3, S8, O4, O5, O6, O7, O20, O21), docs/REFERENCE.md (all of it -- the dispositions your labels must state), explorer/models.py (ModeResult, ResultItem, Evidence, VectorAvailability, CardRow, COMPONENT_RANK_NOT_EXPOSED), explorer/config.py, explorer/app.py, explorer/corpus_adapter/adapter.py (resolve_identifier, cards_for_doc_id, search_lexical, search_hybrid, search_graph, lexical_excerpt, vector_availability; the exact degradation notices HYBRID_DEGRADED_NOTICE / GRAPH_DEGRADED_NOTICE), explorer/diagnostics/routes.py and its template (the shape to follow), explorer/web/templates/base.html, explorer/web/static/app.js (the `/` shortcut focuses `input[name="q"]` -- name your query box that), docs/rounds/R01_probe.report.md (fixture inventory: the E000777 event id, the "quartz lattice lantern" phrase at line 15 of the ANL, the shared HND id). Then the task.

Task B5 -- build explorer/search: the human search surface over the existing retrieval modes (PROMPT.md 5.2 / 5.3: "Where did I write about this?").

Deliver:
1. explorer/search/service.py: `run_search(adapter, settings, query: dict) -> SearchView` (a dataclass you define) with: `q` (stripped; empty -> the page renders the form and the mode explanation, runs nothing); `mode` in {identifier, lexical, hybrid, graph}, default **hybrid** (S3; the accepted flat-hybrid default -- never anything else); identifier mode = `adapter.resolve_identifier(q)` then `adapter.cards_for_doc_id(id)` for each returned id, listing EVERY card row per id (O7: a hand and its amendments share one id; never pick one silently; an id with no card rows is listed as the literal id with the text "no card row in the index"); lexical mode = `adapter.search_lexical(q, doc_type_filter=..., path_glob=..., max_results=...)` with the two module-side filters exposed as labelled controls ("these filters apply to lexical search only", O5) and `max_results` bounded to the module cap; hybrid = `adapter.search_hybrid(q, top_k)` and graph = `adapter.search_graph(q, top_k)` with `top_k` from the query (default 10, max 50); post-hoc narrowing for hybrid/graph results by doc_type / status / lifecycle_state / program (applied to the returned items' cards in Python, never to the retrieval call), exposed as a separate control block labelled exactly "narrow results after retrieval (ranking unchanged)" and reported in the view as `narrowed_from` / `narrowed_to` counts; `mode_effective` and `degradation_notice` taken from the ModeResult verbatim -- when the vector channel is unavailable the mode badge shows `hybrid-degraded-lexical` (or `graph-degraded`) and the notice text is rendered exactly ("Hybrid unavailable: using lexical retrieval. Result ordering is lexical-only."); the label "Hybrid" is never shown for a degraded result (S3/O4). Graph mode is labelled "graph / lineage (auxiliary)" with the REFERENCE.md standing (Recall@10 0.575; auxiliary lineage mode); the mode explanation block states the standing of each mode from docs/REFERENCE.md section 1 in one line each and states that hybrid-rerank is not offered (documented-FAIL batch mode). Result rows carry `rank`, `doc_id`, every card row for the id (title, doc_type, date, status + "frozen" marker, lifecycle_state for CMP, card_ref), the document path (lexical hits: the body path; others: the card's doc_ref), and the open link: lexical hits link to `/doc/<card_ref>?line=<line_no>` (the reader's line target), other modes to `/doc/<card_ref>`. Truncation: lexical shows `returned` of `total_matched` and a visible "truncated at N" line when the module truncated (O20).
2. Evidence disclosure per result (PROMPT.md 5.3; O6): a `<details>` element labelled "Why this result" carrying ONLY observed fields from Evidence: exact identifier match (yes/no), active retrieval mode (mode_effective), returned rank ("retrieval rank in mode X"), matched line / excerpt with context lines (lexical hits: `line_no`, `line`, `context_before`, `context_after`; hybrid/graph: the labelled lexical-lookup excerpt when present, marked "lexical excerpt, not the hybrid evidence", else "no literal match for this query in the document"), graph relation and seed (for graph mode: "expansion of a seed; relation not exposed by the current RHACO API"), degradation status, and the literal line `Component rank: not exposed by current RHACO retrieval API` for hybrid and graph results. Nothing is reconstructed; no score bars, no percentages.
3. explorer/search/routes.py with register(app): `GET /search` (HTML; template search/search.html extending base.html) and `GET /api/search` (JSON twin: dataclasses.asdict(view) -- including every result's evidence). `<html data-mode="<mode_effective or 'search' when no query ran>" data-vector="available|unavailable">`; every result row carries `data-doc-id` (and `data-card-ref` per card row within it, plus `data-rank`); the query box is `<input name="q">` with a label; the mode selector is a labelled control; the active mode badge carries `data-mode-effective`. Adapter-absent -> 503 notice / JSON error as in diagnostics.
4. Template: explorer/search/templates/search/search.html; accessible; the hidden `#keyhelp` element; preserve corpus characters verbatim in excerpts (autoescaped, never re-encoded); optional explorer/search/static/search.css.
5. tests/search/ (TestClient; fixture index built into tmp_path per the conventions; the fixture has NO vector set, so on the fixture `search_hybrid` is degraded by construction -- use that as the degraded path and a fake adapter for the available path): identifier mode `q=E000777` lists the EVT card, `q=RHACO-HND-20260115-003` lists two card rows (hand + amendment) with distinct card_ref, an unknown id yields the "no card row" line; lexical `q=quartz lattice lantern` returns the ANL with `line_no` 15, the excerpt containing the phrase, and an open link ending in `?line=15`; lexical `doc_type_filter=EVT` narrows; hybrid on the fixture returns `mode_effective == "hybrid-degraded-lexical"`, renders the exact notice, and never shows the bare label "Hybrid" as the active mode; graph on the fixture likewise degrades with its notice; a fake adapter with vector available -> mode badge `hybrid`, no notice, and every result's evidence carries the literal component-rank line; the narrowing control reduces a hybrid result set by doc_type and reports narrowed_from/narrowed_to without changing the surviving ranks; empty `q` renders the form and runs no search (assert no result rows and `data-mode="search"`); `/api/search` returns the same ranks, ids and evidence as the page; adapter-absent -> 503; a live-index smoke: `/search?q=RHACO-CMP-20260903-001&mode=identifier` 200 with one result row, guarded by the corpus_cards_sha + mtime check.
Write the report at docs/rounds/R02_search.report.md.
