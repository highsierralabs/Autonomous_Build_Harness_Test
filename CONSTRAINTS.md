# CONSTRAINTS.md -- architectural inputs (PROMPT.md section 1 step 5)

Written 2026-09-04 before any feature code, from PROMPT.md (Standing RHACO Constraints 1-8), DISPATCH_PARAMETERS.md (the hand's section 2 as amended by Amendment A1), the section 0 pre-flight findings (`C:\RHACO\working\preflight_findings_HND-20260903-001.md`), and the round-0 inspection reports under `docs/rounds/` (I1 corpus-index contracts, I2 card schema, I3 retrieval dispositions, I4 corpus conventions). Each row records the constraint, its basis, the design consequence, and whether it produces a user-visible limitation; a `yes` in the last column has a matching row in `SCOPE.md`. Line references are to `C:\RHACO\rhaco\RHACO_corpus_index.py` unless another file is named.

## A. Standing RHACO constraints (PROMPT.md)

| ID | Constraint | Basis | Design consequence | User-visible limitation |
|---|---|---|---|---|
| S1 | **Authoritative corpus boundary.** Sibling `.card.yaml` files and canonical documents under `C:\RHACO\docs\` are the record; `corpus_index.db` is derived, rebuildable, non-authoritative | PROMPT.md Standing Constraint 1; module docstring lines 20-24 ("DERIVED, REBUILDABLE, and NON-AUTHORITATIVE ... never holds record"); index `OUTPUT` note line 38-39 (the db lives outside the walk root) | The reader renders the canonical file read from disk (`cards.doc_path`), never the `fts_docs.body` copy; the card panel reads the `.card.yaml` from disk (`cards.yaml_path`) and shows index-derived fields as "from index"; every page carries the authority notice; diagnostics states the db path and calls it non-authoritative | no (notice only) |
| S2 | **Existing retrieval substrate.** Reuse `RHACO_corpus_index.py` through its public API; mirror the documented public tool surface; reimplement no ranking | PROMPT.md Standing Constraint 2; DISPATCH_PARAMETERS.md item B (import form: path entries `C:\RHACO\rhaco`, `C:\RHACO\tools`); I1 section 4 (19 public functions) | `corpus_adapter` is the only module that imports `RHACO_corpus_index`; retrieval calls are limited to `connect`, `get_meta`, `search_fts`, `resolve_identifier`, `search_hybrid`, `search_graph`, `freshness_check`, `doc_id_from_filename`; no RRF, bm25, or embedding code exists in the workspace | no |
| S3 | **Accepted default.** Flat hybrid is the default; graph is auxiliary lineage retrieval; hybrid-rerank is a documented negative result | docs/REFERENCE.md section 1 (RHACO-ANL-20260712-001 lines 16, 34, 36, 48, 60-61; RHACO-CHG-20260712-002 section 2.1) | Search default = `search_hybrid`; graph labelled "lineage (auxiliary)"; `search_hybrid_rerank` is never called; diagnostics reports rerank's standing only | yes -- SCOPE "Hybrid-rerank retrieval mode in the UI" |
| S4 | **Card semantics.** `identity.status` (library-artifact lifecycle, all types) and CMP-only `identity.lifecycle_state` (investigation lifecycle) are separate channels | Card Schema v1.9 section 5.1.1 lines 132-142 (quoted in I2 section 5); librarian warning class `lifecycle_state_on_non_cmp` (I2 section 5) | Two independent facets (`cards.status`, `cards.lifecycle_state`) and two display fields; `lifecycle_state` shown for CMP rows; a non-CMP row carrying one shows it flagged "informational (non-CMP)"; no template, filter, or query ever joins or substitutes one for the other | no |
| S5 | **Local-first.** Normal browsing and search work without a frontier model; the only model dependency is the module's vector channel (Ollama at `OLLAMA_HOST`, default `http://127.0.0.1:11434`, `nomic-embed-text` CPU floor) through the module | PROMPT.md Standing Constraint 5; I1 section 2 (`OLLAMA_HOST` line 330, `CPU_FLOOR_TAG` line 376, `MODEL_REGISTRY` lines 345-374) | No LLM, no network egress except the module's own local Ollama call; when Ollama or sqlite-vec is unavailable the explorer keeps working on identifier + lexical retrieval and says so | yes, as a runtime state -- semantic search depends on the local embedder; surfaced as the degradation notice, not a SCOPE row |
| S6 | **Read-only V1 surface.** No edit to documents, cards, index rows, tags, relationships, or classifications through the UI | PROMPT.md Standing Constraint 6 and section 11; DISPATCH_PARAMETERS.md item C as amended by A1.5 | No route accepts a write; the adapter never calls `reindex_full`, `reindex_pair`, `reindex_fts_pair`, `reindex_vec`, or `emit_gold_queries` and never opens `corpus_index.db` except through `connect()`; L1 grep enforces per round (O2) | yes -- SCOPE editing rows |
| S7 | **Windows host assumptions.** Do not hard-code more paths than the module already does; expose configuration | PROMPT.md Standing Constraint 7; I1 section 9 (module hard-coded paths: `C:\RHACO\index\corpus_index.db`, `C:\RHACO\docs`, `C:\RHACO\models\bge-reranker-v2-m3`, `C:\RHACO\working\eval\...`) | One config object: db path (default `RHACO_corpus_index.DEFAULT_DB`, override `RHACO_EXPLORER_DB`), docs root (default `RHACO_corpus_index.BODY_SCAN_ROOT`, override `RHACO_EXPLORER_DOCS_ROOT`, display and confinement only), host/port; no other path constants in the workspace; module constants are read, never assigned, outside `fixtures/build_fixture_index.py` | no |
| S8 | **No hidden epistemic uplift.** Rank means retrieval relevance only | PROMPT.md Standing Constraint 8 and section 5.6 authority notice | Result rank is labelled "retrieval rank (mode X)"; the authority notice appears on the search and diagnostics pages; no badge, score bar, or wording implies correctness, authority, or publication readiness | no (notice only) |

## B. Observed constraints (this build)

| ID | Constraint | Basis | Design consequence | User-visible limitation |
|---|---|---|---|---|
| O1 | **Read-path boundary.** The build reads only `C:\RHACO\docs\**`, `C:\RHACO\index\corpus_index.db`, `C:\RHACO\rhaco\RHACO_corpus_index.py`, `C:\RHACO\tools\RHACO_tool_catalog_librarian.py`, `C:\RHACO\working\eval\gold_queries*.yaml`; nothing else under `C:\RHACO\` or `C:\highsierralabs\RHACO\` (in particular not `data\`, not other `rhaco\` modules, not the MCP server source) | DISPATCH_PARAMETERS.md item B; section 2 L D-12 block | The reader serves a file only if its resolved real path lies under the configured docs root (junction-aware, O13); no directory browsing outside the docs root; the "public retrieval behavior" the explorer mirrors is the *documented* tool surface (I4), not the server source | no |
| O2 | **Index-write prohibition with a single allowlist.** The five index-writing names appear outside `CONSTRAINTS.md` / `docs/` in exactly one file, `fixtures/build_fixture_index.py`; the db is opened only through the module's `connect()` | DISPATCH_PARAMETERS.md item C as amended by A1.5 | `tools/l1_index_write_check.py` greps the workspace each round and fails on any hit outside the allowlisted file; result recorded per round in `build_state.json` | no |
| O3 | **Fixture substrate rule (user-invisible).** The fixture index is built only by `fixtures/build_fixture_index.py`, which passes the fixture **db path by argument** (`connect(db_path)` / `reindex_full(scan, db_path=...)`) and **monkeypatches `RHACO_corpus_index.BODY_SCAN_ROOT` in-process** for the body scan, because `reindex_full` calls `iter_body_docs()` with no root (line 855) and `_in_body_scope` / `_rel_posix` read the constant; before any write it asserts the resolved db path is not `C:\RHACO\index\corpus_index.db` and lies under neither `C:\RHACO\` nor `C:\highsierralabs\RHACO\` | DISPATCH_PARAMETERS.md item F as amended by A1.5; section 0 item 12 VERIFY answer (findings file, "S3 item 12"); I1 section 4 (`connect`, `reindex_full`, `iter_body_docs` signatures) | The monkeypatch is confined to the fixture builder's process and is never written to disk; the module file is never edited (`BLOCKED`, PROMPT.md section 11); the librarian side needs no override (`scan_library(root, ...)` / `walk_library(root)` take explicit roots) | no -- recorded here as a **user-invisible constraint** per A1.5 (iii) |
| O4 | **Sanctioned degradation mechanism.** Semantic-channel disable = `RHACO_CORPUS_DISABLE_VEC=1` in the server process (typed `VecUnavailable`; `search_hybrid` degrades to identifier short-circuit + FTS5 with no raise, `search_graph`'s seed channel likewise); rerank disable = `RHACO_CORPUS_DISABLE_RERANK=1`; nothing else | DISPATCH_PARAMETERS.md item G; I1 section 6 (lines 1404-1410, 1572-1573, 1439-1440); section 0 item 13 (W7 mechanism proven in a child process) | Because the module degrades silently, the adapter determines vector availability itself before labelling results: `VEC_DISABLE_ENV` unset, `SELECT vec_version()` succeeds on the module's own connection (documented read-only diagnostic query, D-Q1 in ARCHITECTURE.md), the CPU-floor vec table exists (`get_meta(conn, "vec_rows__nomic-embed-text")`), and a one-item `embed_query_cpu` probe succeeds; any failure sets `vector_available=false` and the UI relabels the mode ("Hybrid unavailable: using lexical retrieval. Result ordering is lexical-only.") | yes, as a runtime state (degradation notice) |
| O5 | **Filters apply to lexical only.** `search_fts(query, doc_type_filter, path_glob, ...)` is the only search function with filter parameters; hybrid, rerank, and graph accept none | I1 section 7; RHACO-CHG-20260712-002 section 2.1 lines 33-36 (I3 section 4) | Catalog facets are catalog-side (parameterized SQL over `cards`, `card_programs`, `card_tags`, D-Q2..D-Q6); on the search page, facet narrowing of hybrid or graph results is a labelled post-hoc narrowing of the returned doc_ids ("narrowed after retrieval; ranking unchanged") and is never called filtering | yes -- SCOPE "Retrieval-side filtering on hybrid and graph modes" |
| O6 | **Component evidence not exposed.** `search_hybrid` and `search_graph` return doc_ids only; `search_fts` returns `path, doc_id, line_no, line, context_before, context_after` | I1 section 4 return shapes | Evidence panel: mode, fused rank, identifier short-circuit flag, degradation state; hybrid rows show `Component rank: not exposed by current RHACO retrieval API`; lexical rows show line and context | yes -- SCOPE "Component-level evidence" and "Matching excerpt / line for hybrid and graph results" |
| O7 | **Shared identifiers.** Amendment children carry the parent's `doc_type/date/seq`, so their canonical doc_id equals the parent's; `cards` is keyed by `yaml_path` (PRIMARY KEY, line 440) with `doc_id` merely indexed; `resolve_identifier` returns every doc_id matching an alias; REF-style documents (versioned reference names) have `doc_id` = card filename stem and **no** alias rows | Card Schema v1.9 section 5.9 lines 300-302; I2 sections 3-4; I1 section 8 (`_card_id_aliases` lines 742-764, line 750 "unresolvable by construction") | Document lookup is by `yaml_path` (or `doc_path`), never by `doc_id` alone; an identifier query lists every card row it matches (parent and amendments) and never picks one silently; identifier mode is documented as covering canonical ids, bare `TYPE-YYYYMMDD-SEQ`, and `E######` event ids only -- reference-document names are found through the catalog or lexical search | yes -- SCOPE "Unique document per identifier" and "Identifier mode for reference-document names" |
| O8 | **Edge semantics are the module's.** Four relations: `cites` (from = citing card, to = target; the only relation that can be `resolved=0`, with `to_id` = the raw literal reference), `supersedes` (from = the superseded document, to = its replacement), `amends` (from = the amendment's **filename stem**, to = the shared parent canonical id), `campaign_child` (from = CMP, to = child) | I2 section 3 (`emit_edges` docstring lines 937-959; code lines 978-1025); DDL lines 481-492 | Lineage renders each relation with its own direction label ("cites", "is superseded by", "amends", "has child"); unresolved targets are shown as literal ids marked unresolved and never dropped or guessed; `amends` from-nodes are mapped to cards through `cards.doc_filename` stem; no edge is inferred or reversed by the UI | no (the amendment-node mapping is internal) |
| O9 | **Freshness is a card-level, on-demand, read-only comparison.** `freshness_check(scan, db_path)` compares a librarian scan (`scan.cards`) with the index and is "PURE READ"; the scan comes from `RHACO_tool_catalog_librarian.scan_library(root, output_path, logger)` / `walk_library(root)`, which read the whole corpus and write nothing | DISPATCH_PARAMETERS.md item H; I1 section 4 (`freshness_check` lines 2193-2199, `FreshnessReport` fields); section 0 item 12 VERIFY notes on `scan_library` | Freshness runs only when the operator asks (diagnostics action), reports `FRESH` / `DRIFT` with `added` / `removed` / `sha_changed`, and never triggers a rebuild; it covers card metadata, not body text (documented on the diagnostics page) | yes, minor -- freshness is on-demand and card-level (stated on the page; no SCOPE row because no required capability is lost) |
| O10 | **The live index moves under the explorer, and `connect()` would create a missing db.** External actors reindex `corpus_index.db` while the explorer runs (librarian gate runs, chat-side MCP pair writes -- both observed in session 3); `connect()` creates the parent dir and file if absent and applies DDL idempotently | section 0 item 5 reconciliation (findings file, "S3 item 5"); I1 section 4 (`connect` lines 779-799) | The adapter opens a connection per request through `connect()` and caches nothing across requests except a short-lived (<= 5 s) index_meta snapshot; **before any `connect()` the adapter requires the configured db file to exist**, refusing with a typed configuration error otherwise (so a misconfigured path can never create a stray database); diagnostics shows `last_full_reindex_utc`, `corpus_cards_sha`, and counts read live | no |
| O11 | **Dependency policy.** Runtime: the workspace venv on the Anaconda 3.13.9 interpreter with `fastapi`, `uvicorn`, `jinja2`, `playwright` (Chrome for Testing 151), `pytest`, plus `pyyaml` (the librarian's `import yaml`) and `sqlite-vec` (the module's vector channel); no CDN or network asset; HTMX not adopted in V1 | section 0 item 11 (findings file); PROMPT.md Proposed Stack; Standing Constraint 5 | Server-rendered Jinja2 pages with plain HTML forms; the only JavaScript is a small vendored-free vanilla file for the graph view and keyboard shortcuts; the stack deviation (no HTMX) is recorded in ARCHITECTURE.md with its reason (no external asset, simpler probe) | no |
| O12 | **Process rules.** Only the workspace venv interpreter runs workspace code; the probe launches the server as a foreground child, waits on readiness, and tears it down; no `run_in_background`; nothing under `C:\RHACO\rhaco\` is imported except `RHACO_corpus_index`, and `RHACO_tool_catalog_librarian` only for read-only walk/scan; no device transport; no `C:\RHACO\data\` access | DISPATCH_PARAMETERS.md items K and L (D-12 + D-3 blocks) | `tools/probe_corpus_explorer.py` owns server lifecycle; no other component starts the server; tests never spawn the server except through the probe's helper; builders inherit these rules in every strand prompt | no |
| O13 | **Junction path duality.** `C:\RHACO` is an NTFS junction to `C:\highsierralabs\RHACO`; the module's constants use the junction spelling, but index rows can be spelled either way (the A1 card row carried the physical path until the next full reindex) | section 0 item 5 (findings file, "S3 item 5"); I3 deviations note (junction verified) | Path comparison in the adapter is done on `os.path.realpath` + `os.path.normcase` forms; display uses the junction spelling; the docs-root confinement check (O1) accepts both spellings of the same physical directory | no |
| O14 | **Vocabulary drift between spec text and implementation.** The librarian's `FORMALIZED_DOC_TYPES` has 25 codes (adds `GDE`) against the Card Schema v1.9 text's 24; `RETIRED_DOC_TYPES` includes `BLD` (retired 2026-06-14) which the spec text does not name | I2 section 7 (librarian lines 424-438 vs spec section 6.1 lines 313-320) | Facet values (doc types, statuses, programs, tags, lifecycle states) are derived from the index's distinct values (what the corpus contains), never from a hardcoded spec list; labels for retired codes are shown raw; the drift is reported to the Director in the handback, not resolved by the build | no |
| O15 | **Body-index scope.** `fts_docs` covers `.md` and `.txt` bodies under `BODY_SCAN_ROOT` excluding `BODY_DENY_DIRS` (`.git`, `__pycache__`, `deprecated`, `docs_archive`, `drafts`, `logs`, `templates`, `_staging`); the card census excludes the librarian `DENY_DIRS` (`docs_archive`, `drafts`, `logs`, `templates`, `_staging`) | I1 section 2 (lines 272-278); I2 section 1 / findings "S2 item 12" (`DENY_DIRS`) | The explorer's corpus is the census: documents outside the walk are not listed; diagnostics states the scope rules; a `.docx` / `.pdf` / `.ipynb` document has a card row but no body row -- lexical search cannot find it and the reader shows card + path without inline rendering | yes -- SCOPE "Inline rendering of non-markdown documents" |
| O16 | **Compatibility gate, not re-evaluation.** Recall@10 through the adapter's hybrid path on Gold v1.1 (40 rows) must not fall below 0.700 aggregate, computed by the build's own L4 oracle from module-returned doc ids; Gold v1.2 is profiled, never gated | DISPATCH_PARAMETERS.md item E; docs/REFERENCE.md section 3 | `tools/l4_gold_oracle.py` runs `search_hybrid` per gold row through the adapter and scores `any-target-in-top-10`; a result below 0.700 is a `SCOPE.md` regression row with cause, never silent; the module is not re-evaluated | no |
| O17 | **Rerank halt path is never reachable.** `RerankModelPinMismatch` propagates as a HALT from `search_hybrid_rerank`; `RerankUnavailable` degrades to plain hybrid | I1 section 3 (lines 417-430) and section 6 | The explorer never calls `search_hybrid_rerank`; diagnostics may report whether the pinned artifact directory exists (path existence only, no load) | covered by S3 |
| O18 | **Gold emission is a write.** `emit_gold_queries` writes `C:\RHACO\working\eval\gold_queries.yaml` | I1 section 4 and 9 | Never called (one of the five names, O2); the oracle reads the frozen YAML only | no |
| O19 | **Encoding and console.** Corpus files are UTF-8 with em dashes, section signs, box drawing; the Windows console is cp1252 | RHACO CLAUDE.md section 6 (orchestrator pre-flight read); PROMPT.md title line (em dash) | Server responses are `charset=utf-8`; templates preserve the document's characters verbatim; every tool `print()` and probe log is ASCII-only with `PYTHONIOENCODING=utf-8` set by the probe for child processes; JSON observations are UTF-8 | no |
| O20 | **Search-result truncation.** `search_fts` caps at `max_results` (default 100, hard `MAX_SEARCH_RESULTS = 200`) and reports `total_matched`, `returned`, `truncated`; hybrid returns `top_k` (default 10) | I1 sections 2 and 4 | The search page shows returned vs total and the truncation flag; paging on the lexical channel is done by raising `max_results` within the module cap, never by re-ranking client-side | no |

## C. Corpus conventions (from inspection strand I4, `docs/rounds/R00_I4_corpus_conventions.report.md`)

| ID | Constraint | Basis | Design consequence | User-visible limitation |
|---|---|---|---|---|
| O21 | **Identifier grammar of record.** Filenames: `^RHACO-([A-Z]+)-(\d{8})-(\d+)(?:_[^.]*)?\.(md|docx|pdf|ipynb)$` (librarian `CANONICAL_FILENAME_RE` lines 610-613; module `_CANONICAL_FILENAME_RE` lines 285-286); canonical id `RHACO-{TYPE}-{YYYYMMDD}-{SEQ:03d}`; 25 active type codes (`EVT FRB GLE ANL CAL FIX OBS HWR SPC NET RDM HND EXT DLB CCX CHG MTN CMP INC SOP CHR COR FND GDE PUB`), retired `THR SCA BLD`; versioned reference documents are `RHACO_{Name}_v{X_Y}.md` (living documents; prior versions relocate to `docs_archive`); SOPs are the one dated type that carries `_vN` in the filename | I4 section 1 (Naming Convention v1.17 lines 13-23, 68-104, 288-298, 348; Style Guide v1.9 lines 384-435) | The identifier parser accepts exactly the module's three forms (`_RESOLVE_FULL_RE`, `_RESOLVE_BARE_RE`, `_RESOLVE_EVENT_RE`) and hands them to `resolve_identifier`; any other text goes to lexical/hybrid; reference-document names are matched by the catalog (`doc_id` prefix / title) and, where a `supersedes` chain exists, the reader offers the chain head ("current version") beside the requested version | covered by SCOPE "Identifier mode for reference-document names" |
| O22 | **Bare reference names resolve to the supersession head.** Gold v1.2 rows use `resolve: supersession-head` for `RHACO_Card_YAML_Schema_Specification`, `RHACO_Glossary`, `RHACO_Naming_Convention`; the documented MCP tool `traverse_supersession_chain(doc_id, direction)` walks `supersedes` edges to the head | I4 section 1 (gold_queries_v1_2.yaml lines 166-171, 230-236, 289-294) and section 2 item 11 | Lineage exposes a supersession-chain view (recursive CTE over `supersedes`, D-Q9) with "current head" marked; the reader banner on a `Superseded` document links to the head; the L4 oracle honors `resolve: supersession-head` on Gold v1.1 rows that carry it | no |
| O23 | **Anchor conventions a jump-to-section must resolve.** Section-numbered headings (`section 4.2` / the section sign form), CMP milestone anchors (`M1`, `M3-A.2`, `M4b`), amendment anchors (`A1`, `A1.3`), drift/gate anchors (`D1`, `G1`, `CS-1`), and named phase/step headers with no number (`#### Phase 1 Step 0 -- ...`); the librarian-generated Document Catalog links rows by GitHub-flavored-markdown heading slugs | I4 section 3 (Style Guide v1.9 lines 462-476, 688-698; MCP SOP v2 lines 116-266, 303-312; Document Catalog line 65) | The reader assigns every heading a GFM slug id and a line number; a search hit opens at its `line_no` (lexical) and the table of contents lists headings with their slugs; a free-text anchor request ("section 4.2", "M1", "A1.3") is matched against heading text and the milestone/amendment tokens inside headings, with an explicit "anchor not found" state instead of a silent top-of-page landing | no |
| O24 | **Frozen / live partition.** Walk exclusions: librarian `DENY_DIRS = {docs_archive, drafts, logs, templates, _staging}` (basename, case-insensitive); body-index `BODY_DENY_DIRS` adds `.git`, `__pycache__`, `deprecated`; `docs\docs_archive\` holds superseded prior versions and archived drafts (walk-excluded, "no longer a catalog member"); `docs\_staging\` is the MCP write quarantine ("nothing in staging is canonical or live"); `status` values `Superseded` and `Archived` mean frozen, `Baseline` means settled-but-current; a `Superseded` card must carry `superseded_by` | I4 section 4 (librarian lines 440-441, 495, 809; MCP SOP v2 line 307-312; CHG-20260531-003; HND-20260716-002 lines 32-54) | The catalog shows `status` with a frozen marker for `Superseded` / `Archived`; nothing outside the census is listed; diagnostics states the exclusion sets read live from the module and librarian constants (import-time constants, read-only); the explorer never lists `_staging` or `docs_archive` | no |
| O25 | **Documented public read surface to mirror.** `read_corpus` (byte-range, UTF-8-boundary-safe), `list_dir`, `find_files`, `get_card` (amendment-aware id resolution), `query_catalog`, `search_corpus` (FTS5, `doc_type_filter` / `path_glob`, `engine` field, caps 5 / 200), `traverse_supersession_chain`, `traverse_campaign_subtree` (cap 200, `counts_by_doc_type`), `query_dangling_references` (`edges WHERE resolved=0`, cap 200) | I4 section 2 (Tool Reference v1.35 lines 330-353) | The adapter's read operations map one-to-one onto these documented behaviors (D-Q9 supersession chain, D-Q10 campaign subtree, D-Q11 dangling references, the same caps); where the Tool Reference says a tool wraps a module function, the adapter calls that function; the server source itself is out of bounds (O1) | no |
| O26 | **An "Event Viewer" already exists and is a different product.** The corpus carries the Event Viewer extraction tool and its SOP (RHACO-SOP-20260504-001 Event Viewer Extraction Tool Use v2; RHACO-BLD-20260504-001/002/003; RHACO-HND-20260503-011/012/013): an event-window data-extraction viewer over `castle_event_log.csv`, not a document browser. (I4 saw these only at the junction's physical path and did not read them; existence confirmed by the orchestrator from the corpus listing.) | I4 section 5; corpus directory listing 2026-09-04 | The product is named "RHACO Corpus Explorer" everywhere; no route, title, or label uses "Event Viewer"; the diagnostics page states the explorer reads documents and cards only | no |
| O27 | **Documentation drift is a corpus fact, not a build defect.** Tool Reference v1.35's MCP binding line cites `LIBRARIAN_VERSION=1.13` / `CURRENT_NC_VERSION=1.16` while the on-disk librarian carries `1.19` / `1.17`; the Glossary's "walker-exempt" list differs from `DENY_DIRS`; the librarian's `VALID_LIFECYCLE_STATES` includes `ACTIVE-STANDING` (Card Schema v1.9 section 5.1 lists it; the Naming Convention prose does not) | I4 sections 4 and 7; I2 section 1 | Diagnostics shows versions read live from `index_meta` (`librarian_version`, `current_nc_version`, `index_schema_version`) rather than from documentation; vocabularies come from the index (O14); the drifts are reported to the Director in the handback | no |
| O28 | **House typography.** UTF-8 throughout (HND Specification v1.1 line 822); prose uses em dashes, section signs, multiplication signs, arrows, degree signs; Windows paths appear in backticks with backslashes; no box-drawing characters found; PDF identity (deep purple `#4B286D`, Arial) is optional for a markdown-first UI | I4 section 6 | Rendering preserves every character; monospace spans keep backslashes; the UI palette may echo `#4B286D` as an accent but the body text is system sans-serif for legibility; console output of every tool stays ASCII (O19) | no |
| O29 | **`search_fts` filter argument shape.** `RHACO_corpus_index.search_fts(query, doc_type_filter=...)` treats `doc_type_filter` as an iterable of type codes (`{t.upper() for t in doc_type_filter}`); a bare string is iterated character by character (`"EVT"` -> `{"E","V","T"}`), matching no real code and returning zero hits silently | round-2 search builder, `docs/rounds/R02_search.report.md` Evidence #1 (fixture probe: bare string `total_matched=0`; list `total_matched=4`) | Callers pass a one-item list; `explorer/search/service.py` wraps the single query-string value before calling `adapter.search_lexical`; the adapter passes the argument through unchanged (a normalization there would be a round-3 adapter change request) | no (internal call shape) |
