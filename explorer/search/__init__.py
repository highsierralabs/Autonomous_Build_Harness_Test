"""Search module (builder-owned: search). ARCHITECTURE.md section 4.3; PROMPT.md 5.2/5.3.

The human search surface over the adapter's existing retrieval modes
(identifier, lexical, hybrid, graph) -- never a write path, never a
reimplementation of ranking.
"""
