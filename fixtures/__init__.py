"""fixtures/ -- probe-owned fixture substrate (ARCHITECTURE.md section 2).

Package marker only, so `from fixtures.build_fixture_index import build` is a
plain import regardless of pytest's rootdir/import-mode. Never named alongside
the index-writing functions (CONSTRAINTS.md O2) -- see build_fixture_index.py.
"""
